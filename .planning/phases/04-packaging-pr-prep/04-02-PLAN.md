---
phase: 4
plan: 04-02
wave: 2
depends_on: [04-01]
requirements:
  - PKG-02 (owned — completion: the in-tree arrangement exists and is proven; 04-01 contributed the docs half)
  - PKG-03 (owned — parity re-verified at the PR-time upstream main SHA)
  - PKG-04 (owned — PR branch + PR_BODY.md prepared; submission BLOCKED pending Dr. Mani sign-off)
files_modified:
  - .planning/phases/04-packaging-pr-prep/04-PARITY.md
  - .planning/phases/04-packaging-pr-prep/PR_BODY.md
  - .planning/phases/04-packaging-pr-prep/04-SIGNOFF.md
  - /Users/manisaintvictor/.hermes/worktrees/pr-anansi/plugins/anansi/ (new, worktree branch only)
  - /Users/manisaintvictor/.hermes/worktrees/pr-anansi/tests/plugins/anansi/ (new, worktree branch only)
autonomous: true
single_layer_justified: true  # packaging/PR-prep phase — the deliverable IS the arrangement + artifacts, not a feature slice
objective: "An upstream-ready PR branch on a worktree based at the freshly-verified upstream/main SHA — plugin in-tree under plugins/anansi, full suite green under upstream's own pytest standards — plus PR_BODY.md and a single top-to-bottom sign-off artifact for Dr. Mani; NOTHING is pushed to any remote."
must_haves:
  - "04-PARITY.md records the PR-time upstream/main SHA and the diff verdict vs 183d86b3e for hermes_cli/plugins.py, agent/plugin_llm.py, tests/agent/test_plugin_llm.py, and plugins/ — including the provides_hooks manifest-key answer and the complete_structured signature check"
  - "Worktree /Users/manisaintvictor/.hermes/worktrees/pr-anansi exists on branch feat/anansi-plugin based at the recorded SHA, with exactly one commit adding only plugins/anansi/* and tests/plugins/anansi/*"
  - "The plugin suite passes inside the worktree using upstream's pytest config (addopts: -m 'not integration' --timeout=30 --timeout-method=thread) with the hermes venv python — pass count recorded verbatim in 04-SIGNOFF.md"
  - "An in-tree layout/discovery test (modeled on tests/plugins/test_langfuse_plugin.py) is part of the worktree suite and green"
  - "PR_BODY.md committed in this repo: evidence from 02/03-VALIDATION, honest limitations, icarus-regex cross-reference, post_memory_prefetch as future work"
  - "04-SIGNOFF.md committed: branch+SHA, verbatim diff stat, verbatim suite result, PR body, exact submit commands — and an explicit STOP line; awaiting Dr. Mani approval"
  - "ZERO pushes: git ls-remote origin feat/anansi-plugin returns empty; upstream push remains DISABLED; no gh pr create executed"
---

# Plan 04-02: Parity Re-Check, In-Tree Arrangement, PR Prep — Stop at Sign-Off

## Objective

Re-verify host parity at the actual PR-time upstream/main SHA (PKG-03), arrange the plugin
in-tree on a worktree branch cut from that SHA and prove it to upstream's own test standards
(PKG-02 completion), and prepare — but never submit — the PR (PKG-04): PR_BODY.md plus a single
sign-off artifact Dr. Mani can read top-to-bottom, mirroring today's Hindsight #2117 flow.

## Context

**Read before implementing (binding):**
- `/Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/04-packaging-pr-prep/04-CONTEXT.md` — locked: branch from current upstream main; push via fork only; the run PREPARES, never submits
- `/Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/02-appraisal-path/02-VALIDATION.md` + `/Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/03-failopen-reflection/03-VALIDATION.md` — PR_BODY evidence sources
- `/Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/02-appraisal-path/02-01-SUMMARY.md` — the icarus-regex bug detail ("Decisions made": original icarus regex missed "ignore previous instructions" without "all"; a system-prompt-exfiltration pattern was also added — upstream-relevant for icarus)

**Upstream facts (ALL verified 2026-06-10 ~8:25pm EDT during planning — re-verify the ones marked PR-TIME):**
- Local checkout `~/.hermes/hermes-agent`, branch `local-desktop-fixes`. Remotes:
  `origin` = `https://github.com/bionicbutterfly13/hermes-agent-lab.git` (push allowed),
  `upstream` = `https://github.com/NousResearch/hermes-agent.git` with **push DISABLED**.
- `upstream/main` at planning time = `d1383a6b1450c6c139720b1b01f8b99cc130453f` (33 commits past
  183d86b3e). **PR-TIME: fetch fresh and use whatever SHA main is at then.**
- Parity at d1383a6b1: `git diff --stat 183d86b3e upstream/main -- hermes_cli/plugins.py
  agent/plugin_llm.py tests/agent/test_plugin_llm.py plugins/` is EMPTY — all four surfaces
  byte-identical since the Phase-1 verification. **PR-TIME: re-run this exact diff.**
- Manifest key: the loader reads `provides_hooks` (`hermes_cli/plugins.py:1386`,
  `data.get("provides_hooks", [])` into `PluginManifest.provides_hooks`); the `hooks:` key seen
  in some in-tree manifests (langfuse, disk-cleanup) is NOT read by any non-test code — it is
  informational metadata. Our `provides_hooks` key is the correct one. Keep it.
- In-tree layout convention: `plugins/<name>/` containing `__init__.py` + `plugin.yaml` +
  optional `README.md` (+ extra modules). Discovered as `source="bundled"` from the repo
  `plugins/` dir (plugins.py:1089-1097; top-level dirs + `plugins/platforms`). Bundled
  `kind: standalone` plugins are opt-in (the langfuse test explicitly guards "not loaded unless
  explicitly enabled"). Both hyphen and underscore dir names exist upstream.
- Loader import mechanism (plugins.py:1535-1549): plugins load as `hermes_plugins.<slug>`
  packages via `spec_from_file_location(..., submodule_search_locations=[plugin_dir])` — our
  plugin's relative imports (`from . import appraisal, store`) work identically in both layouts.
- Host tests for in-tree plugins live under `tests/plugins/` — flat `test_<name>_plugin.py` files
  AND subpackage dirs (`tests/plugins/memory/`, `tests/plugins/image_gen/`, both with
  `__init__.py`). `tests/plugins/test_langfuse_plugin.py` is the layout/discovery template
  (manifest assertions + opt-in discovery, locating the plugin via
  `REPO_ROOT / "plugins" / ...`).
- Pytest config (pyproject.toml:324-335): `testpaths = ["tests"]`,
  `addopts = "-m 'not integration' --timeout=30 --timeout-method=thread"`. Canonical runner
  `scripts/run_tests.sh` (per-file isolation, env vars blanked, TZ=UTC, venv probe order:
  `.venv`, `venv`, `~/.hermes/hermes-agent/venv`).
- **The hermes venv has NO pytest and NO pytest-timeout** (verified by import probe); it DOES have
  jsonschema 4.26.0 and PyYAML 6.0.3. Upstream dev deps pin `pytest==9.0.2`,
  `pytest-timeout==2.4.0`, `pytest-asyncio==1.3.0` (pyproject.toml:134). The hermes-agent package
  is installed editable from `~/.hermes/hermes-agent` — worktree test runs MUST put the worktree
  root first on `sys.path` so `agent`/`hermes_cli` resolve to upstream-main code, not the
  local-desktop-fixes checkout.
- `complete_structured` on upstream/main (agent/plugin_llm.py): keyword-only
  `(instructions, input, json_schema, json_mode, schema_name, system_prompt, provider, model,
  temperature, max_tokens, timeout, agent_id, profile, purpose)`; `PluginLlmTrustError` and
  `make_plugin_llm_for_test` (plugin_llm.py:1016) both present.
- Worktree precedent: `~/.hermes/worktrees/pr-credorder`, `pr-memwarn`, `pr-testfix` already
  exist from today's earlier PRs — follow the same pattern.

**Hard rules:**
- `~/.hermes/hermes-agent` is READ-ONLY except: `git fetch upstream`, creating the worktree +
  branch, and commits INSIDE the new worktree. Never modify the main working tree or its branch.
- NEVER touch `/Users/manisaintvictor/.hermes/plugins/anansi`. Never break the live symlink
  `~/.hermes/plugins/anansi`.
- **NO PUSH to any remote. NO `gh pr create`.** The sign-off artifact carries the commands;
  Dr. Mani executes or approves them. (04-CONTEXT/analysis §6: "nothing pushes without explicit
  approval".) Verify `upstream` push is still DISABLED before finishing.
- One-time dev-tooling staging via `uv pip install --target` is network-exempt (03-01 precedent);
  plugin tests themselves need zero network.
- Plugin-repo commits per task with `git -C /Volumes/Asylum/repos/hermes-anansi-plugin ...`;
  worktree commit with `git -C /Users/manisaintvictor/.hermes/worktrees/pr-anansi ...`.

## Tasks

<task id="04-02-01">
<name>PKG-03 parity re-check at the PR-time SHA</name>
<files>
- .planning/phases/04-packaging-pr-prep/04-PARITY.md
</files>
<action>
1. `git -C ~/.hermes/hermes-agent fetch upstream` (read-only; allowed). Record
   `git -C ~/.hermes/hermes-agent rev-parse upstream/main` — this is THE PR-time SHA used by
   task 2.
2. Re-run the parity diff against the Phase-1 baseline:
   `git -C ~/.hermes/hermes-agent diff --stat 183d86b3e <SHA> -- hermes_cli/plugins.py agent/plugin_llm.py tests/agent/test_plugin_llm.py plugins/`
   - Empty output → parity holds, record it.
   - Non-empty → inspect each hunk (`git diff 183d86b3e <SHA> -- <file>`); specifically verify:
     `provides_hooks` still read in `_parse_manifest`; `complete_structured` keyword-only
     signature still a superset of every kwarg `anansi/appraisal.py` and
     `reflection.py` actually pass (grep both call sites and compare); `PluginLlmTrustError` +
     `make_plugin_llm_for_test` still exported. If any surface the plugin depends on changed
     incompatibly: STOP the plan and report — do not adapt plugin code silently (Phase 4 is
     packaging; an API break is a blocker for Dr. Mani, not a silent fix).
3. Write `04-PARITY.md` (concise, evidence-first): date/time, PR-time SHA, baseline SHA
   183d86b3e, the verbatim diff-stat output (or "empty"), the manifest-key verdict (loader reads
   `provides_hooks`; in-tree `hooks:` keys are unread metadata — cite plugins.py line), the
   `complete_structured`/trust-gate verdict, and the conclusion sentence for PR_BODY.
4. Commit:
   `git -C /Volumes/Asylum/repos/hermes-anansi-plugin add .planning/phases/04-packaging-pr-prep/04-PARITY.md && git -C /Volumes/Asylum/repos/hermes-anansi-plugin commit -m "docs(04-02): PKG-03 parity re-check at upstream main <short-sha>"`
</action>
<verify>
`04-PARITY.md` exists, contains a 40-char SHA, the verbatim diff command + output, and explicit
verdicts for the manifest key and `complete_structured`. The hermes-agent main working tree is
untouched (`git -C ~/.hermes/hermes-agent status --short` clean, branch still
`local-desktop-fixes`). Commit exists.
</verify>
<done>[ ]</done>
</task>

<task id="04-02-02">
<name>In-tree arrangement on a worktree branch + suite to host standards</name>
<files>
- /Users/manisaintvictor/.hermes/worktrees/pr-anansi/plugins/anansi/ (new: __init__.py, appraisal.py, config.py, reflection.py, render.py, store.py, plugin.yaml, README.md)
- /Users/manisaintvictor/.hermes/worktrees/pr-anansi/tests/plugins/anansi/ (new: __init__.py, conftest.py [adapted], all test_*.py, fixtures/, test_intree_layout.py [new])
- /Volumes/Asylum/repos/hermes-anansi-plugin/.devtools/pytest (staging only, gitignored)
</files>
<action>
**1. Preconditions (verify, do not assume):** `git -C ~/.hermes/hermes-agent remote -v` shows
origin = fork (push allowed) and upstream push DISABLED; `~/.hermes/worktrees/` exists.
EXECUTOR CONSTRAINT (plan-check hardening): the strings `git push` / `gh pr create` may appear
in this run ONLY inside 04-SIGNOFF.md fenced code blocks — never as executed commands.
MODULE-IDENTITY RULE: never import the plugin via `plugins.anansi` (upstream's
`plugins/__init__.py` makes it importable); tests use the conftest-registered `anansi`
name only — three module identities in one process is the trap.

**2. Create the worktree at the task-1 SHA:**
`git -C ~/.hermes/hermes-agent worktree add /Users/manisaintvictor/.hermes/worktrees/pr-anansi -b feat/anansi-plugin <SHA>`

**3. Arrange the plugin in-tree** (copy from the plugin repo — post-04-01 state, working tree
clean): create `<worktree>/plugins/anansi/` with exactly the six source modules
(`__init__.py`, `appraisal.py`, `config.py`, `reflection.py`, `render.py`, `store.py`) +
`plugin.yaml` + `README.md`. EXCLUDE `tests/` and `__pycache__`. Keep `provides_hooks` in
plugin.yaml (the loader-read key — task-1 verdict).

**4. Arrange the tests** at `tests/plugins/anansi/` (subpackage precedent:
`tests/plugins/memory/`): add an empty `__init__.py`; copy every `test_*.py` module and the
`fixtures/` dir unchanged; copy `conftest.py` with ONE adaptation — replace the repo-root
sys.path block (conftest.py:16-18) with:
   - insert the WORKTREE root (`Path(__file__).resolve().parents[3]`) at `sys.path[0]` so
     `agent`/`hermes_cli` resolve to upstream-main code, shadowing the editable install of the
     local branch (parity-critical);
   - load `plugins/anansi` as importable package `anansi`:
     `importlib.util.spec_from_file_location("anansi", <worktree>/plugins/anansi/__init__.py, submodule_search_locations=[<dir>])`,
     register in `sys.modules` BEFORE `exec_module` (relative imports require it), then exec.
   Everything else in conftest (DIRECTIVE_PATTERNS, helpers) stays byte-identical.
   If any test hardcodes a path relative to the old layout, fix it the same way (grep for
   `parents[2]`/`parents[1]` first).

**5. Add `test_intree_layout.py`** (worktree-only module, modeled on
`tests/plugins/test_langfuse_plugin.py` TestManifest/TestDiscovery): assert the
`plugins/anansi/` dir layout (plugin.yaml + __init__.py + README.md present); manifest
fields (`name == "anansi"`, `kind == "standalone"`, `pip_dependencies == []`,
`provides_hooks` == the four hooks); and discovery-as-opt-in via the upstream loader with a
tmp `HERMES_HOME` (mirror the langfuse test's arrangement — read it first).

**6. Stage dev tooling** (plugin repo's gitignored staging dir, NOT the worktree, NOT the venv):
`uv pip install --python ~/.hermes/hermes-agent/venv/bin/python --target /Volumes/Asylum/repos/hermes-anansi-plugin/.devtools/pytest pytest==9.0.2 pytest-timeout==2.4.0 pytest-asyncio==1.3.0`
(upstream's pinned dev versions; pytest may already be staged — re-stage to get the pins).

**7. Run the suite to host standards** from the worktree root so upstream's pyproject addopts
(`-m 'not integration' --timeout=30 --timeout-method=thread`) and `tests/conftest.py`
(env blanking) apply:
`cd /Users/manisaintvictor/.hermes/worktrees/pr-anansi && PYTHONPATH="$PWD:/Volumes/Asylum/repos/hermes-anansi-plugin/.devtools/pytest" ~/.hermes/hermes-agent/venv/bin/python -m pytest tests/plugins/anansi -q`
All tests green (expect ≥ the plugin repo's count + the new layout tests). If upstream's
`tests/conftest.py` needs another dev dep at import time, stage it the same way and record it.
Capture the verbatim pass line for 04-SIGNOFF.md. Also run one host-side sanity file to prove the
arrangement doesn't disturb the loader's own tests:
`PYTHONPATH=... ... -m pytest tests/hermes_cli/test_plugins.py -q` — green.

**8. Single focused commit in the worktree** (CONTRIBUTING: one logical change per PR):
`git -C /Users/manisaintvictor/.hermes/worktrees/pr-anansi add plugins/anansi tests/plugins/anansi && git -C ... commit -m "feat(plugins): add anansi — observational metacognitive appraisal (opt-in, zero deps, fail-open)"`
Verify nothing else is staged/dirty (`git status --short` clean afterward; `.devtools` lives
outside the worktree by design). **DO NOT PUSH.** Immediately after the commit, run
`git ls-remote origin feat/anansi-plugin` and confirm empty (in-action check, not
just post-hoc verify).
</action>
<verify>
`git -C /Users/manisaintvictor/.hermes/worktrees/pr-anansi log --oneline upstream/main..HEAD`
shows exactly one commit; `git diff upstream/main...HEAD --stat` touches only
`plugins/anansi/*` and `tests/plugins/anansi/*`. The pytest command exits 0 with
all tests passed under the 30s-per-test cap. `tests/hermes_cli/test_plugins.py` green.
`git -C ~/.hermes/hermes-agent status --short` still clean on `local-desktop-fixes`.
`git ls-remote origin feat/anansi-plugin` returns empty (nothing pushed).
Live symlink `~/.hermes/plugins/anansi` still resolves to the plugin repo.
</verify>
<done>[ ]</done>
</task>

<task id="04-02-03">
<name>PR_BODY.md + sign-off artifact — STOP at sign-off</name>
<files>
- .planning/phases/04-packaging-pr-prep/PR_BODY.md
- .planning/phases/04-packaging-pr-prep/04-SIGNOFF.md
</files>
<action>
**PR_BODY.md** (the literal body for the upstream PR — maintainer audience, honest, concise;
length/tone at executor's discretion). Required content (04-CONTEXT locked sources):
1. What: opt-in `kind: standalone` plugin; one host-owned `ctx.llm.complete_structured` JSON-mode
   appraisal per eligible turn; sentinel-prefixed observational block into the user message only;
   debounced idempotent reflection pass; SQLite (WAL) state under `$HERMES_HOME`;
   `pip_dependencies: []` (stdlib + host surfaces, enforced by an AST import-allowlist test).
2. Safety posture: fail-open law (executor-bounded deadline, every failure → empty injection +
   telemetry row), anti-creep suite (no directive language, no tool execution, no memory-provider
   writes, no turn gating), opt-in with a config kill switch — zero behavior change unless enabled.
3. Evidence (from 02/03-VALIDATION — cite, don't inflate): hermetic suite count from task 2's run;
   live p50 5563.5ms within the 8.0s deadline; contradiction fixture detection 6/6 with 0/3 false
   positives; cross-session one-turn-lag loop demonstrated live on attempt 1/1; parallel
   sub-session timeouts failed open with turns intact. Include the dry-run demo transcript excerpt
   (test_dryrun_demo.py / the recorded demo block) and a compact telemetry summary (outcome
   distribution) — both are 04-CONTEXT-locked sources.
4. Honest limitations: one-turn lag (deliberate; reflection carries context across it);
   sub-session hook traffic and contention; deadline/latency trade-off on cheap-tier models;
   trust-fallback degrades to fail-open timeout on slow host models.
5. Parity note: built and tested against upstream/main at <SHA> (04-PARITY.md conclusion).
6. Cross-references: the icarus injection-regex gap found during development (original pattern
   missed "ignore previous instructions" without "all"; a system-prompt-exfiltration pattern was
   also needed — see 02-01-SUMMARY.md; offer as a separate fix). Future work, not in this PR:
   a `post_memory_prefetch` host hook would close the one-turn lag (mention only).
7. Reviewer instructions: enable command, config block pointer (plugins/anansi/README.md),
   test command for the plugin's suite.

**04-SIGNOFF.md** — the single top-to-bottom file for Dr. Mani (mirror today's Hindsight #2117
flow). Sections in order:
1. Header: prepared <date>, awaiting Dr. Mani sign-off; branch `feat/anansi-plugin`;
   worktree path; base = upstream/main @ <SHA>.
2. Diff stat: verbatim `git diff upstream/main...HEAD --stat` output from the worktree.
3. Suite proof: the verbatim pytest pass line(s) from task 2 (plugin suite + loader sanity file)
   and the exact commands that produced them.
4. Parity: 2-3 line summary + pointer to 04-PARITY.md.
5. The PR body: full inline copy of PR_BODY.md.
6. How to submit (commands NOT executed by this run — fenced, copy-paste ready):
   - `git -C /Users/manisaintvictor/.hermes/worktrees/pr-anansi push -u origin feat/anansi-plugin`
   - `gh pr create --repo NousResearch/hermes-agent --head bionicbutterfly13:feat/anansi-plugin --title "..." --body-file <path-to-PR_BODY.md>`
7. Final line, verbatim intent: "STOP — nothing has been pushed to any remote. Submission
   requires Dr. Mani's explicit approval."

Commit both files:
`git -C /Volumes/Asylum/repos/hermes-anansi-plugin add .planning/phases/04-packaging-pr-prep/PR_BODY.md .planning/phases/04-packaging-pr-prep/04-SIGNOFF.md && git -C /Volumes/Asylum/repos/hermes-anansi-plugin commit -m "docs(04-02): PR_BODY + sign-off artifact — prepared, not submitted"`

**Final state checks (record results in the plan summary):** `git -C ~/.hermes/hermes-agent remote -v`
still shows upstream push DISABLED; `git ls-remote origin feat/anansi-plugin` empty;
plugin-repo suite still green via `./scripts/test.sh`; live symlink intact;
`~/.hermes/plugins/anansi` never entered.
</action>
<verify>
Both files exist and are committed. 04-SIGNOFF.md contains: a 40-char SHA, a verbatim diff stat
block, a verbatim test-pass line, fenced submit commands, and the STOP line.
`git ls-remote origin feat/anansi-plugin` returns empty (the real proof, with upstream
push still DISABLED — shell history is not reliably inspectable; rely on these checks).
`./scripts/test.sh` green in the plugin repo.
</verify>
<done>[ ]</done>
</task>

## Must-Haves

After all tasks complete, the following must be true:

- [ ] 04-PARITY.md: PR-time SHA recorded; provides_hooks + complete_structured + trust-gate verdicts explicit; diff vs 183d86b3e shown verbatim
- [ ] Worktree branch `feat/anansi-plugin` at the recorded SHA with exactly one commit touching only `plugins/anansi/*` + `tests/plugins/anansi/*`
- [ ] Plugin suite + `test_intree_layout.py` green inside the worktree under upstream's addopts (30s/test cap, not-integration marker) with the worktree root shadowing the editable install; `tests/hermes_cli/test_plugins.py` green alongside
- [ ] PR_BODY.md complete per the 7-point checklist; 04-SIGNOFF.md complete per the 7-section order, ending in the STOP line
- [ ] Nothing pushed: `git ls-remote origin feat/anansi-plugin` empty; upstream push still DISABLED; no PR opened
- [ ] hermes-agent main working tree clean on `local-desktop-fixes`; live symlink + guardrails plugin untouched; plugin-repo suite still green

## Success Criteria Mapping (ROADMAP Phase 4)

| Criterion | Coverage by this plan |
|-----------|----------------------|
| 1 — installs cleanly in both layouts | In-tree half: arrangement + discovery/layout test green in the worktree (standalone half: 04-01 docs + live symlink) |
| 2 — `pip_dependencies: []` verified; suite passes to host standards on upstream main | Suite run inside the worktree at the PR-time SHA under upstream pytest config; manifest assertions repeated in-tree by test_intree_layout.py |
| 3 — PR branch + PR_BODY.md ready; submission blocked pending sign-off | Tasks 2-3: branch prepared, PR_BODY.md + 04-SIGNOFF.md committed, zero pushes, explicit STOP |
