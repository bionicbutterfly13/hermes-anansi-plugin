---
phase: 4
plan: 04-01
wave: 1
depends_on: []
requirements:
  - PKG-01 (owned — zero-dep proof wiring: manifest test + SAFE-04 allowlist cross-reference)
  - PKG-02 (docs contribution only — install for both layouts, config block, WAL idiom, sub-session note; completion tracking owned by 04-02, which proves the in-tree arrangement)
  - OBS-01 carry-in polish (telemetry_summary reflect_* vocabulary — 03-VERIFICATION finding 1; not a Phase-4 requirement, sanctioned by 04-CONTEXT)
files_modified:
  - anansi/store.py
  - anansi/tests/test_telemetry_store.py
  - anansi/tests/test_anticreep.py
  - anansi/README.md
  - README.md
autonomous: true
single_layer_justified: true  # Phase 4 is a packaging/docs phase — no new user-facing behavior layers exist to slice through; the lone code change is a sanctioned one-function polish fix
objective: "The plugin repo is documentation-complete for both install layouts (config reference, WAL sidecar inspection idiom, sub-session behavior, honest limitations), PKG-01 is provable by running one test, and telemetry_summary stops mislabeling reflect_* outcomes as failures — suite green at 105+ throughout."
must_haves:
  - "store.py telemetry_summary counts reflect_ok and reflect_skipped:* as non-failures (failure vocabulary: timeout, llm_error, parse_fail, reflect_timeout, reflect_llm_error, reflect_parse_fail); last_error follows the same definition; p50_wall_ms semantics unchanged (ok/trust_fallback only)"
  - "A test seeds mixed appraisal+reflection outcomes and asserts failure_count and last_error are correct under the new vocabulary; suite green via ./scripts/test.sh with ≥106 tests"
  - "test_anticreep.py has a PKG-01 test asserting plugin.yaml parses with pip_dependencies == [], kind == standalone, name == anansi, and provides_hooks == exactly the four registered hooks; its docstring cross-references test_safe04_import_allowlist as the import-side half of the proof"
  - "anansi/README.md exists covering: overview/non-goals, standalone + in-tree install, the full plugins.entries.anansi config block (every key in config.py with default + clamp), outcome vocabulary + telemetry_summary, the WAL sidecar copy idiom, the sub-session note, and the honest limitations section (R1/R2/R3 + live timeout ratio)"
  - "Repo-root README.md exists (thin: what this repo is, dev test command, pointer to the plugin README)"
  - "Live deploy unaffected: ~/.hermes/plugins/anansi symlink untouched; no behavior change beyond the telemetry_summary derived view"
---

# Plan 04-01: Docs, Zero-Dep Proof, Telemetry Vocabulary Polish

## Objective

Make the plugin contributable on paper before 04-02 makes it contributable in-tree: write the
canonical plugin README (it ships verbatim into the upstream arrangement), prove PKG-01 with a
test instead of a claim, and close the one sanctioned carry-in code fix from 03-VERIFICATION
(telemetry_summary's failure vocabulary predates reflection and mislabels every reflect_* row).

## Context

**Read before implementing (binding):**
- `/Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/04-packaging-pr-prep/04-CONTEXT.md` — every decision LOCKED; Phase 4 is packaging, resist all other code changes
- `/Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/03-failopen-reflection/03-VERIFICATION.md` — findings 1 (vocabulary), 2 (sub-session timeouts), non-blocking observations 2 (timeout ratio) and 4 (WAL sidecar idiom)
- `/Volumes/Asylum/repos/hermes-anansi-plugin/.planning/DECISIONS.md` — R1/R2/R3 entries (the limitations section quotes these)

**Code facts (verified 2026-06-10 during planning):**
- `store.py:593-649` — `telemetry_summary`. Failure logic at :615-620 excludes only
  `("ok", "trust_fallback")` and prefix `skipped:`; the `last_error` query at :621-626 uses the
  same stale definition. `reflect_ok` and `reflect_skipped:*` therefore count as failures.
- Full outcome vocabulary (grep-verified): appraisal — `ok`, `trust_fallback`, `timeout`,
  `llm_error`, `parse_fail`, `skipped:<reason>` (appraisal.py:285-342); reflection —
  `reflect_ok`, `reflect_timeout`, `reflect_llm_error`, `reflect_parse_fail`,
  `reflect_skipped:<reason>` (reflection.py:327-381, :678-707).
- Config keys + defaults + clamps (config.py:39-150): `enabled` (True), `confidence_threshold`
  (0.6, [0.0,1.0]), `deadline_seconds` (8.0, [0.5,10.0]), `history_chars` (4000, ≥0),
  `llm.model` (None — the model override rides under the `llm:` sub-key), `max_tokens` (700, ≥1),
  `reflection_enabled` (True), `reflect_every_n_turns` (5, [1,50]), `reflect_max_tokens`
  (700, ≥1), `reflect_deadline_seconds` (8.0, [0.5,10.0]).
- `plugin.yaml`: name `anansi`, kind `standalone`, `provides_hooks` lists exactly
  `on_session_start`, `pre_llm_call`, `post_llm_call`, `on_session_end`; `pip_dependencies: []`.
- No README exists anywhere in the repo (root ls: AGENTS.md, CLAUDE.md, anansi/, scripts/).
- PyYAML 6.0.3 is in the hermes venv (the PKG-01 test may parse plugin.yaml with `yaml.safe_load`).
- Upstream-main loader fact for the README's in-tree section (verified against
  `upstream/main:hermes_cli/plugins.py`): the loader reads the `provides_hooks` manifest key
  (:1386); bundled `kind: standalone` plugins are opt-in (enable required), discovered from the
  repo `plugins/` dir (:1089-1097).

**Hard rules (unchanged):**
- NEVER touch `/Users/manisaintvictor/.hermes/plugins/anansi` (separate live guardrails plugin).
- The plugin is LIVE via symlink (`~/.hermes/plugins/anansi` → this repo's
  `anansi/`). Every commit leaves the suite green.
- No new pip deps; no literal `/Users/` paths; paths via `$HERMES_HOME`.
- Commit per task with `git -C /Volumes/Asylum/repos/hermes-anansi-plugin ...`.
- Test command: `cd /Volumes/Asylum/repos/hermes-anansi-plugin && ./scripts/test.sh`.

## Tasks

<task id="04-01-01">
<name>telemetry_summary reflect_* vocabulary fix + test</name>
<files>
- anansi/store.py
- anansi/tests/test_telemetry_store.py
</files>
<action>
**store.py — minimal vocabulary fix (nothing else in telemetry changes):**
1. In `telemetry_summary`, change the non-failure definition in BOTH places (the
   `failure_count` comprehension and the `last_error` SQL) to:
   - exact non-failures: `ok`, `trust_fallback`, `reflect_ok`
   - prefix non-failures: `skipped:`, `reflect_skipped:`
   Failures are exactly: `timeout`, `llm_error`, `parse_fail`, `reflect_timeout`,
   `reflect_llm_error`, `reflect_parse_fail` (and any future unknown outcome — keep the
   exclusion-list shape; do NOT switch to an inclusion list).
2. `p50_wall_ms` stays appraisal-only (`ok`/`trust_fallback`) — unchanged.
3. Update the docstring (:594-602) to state the new vocabulary explicitly.

**test_telemetry_store.py:**
4. Sweep existing summary tests for assertions encoding the old definition; update them.
5. Add one new test: seed a tmp DB with rows covering `ok`, `timeout`, `reflect_ok`,
   `reflect_skipped:debounce`, `reflect_timeout` (newest, with a distinct error string) →
   assert `failure_count == 2`, `last_error` equals the `reflect_timeout` row's error,
   `by_outcome` has all five keys, and `p50_wall_ms` reflects only the `ok` row.

Run the full suite. Commit:
`git -C /Volumes/Asylum/repos/hermes-anansi-plugin add -A && git -C /Volumes/Asylum/repos/hermes-anansi-plugin commit -m "fix(04-01): telemetry_summary counts reflect_* outcomes correctly (03-VERIFICATION carry-in)"`
</action>
<verify>
`./scripts/test.sh` green (≥106 tests). `grep -n "reflect_ok\|reflect_skipped" anansi/store.py`
hits inside `telemetry_summary` in both the comprehension and the SQL. The new test fails if the
old exclusion list is restored (mutation check by inspection). Commit exists.
</verify>
<done>[ ]</done>
</task>

<task id="04-01-02">
<name>PKG-01 zero-dep proof wiring</name>
<files>
- anansi/tests/test_anticreep.py
</files>
<action>
Add a PKG-01 manifest test to `test_anticreep.py` (extends the existing static-proof module —
no new file):

1. `test_pkg01_manifest_zero_deps_and_accurate_hooks`:
   - locate `plugin.yaml` relative to the `anansi` package dir (no literal paths);
   - `yaml.safe_load` it; assert `pip_dependencies == []`, `kind == "standalone"`,
     `name == "anansi"`;
   - assert `set(provides_hooks) == {"on_session_start", "pre_llm_call", "post_llm_call",
     "on_session_end"}`;
   - cross-check accuracy against the code: read `anansi/__init__.py` source and assert
     every listed hook name appears as a registered/defined hook (match the existing registration
     idiom — grep `__init__.py` first to mirror how `register(ctx)` wires hooks; the Phase-1 tests
     in this suite already exercise registration, reuse their arrangement if simpler).
2. Test docstring: state that PKG-01's import-side half ("stdlib + host surfaces only") is proven
   by `test_safe04_import_allowlist` (AST-based, same module — 03-VERIFICATION confirmed it
   pre-proves PKG-01), and this test proves the manifest side (`pip_dependencies: []` + an
   accurate `provides_hooks` list, the key upstream's loader actually reads —
   `hermes_cli/plugins.py:1386` on upstream/main).

Run the full suite. Commit:
`git -C /Volumes/Asylum/repos/hermes-anansi-plugin add -A && git -C /Volumes/Asylum/repos/hermes-anansi-plugin commit -m "test(04-01): PKG-01 manifest proof — zero pip deps + accurate provides_hooks"`
</action>
<verify>
`./scripts/test.sh anansi/tests/test_anticreep.py` green with the new test collected.
Temporarily-broken check is unnecessary; assertion shape is direct. Full suite green. Commit exists.
</verify>
<done>[ ]</done>
</task>

<task id="04-01-03">
<name>README pair — canonical plugin doc + thin repo doc</name>
<files>
- anansi/README.md
- README.md
</files>
<action>
**`anansi/README.md` — the canonical plugin README.** It lives inside the package dir
deliberately: it ships unchanged into the in-tree arrangement (`plugins/anansi/README.md`,
matching the upstream convention seen in `plugins/disk-cleanup/README.md` and
`plugins/observability/langfuse/README.md`). Required sections (binding content from 04-CONTEXT;
structure at executor's discretion):

1. **Overview + non-goals** — observational metacognitive appraisal; one JSON-mode LLM call per
   eligible turn via the host-owned `ctx.llm.complete_structured`; renders a sentinel-prefixed
   `[anansi appraisal]` advisory block into the user message only. Non-goals (locked anti-features):
   never executes tools, never writes memory providers, never gates/delays a turn, no directive
   language, no schedulers, fail-open everywhere.
2. **Install — standalone**: copy or symlink the `anansi/` directory to
   `$HERMES_HOME/plugins/anansi`, then `hermes plugins enable anansi`.
   Note `HERMES_PLUGINS_DEBUG=1` for load verification.
3. **Install — in-tree**: present under the hermes-agent repo's `plugins/anansi/`;
   bundled standalone plugins are opt-in — same enable command. (Do not overpromise: the in-tree
   arrangement is proven by plan 04-02.)
4. **Configuration** — a complete `plugins.entries.anansi` YAML block using the exact
   keys/defaults/clamps from config.py listed in this plan's Context section, with the model
   override under the `llm:` sub-key. Note the kill switch (`enabled: false`) and that absent or
   malformed config degrades to defaults without error.
5. **Observability** — telemetry table location (plugin's own SQLite under
   `$HERMES_HOME/anansi/`), full outcome vocabulary including `reflect_*`,
   `telemetry_summary` field semantics (post-04-01-01 vocabulary). Include the **WAL sidecar
   inspection idiom**: the state DB runs WAL mode; to inspect a live DB, copy `state.db` AND
   `state.db-wal` AND `state.db-shm` together — a read-only URI open on a bare `.db` copy fails
   with SQLITE_CANTOPEN (03-VERIFICATION observation 4).
6. **Sub-session behavior (honest)** — the host runs the full hook set per sub-session; parallel
   sub-session appraisals can contend into the fail-open timeout (observed live: 8005-8006ms
   timeout rows in parallel sub-sessions; turns proceeded intact every time).
7. **Limitations (honest)** — quote DECISIONS.md R1/R2/R3 substance:
   - one-turn lag is deliberate: appraisal sees message + history + state, not current-turn memory
     injection; the reflection pass is the carrier of that context across the lag (R2);
   - deadline/latency trade-off: default 8.0s deadline, live p50 ≈5.5s with a cheap-tier model —
     headroom is thin under load and the live timeout fraction is nontrivial (3 timeouts vs 6 ok
     among real attempts at validation time); every timeout fails open (R1);
   - trust-gate fallback degrades to the fail-open timeout on hosts whose active model is slower
     than the deadline clamp (R3) — correct on faster hosts.
8. **State + privacy** — local SQLite only, capped + decayed tables, hot path reads via read-only
   snapshot, writes confined to the reflection pass.

**Repo-root `README.md` — thin.** What this repo is (development home of the plugin + its
learnship planning artifacts), the dev test command (`./scripts/test.sh`), the live-deploy symlink
idiom, and a pointer to `anansi/README.md` for install/config/limitations.

Both files: observational tone, no claims beyond what 02/03-VALIDATION evidence supports. Address
the maintainer audience, not Dr. Mani.

Run the full suite (docs must not break collection). Commit:
`git -C /Volumes/Asylum/repos/hermes-anansi-plugin add -A && git -C /Volumes/Asylum/repos/hermes-anansi-plugin commit -m "docs(04-01): README pair — dual-layout install, config reference, WAL idiom, honest limitations"`
</action>
<verify>
Both files exist. `grep -n "reflect_deadline_seconds\|reflect_every_n_turns\|history_chars" anansi/README.md`
hits (config block is complete, not a sample). `grep -in "state.db-wal" anansi/README.md` hits
(WAL idiom present). `grep -in "sub-session" anansi/README.md` hits. No key in the README
config block is absent from config.py (`grep` each documented key against config.py). Suite green.
Commit exists.
</verify>
<done>[ ]</done>
</task>

## Must-Haves

After all tasks complete, the following must be true:

- [ ] `telemetry_summary` failure vocabulary: `reflect_ok`/`reflect_skipped:*` non-failures, `reflect_timeout`/`reflect_llm_error`/`reflect_parse_fail` failures; `last_error` consistent; new test green
- [ ] PKG-01 observable by one test run: manifest test (pip_dependencies `[]`, accurate `provides_hooks`) + the referenced SAFE-04 import-allowlist test, both green
- [ ] `anansi/README.md` complete per the 8-section checklist; repo-root `README.md` exists
- [ ] `./scripts/test.sh` green, ≥106 tests, zero network needed by any test
- [ ] Live symlink `~/.hermes/plugins/anansi` untouched and still resolving; `~/.hermes/plugins/anansi` never entered
- [ ] Three commits, one per task

## Success Criteria Mapping (ROADMAP Phase 4)

| Criterion | Coverage by this plan |
|-----------|----------------------|
| 1 — installs cleanly in both layouts | Standalone half: install docs + live symlink still working (in-tree half: 04-02) |
| 2 — `pip_dependencies: []` verified; suite passes to host standards on upstream main | PKG-01 verification half: manifest test + allowlist cross-reference (upstream-main suite run: 04-02) |
| 3 — PR branch + PR_BODY.md ready, submission blocked | Not this plan (04-02) — but the README this plan writes ships in the PR branch |
