---
wave: 2
depends_on: ["02-01"]
files_modified:
  - anansi/tests/fixtures/contradictions.json
  - scripts/live_smoke.py
  - scripts/live_contradiction_fixtures.py
  - .planning/phases/02-appraisal-path/02-VALIDATION.md
autonomous: true
single_layer_justified: true  # empirical-validation slice: proves the 02-01 vertical slice against the live install; adds no product layers
objective: "The 02-01 appraisal path is proven against the live install with a real cheap-model call: contradiction fixtures scored (Phase-0 item 6), trust-gate fallback and kill switch verified live, a real hermes turn shows a [anansi appraisal] block, and telemetry p50 is read out — with every network-blocked item recorded as pending-network instead of failing."
must_haves:
  - ".planning/phases/02-appraisal-path/02-VALIDATION.md exists with sections: Network Status, Live Config, Smoke Call, Trust-Gate Fallback, Kill Switch, Throttles, Contradiction Fixtures (Phase-0 item 6), Real-Turn Demo, Telemetry Readout (Phase-0 item 7), Model Quirks (Phase-0 item 5)"
  - "anansi/tests/fixtures/contradictions.json exists with >= 8 cases including >= 2 no-contradiction controls"
  - "scripts/live_smoke.py and scripts/live_contradiction_fixtures.py exist and run under the hermes venv python (network-dependent steps may report pending-network)"
  - "~/.hermes/config.yaml gains a plugins.entries.anansi block; the plugins.entries.anansi (guardrails) block is byte-identical to the pre-edit backup"
  - "Every ROADMAP Phase-2 success criterion is either evidenced in 02-VALIDATION.md or explicitly marked pending-network — the plan is NOT failed for network outage"
---

# Plan 02-02: Live Empirical Validation (Real Cheap-Model Call)

## Objective

Validate the Plan 02-01 appraisal path empirically against the live install: configure the trust gate
for a real cheap model, run a smoke call and the contradiction fixture set (Phase-0 item 6) through
`run_appraisal`, demonstrate a `[anansi appraisal]` block on a real `hermes` turn, verify kill switch /
trust-gate fallback / throttles live, and read out telemetry (outcome distribution + p50 wall time vs
the <= 1.0s target). Evidence lands in `02-VALIDATION.md` (ROADMAP Phase-2 success criteria 1-5).

## Context

**MANDATORY NETWORK-OUTAGE CONTINGENCY (applies to every task):** at Phase-1 close this machine had a
total outbound-HTTPS outage (anthropic + openai + github all unreachable — see 01-VALIDATION.md). At
the START of this plan, probe connectivity:
`curl -s -o /dev/null -w "%{http_code}" --max-time 5 https://api.anthropic.com/` and the same for
`https://api.openai.com/`. If BOTH lanes are still down: execute ALL non-network tasks (fixture file,
scripts, config block, VALIDATION skeleton), record each blocked item in `02-VALIDATION.md` under a
`pending-network` marker with the exact probe output, and finish the plan as COMPLETE. Do NOT fail the
plan, do NOT retry-loop, do NOT claim any network step succeeded without captured output. Lane choice
(anthropic vs openai) is agent's discretion per 02-CONTEXT — use whichever works and record which.

**Prerequisites from Plan 02-01 (verify before starting):** `appraisal.run_appraisal`,
`render.render_block`, `store.record_telemetry`/`telemetry_summary`, schema v2, kill switch + throttles
wired in `pre_llm_call`, `ANANSI_DEBUG_DUMP` env-gated block dump, full suite green.

**Live install facts:**
- `$HERMES_HOME` = `/Users/manisaintvictor/.hermes`; plugin deployed by symlink
  `$HERMES_HOME/plugins/anansi` -> `/Volumes/Asylum/repos/hermes-anansi-plugin/anansi`
  (created in Phase 1 — verify with `ls -l`, recreate if missing).
- **NEVER touch `/Users/manisaintvictor/.hermes/plugins/anansi`** — that is the separate, live
  "Anansi Metacognitive Guardrails" plugin. Its config lives at `plugins.entries.anansi` in
  `~/.hermes/config.yaml` (~lines 588-603). Our key is `plugins.entries.anansi` — one
  character of drift into the `anansi` block is a release blocker.
- Trust gate (host-enforced, re-read per call, no restart needed): `agent/plugin_llm.py:202-246`
  reads `plugins.entries.anansi.llm.{allow_model_override, allowed_models, ...}`; denial
  raises `PluginLlmTrustError` (`:249-329`).
- Schema v2 consequence: the live v1 `state.db` at `$HERMES_HOME/anansi/` will
  quarantine-and-recreate on first `ensure_db()` — expected (disposable-state doctrine), note it in
  VALIDATION when observed.
- `hermes -z` exits 0 even when the API call fails (01-02-SUMMARY) — never use exit code as evidence;
  capture actual output to `/tmp/` files.
- Known quirk to watch for (Phase-0 item 5, PITFALLS G4): gpt-5-family param rejection,
  DeepSeek `content: null` — record anything observed under Model Quirks.

**Cheap-model defaults to offer the trust gate (02-CONTEXT, never auto-applied by plugin code):**
anthropic `claude-haiku-4-5`; openai `gpt-4o-mini`. Pick per working lane.

**Commits** in `/Volumes/Asylum/repos/hermes-anansi-plugin` (use `git -C`). `~/.hermes/config.yaml` is
NOT in this repo — back it up to a timestamped sibling file before editing instead.

## Tasks

<task id="02-02-01">
<name>Live trust-gate config + real smoke call</name>
<files>
- scripts/live_smoke.py
- .planning/phases/02-appraisal-path/02-VALIDATION.md
</files>
<action>
1. Create `02-VALIDATION.md` skeleton with the ten section headers from must_haves, and record the
   network probe results (exact curl outputs, timestamps) under **Network Status**.
2. Back up the live config: `cp ~/.hermes/config.yaml ~/.hermes/config.yaml.bak-$(date +%Y%m%dT%H%M%S)`.
   Record the backup filename in VALIDATION.
3. Edit `~/.hermes/config.yaml`: under `plugins.entries`, add (or extend) the `anansi` entry —
   plugin-own keys + trust gate, e.g. (anthropic lane shown; adjust model to the working lane):
   ```yaml
   anansi:
     enabled: true
     confidence_threshold: 0.6
     deadline_seconds: 2.5
     history_chars: 4000
     llm:
       allow_model_override: true
       allowed_models: ["claude-haiku-4-5"]
       model: "claude-haiku-4-5"
   ```
   Then verify the guardrails block is untouched: `diff <(grep -A 20 "  anansi:" ~/.hermes/config.yaml.bak-*) <(grep -A 20 "  anansi:" ~/.hermes/config.yaml)`
   must be empty (adapt the grep window to the actual block size). Record the diff result in VALIDATION.
4. Write `scripts/live_smoke.py` (run with the hermes venv python; insert both
   `$HERMES_HOME/hermes-agent` and the repo root into sys.path from env, no literal paths):
   builds the REAL facade `PluginLlm(plugin_id="anansi")` (from `agent.plugin_llm`), a contrived
   message ("Earlier I said I prefer Postgres for everything; now I think SQLite is always the right
   choice. What should we use?"), a small fake snapshot dict, `cfg = config.get_cfg(force_reload=True)`,
   then calls `appraisal.run_appraisal(...)` and prints: outcome, wall_ms, model actually used,
   tokens_in/out, the parsed signals, and `render.render_block(signals)`. Also prints
   `store.telemetry_summary()` of the live DB (read-only). The script itself records a telemetry row only
   if you point it at the live DB path explicitly — default behavior: do NOT write to the live state.db
   from the script; print only (the real-turn task produces the live rows).
5. **If network is up:** run the smoke once per available lane choice (one is enough); paste the output
   into VALIDATION under **Smoke Call**: model used, outcome ok, wall_ms, and the rendered block. This is
   the first real-model proof of APPR-01/02/04.
   **If network is down:** record the probe evidence and mark Smoke Call `pending-network` with the exact
   command to run later: `/Users/manisaintvictor/.hermes/hermes-agent/venv/bin/python scripts/live_smoke.py`.
6. Commit (repo files only): `docs(02-02): validation skeleton + live smoke script`.
</action>
<verify>
`02-VALIDATION.md` has Network Status + Live Config filled with captured command output (not claims).
`~/.hermes/config.yaml` contains the `anansi` entry; the guardrails `anansi` block diff is empty.
`scripts/live_smoke.py` runs under the hermes venv python (prints results, or a clean pending-network
message when offline). Commit exists.
</verify>
<done>[ ]</done>
</task>

<task id="02-02-02">
<name>Contradiction fixture set + scoring run (Phase-0 item 6)</name>
<files>
- anansi/tests/fixtures/contradictions.json
- scripts/live_contradiction_fixtures.py
- .planning/phases/02-appraisal-path/02-VALIDATION.md
</files>
<action>
1. Write `anansi/tests/fixtures/contradictions.json`: a list of >= 8 contrived cases, each
   `{"id": str, "kind_expected": "semantic|narrative|relational|emotional|none", "user_message": str, "history": [{"role": ..., "content": ...}], "state": {<snapshot-shaped dict with planted concerns/contradictions/trust_scores>}, "notes": str}`.
   Coverage: 2 semantic (user asserts X after history/state says not-X), 1-2 narrative (timeline
   inconsistency), 1 relational (claim conflicting with a stored preference/trust score), 1 emotional
   (stated calm vs distress markers in history), and >= 2 `none` controls (benign exchanges — these
   measure false positives). Keep each case small (<= ~600 chars total context).
2. Write `scripts/live_contradiction_fixtures.py` (same sys.path bootstrap as live_smoke.py): loads the
   fixture file, builds the real `PluginLlm(plugin_id="anansi")` and `cfg = config.get_cfg(force_reload=True)`,
   runs each case through `run_appraisal` sequentially (one call per case — ~8 calls total), and prints a
   results table: case id | expected kind | flagged kinds (from `contradiction_flags`) | top confidence |
   wall_ms | outcome. Footer: detection rate on contradiction cases, false-positive rate on controls,
   p50 wall_ms across the run. No writes to the live state.db.
3. **If network is up:** run it; paste the table + rates into VALIDATION under **Contradiction Fixtures**.
   This is a LOW-confidence area (02-CONTEXT) — record honest results, including misses; if detection is
   poor, note it as evidence for Phase-3 D1 depth decisions, not as a failure of this plan. Record any
   parameter/JSON quirks observed under **Model Quirks** (Phase-0 item 5).
   **If network is down:** fixtures + script are the deliverable; mark the run `pending-network` with the
   exact command.
4. Commit: `feat(02-02): contradiction fixture set + live scoring harness`.
</action>
<verify>
Fixture file parses (`python -m json.tool` on it) and has >= 8 cases with >= 2 controls. The script runs
under the hermes venv python (results table, or pending-network message). VALIDATION's Contradiction
Fixtures section is filled or marked pending-network. Commit exists.
</verify>
<done>[ ]</done>
</task>

<task id="02-02-03">
<name>Real-turn demo, kill-switch/trust/throttle verification, telemetry readout</name>
<files>
- .planning/phases/02-appraisal-path/02-VALIDATION.md
</files>
<action>
All steps capture output to `/tmp/anansi2-*.txt` files and paste evidence into VALIDATION. If network is
down, perform steps a, b-i/b-ii skip-paths only as far as they go without a completed turn, and mark the
rest pending-network with exact replay commands.

a. Pre-flight (no network needed): verify the symlink deploy (`ls -l ~/.hermes/plugins/anansi`),
   plugin enabled (`hermes plugins list | grep anansi`), and that the live v1 state.db quarantine-recreates
   to schema v2 on the next session (after the first turn below, check
   `ls ~/.hermes/anansi/` for a `state.db.quarantined-*` file and confirm
   `sqlite3 "file:$HOME/.hermes/anansi/state.db?mode=ro" "SELECT value FROM meta WHERE key='schema_version'"` -> 2).

b. **Real-turn demo (ROADMAP criterion 1):**
   i. `ANANSI_DEBUG_DUMP=/tmp/anansi2-block.txt HERMES_PLUGINS_DEBUG=1 hermes -z "Earlier I told you I prefer Postgres for everything, but now I am sure SQLite is always the right choice for plugin state. Which is it?" > /tmp/anansi2-turn1.txt 2>&1`
   ii. Evidence: `/tmp/anansi2-block.txt` contains a block starting `[anansi appraisal]` with instinct/
       salience signals grounded in the message; the turn output is normal (no traceback, no raw JSON
       leakage). Paste the block into VALIDATION under **Real-Turn Demo**. If the appraisal suppressed
       (empty dump), retry once with a more contradiction-laden prompt before concluding.
c. **Kill switch (ROADMAP criterion 3a):** set `enabled: false` in the config entry, run
   `hermes -z "Another substantive question about SQLite vs Postgres trade-offs"`, then verify the
   newest telemetry row is `skipped:disabled` and NO new `ok` row appeared:
   `sqlite3 "file:$HOME/.hermes/anansi/state.db?mode=ro" "SELECT ts, outcome FROM telemetry ORDER BY id DESC LIMIT 5"`.
   Restore `enabled: true`. (Config is re-read per session — no restart needed.)
d. **Trust-gate fallback (ROADMAP criterion 3b):** set `llm.allow_model_override: false` (leave `model`
   set), run one substantive turn, verify the newest non-skip telemetry row has outcome
   `trust_fallback` and `model` = the host's active model (not the cheap model). Restore
   `allow_model_override: true`. Record both states in VALIDATION under **Trust-Gate Fallback**.
e. **Throttles (ROADMAP criterion 4):** `hermes -z "ok"` -> newest row `skipped:social_close` and
   `/tmp/anansi2-block.txt` unchanged. For the near-duplicate gate, run two identical substantive
   messages within ONE session (interactive `hermes` with two sends, or two turns via the gateway lane
   if easier); if a one-session two-turn run is impractical, record that the duplicate gate is covered
   by `test_pre_llm_call.py::duplicate` unit evidence and mark the live check optional. Record under
   **Throttles**.
f. **Telemetry readout (ROADMAP criterion 2, Phase-0 item 7):** after the above turns + the fixture run,
   query the live DB read-only:
   `sqlite3 "file:$HOME/.hermes/anansi/state.db?mode=ro" "SELECT outcome, COUNT(*) FROM telemetry GROUP BY outcome"`
   plus `telemetry_summary()` via a one-liner under the hermes venv python for `p50_wall_ms`,
   `failure_count`, `last_error`. Evidence target: p50 <= 1.0s for ok rows, exactly one LLM call per
   eligible turn (one non-skip row per substantive turn — cross-check row count vs turns run). If p50
   misses the target, record it honestly with the model used — that is telemetry doing its job, not a
   plan failure. Paste under **Telemetry Readout**.
g. Fill **Model Quirks** (Phase-0 item 5) with anything observed across smoke/fixtures/turns (param
   rejections, content:null, truncation, JSON oddities) or "none observed".
h. Finish VALIDATION: a summary table mapping ROADMAP Phase-2 success criteria 1-5 -> evidence line or
   `pending-network` marker. Add the standing note for Dr. Mani listing every pending-network item with
   its exact replay command.
i. Commit: `docs(02-02): record live validation evidence` (and any /tmp evidence paths referenced).
</action>
<verify>
`02-VALIDATION.md` has every section filled with captured command output or an explicit pending-network
marker + replay command. The guardrails plugin is still enabled and untouched
(`hermes plugins list | grep "anansi "` shows it, config block byte-identical to backup). Config entry
restored to `enabled: true`, `allow_model_override: true`. Commit exists.
</verify>
<done>[ ]</done>
</task>

## Must-Haves

After all tasks complete, the following must be true:

- [ ] `02-VALIDATION.md` exists with all ten sections, each containing captured evidence or an explicit `pending-network` marker with a replay command — never an unevidenced success claim
- [ ] `anansi/tests/fixtures/contradictions.json` has >= 8 cases (>= 2 controls) and parses as JSON
- [ ] `scripts/live_smoke.py` and `scripts/live_contradiction_fixtures.py` run under `/Users/manisaintvictor/.hermes/hermes-agent/venv/bin/python` with no literal paths (sys.path from `$HERMES_HOME`)
- [ ] `~/.hermes/config.yaml` has the `plugins.entries.anansi` block (enabled true, trust gate restored); a timestamped backup exists; the `plugins.entries.anansi` guardrails block is unmodified
- [ ] If network was available: real-turn `[anansi appraisal]` block, kill-switch zero-call proof, trust_fallback telemetry row, `skipped:social_close` row, and outcome-distribution + p50 readout are all in VALIDATION; if not: each is marked pending-network and the plan is complete anyway
