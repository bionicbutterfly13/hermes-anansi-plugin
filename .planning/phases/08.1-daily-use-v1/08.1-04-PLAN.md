---
phase: 08.1-daily-use-v1
plan: "04"
type: execute
wave: 3
depends_on:
  - "08.1-03"
files_modified:
  - anansi/goals_cli.py
  - anansi/__init__.py
  - anansi/tests/test_goals_cli.py
  - anansi/tests/test_anticreep.py
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - REQ-08.1-04
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-04 (revised): Dr. Mani can add, flag, re-status and list goals with `/anansi goals add|flag|status|list` inside a Hermes session and with `hermes anansi goals add|flag|status|list` in a terminal, and both surfaces call the same `goals_cli.dispatch`."
    - "D-04: `add <text> [--domain X]` stores the goal with status `active` (never the inert `candidate` default) and replies with the new goal id; `flag <id>` sets `flagged_priority` to 1; `status <id> active|queued|backburner` changes status; `list` shows every goal ordered by id."
    - "D-11: `--files` is rejected with a usage message naming it as unsupported; no files column or schema change is added."
    - "D-04, Constitution II: goal writes go only through `store.ensure_db` and `store.apply_deltas`; the plugin registers no tool, and no hook parses chat text into goal writes."
    - "The slash command name `anansi` does not resolve to a host built-in (`hermes_cli.commands.resolve_command('anansi') is None`), so the host does not skip it the way it skips `/goal`."
    - "Every command error (unknown subcommand, missing or non-integer id, unknown id, bad status, unbalanced quotes, unavailable store) returns a usage or error message and never raises into the host; the CLI returns 0 on success, 1 on a store or unknown-id failure, 2 on a usage error."
    - "Any goal the 50-goal cap evicts during `add` is named in the reply (anti-erasure)."
    - "A ctx without `register_command` or `register_cli_command`, or whose registration raises, still ends with the four lifecycle hooks registered and `register()` raising nothing."
    - "D-15: the plan branch is merged to main with `--no-ff`, main is pushed to origin after the green gate, and the plan worktree and branch are removed."
    - "[EDGE REQ-08.1-04 idempotency] `flag` on an already-flagged goal and `status` to the goal's current status perform no write (updated_at unchanged) and say so; `add` of the same text twice creates two goals with distinct ids, both shown by `list`."
    - "[EDGE REQ-08.1-04 concurrency] When the state store is locked past its busy timeout, a goal command replies that the store is unavailable, returns exit code 1 on the CLI, leaves no partial write, and raises nothing."
  artifacts:
    - path: anansi/goals_cli.py
      provides: "USAGE, ALLOWED_STATUSES, dispatch, handle_slash, setup_cli, handle_cli"
      contains: "def dispatch"
    - path: anansi/__init__.py
      provides: "guarded register_command / register_cli_command for 'anansi' with lazy wrappers"
      contains: "register_command"
    - path: anansi/tests/test_goals_cli.py
      provides: "grammar, write-path, idempotency, lock, eviction, built-in-name, registration-resilience and argparse CLI tests"
  key_links:
    - from: anansi/__init__.py
      to: anansi/goals_cli.py
      via: "register() registers slash 'anansi' and CLI 'anansi' whose wrappers call goals_cli.handle_slash / setup_cli / handle_cli"
      pattern: "goals_cli"
    - from: anansi/goals_cli.py
      to: anansi/store.py
      via: "dispatch writes via store.apply_deltas goals_add/goals_update/goals_status and reads via store.read_snapshot"
      pattern: "apply_deltas"
  assumptions:
    - id: A6-tui-path
      status: flagged-unresolved
      statement: "Research A6: the TUI `tui_gateway/methods_tools.py` command path is user-initiated, not agent-invokable. If the host ever lets the model run plugin slash commands, Constitution II needs review for `/anansi goals`."
  prohibitions:
    - requirement_id: REQ-08.1-04
      category: autonomy
      statement: "MUST NOT expose any goal write path to the model: no registered tool, no hook-side parsing of chat text into goal writes; only Dr. Mani's own slash or CLI invocation writes goals."
      status: unresolved
      verification: null
      flagged: true
    - requirement_id: REQ-08.1-04
      category: values
      statement: "MUST NOT silently evict, overwrite or re-date an existing goal: cap evictions caused by `add` are named in the reply, and no-op flag/status commands do not touch updated_at."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Give Dr. Mani a user-invoked way to enter and manage goals (D-04 as revised on 2026-09-24, D-11).

Purpose: the drive layer has no real input today; `store.apply_deltas` goal keys have no caller, so flagged priorities never exist in practice. `/goal` is a Hermes built-in the host silently skips, so the surface is `/anansi goals ...` mirroring `hermes anansi goals ...`, both sharing one handler.
Output: new `anansi/goals_cli.py`, guarded command registration in `register()`, tests, README and CHANGELOG updates.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/reference/CONSTITUTION.md
@.planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
@.planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
@.planning/phases/08.1-daily-use-v1/08.1-PATTERNS.md
@anansi/__init__.py
@anansi/store.py
@anansi/reflection.py
@anansi/tests/test_anticreep.py
</context>

<interfaces>
Host contracts (hermes-agent 0.21.4, `hermes_cli/plugins.py`, verified at planning time; re-read the signatures before coding because 08.1-02 may re-sync the host):
- `ctx.register_command(name, handler, description="", args_hint="", argument_mode=None)`; handler `fn(raw_args: str) -> str | None`, sync or async; the host skips any name for which `hermes_cli.commands.resolve_command(name)` is not None (`goal` is a built-in; `anansi` is not).
- `ctx.register_cli_command(name, help, setup_fn, handler_fn=None, description="")`; `setup_fn(subparser)` receives the argparse subparser for `hermes <name>`; `handler_fn(args)` becomes `set_defaults(func=...)` and a non-zero int return becomes the process exit code.
- An exception escaping `register()` makes the host dispose every registration of the plugin, hooks included (`hermes_cli/plugins_loader.py`), so every new registration is probed with `getattr` and guarded individually.
</interfaces>

## Artifacts this phase produces

- New module `anansi/goals_cli.py` with: `USAGE` (str), `ALLOWED_STATUSES = ("active", "queued", "backburner")`, `dispatch(tokens, db_path=None) -> (int, str)`, `handle_slash(raw_args, db_path=None) -> str`, `setup_cli(subparser) -> None`, `handle_cli(args, db_path=None) -> int`; logger `hermes.plugins.anansi.goals_cli`.
- Slash command `/anansi` (grammar `goals add <text> [--domain X]`, `goals flag <id>`, `goals status <id> active|queued|backburner`, `goals list`, `help`).
- CLI command `hermes anansi goals ...` (argparse sub-subparser `goals` with a `goal_args` REMAINDER positional; parser dest `anansi_command`).
- In `anansi/__init__.py`: wrappers `_anansi_slash(raw_args)`, `_anansi_cli_setup(subparser)`, `_anansi_cli_handler(args)`, and guarded registration blocks in `register()`.
- New test module `anansi/tests/test_goals_cli.py`; `goals_cli.py` added to the anti-creep module inventory; a new AST scan test forbidding any `register_tool` call in plugin modules.
- Exit codes for `hermes anansi goals`: 0 success, 1 store failure or unknown id, 2 usage error.

<integration_protocol>
- Branch `feat/08.1-04-anansi-goals`, sibling worktree `/Volumes/Asylum/repos/hermes-anansi-plugin-08.1-04-anansi-goals`, created from the main checkout with `git worktree add -b feat/08.1-04-anansi-goals ../hermes-anansi-plugin-08.1-04-anansi-goals main` after `git fetch origin` shows `main` is not behind `origin/main`. The main checkout stays on `main`.
- If `.devtools/pytest` is missing in the worktree, copy the main checkout's gitignored `.devtools/` into it, or let `./scripts/test.sh` stage it.
- Before every full-suite run, confirm `$HOME/.hermes/hermes-agent/.git/MERGE_HEAD` does not exist (a host re-sync by 08.1-02 would swap the test venv mid-run); wait if it does.
- Commit per task with conventional messages. Never rebase, squash, force-push or pass `--no-verify`.
- Integration: full suite green in the worktree; from the main checkout `git merge --no-ff feat/08.1-04-anansi-goals -m "Merge 08.1-04 /anansi goals and hermes anansi goals"`; full suite green on main; `git push origin main` (D-15); `graphify update .` in the main checkout; re-audit `git worktree list`, worktree status and ancestry; `git worktree remove ../hermes-anansi-plugin-08.1-04-anansi-goals`; `git branch -d feat/08.1-04-anansi-goals`. On a merge conflict: abort and report.
</integration_protocol>

<tasks>

<task id="08.1-04-T1" type="tracer" tdd="true">
  <name>Task 1: `/anansi goals add` from a registered slash handler to an active stored goal and back through `list`</name>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
    .planning/phases/08.1-daily-use-v1/08.1-PATTERNS.md
    anansi/__init__.py
    anansi/store.py
    anansi/reflection.py
    anansi/tests/test_drive_store.py
    anansi/tests/test_anticreep.py
    $HOME/.hermes/hermes-agent/hermes_cli/plugins.py
  </read_first>
  <files>anansi/goals_cli.py, anansi/__init__.py, anansi/tests/test_goals_cli.py, anansi/tests/test_anticreep.py</files>
  <behavior>
    - `anansi.register(ctx)` with a SimpleNamespace ctx that records `register_hook` and `register_command` calls registers the four hooks and one command named `anansi`.
    - Calling the captured handler with `goals add ship the drive layer --domain anansi` against a tmp DB returns text containing `added goal #1`, and `store.read_snapshot(db)` then holds one goal with text `ship the drive layer`, status `active`, domain `anansi`, flagged_priority 0.
    - Calling the handler with `goals list` returns text containing `#1 [active]` and `ship the drive layer`.
    - A quoted goal text (`goals add "fix the gateway: restart loop"`) is stored verbatim.
  </behavior>
  <action>
Set up the branch and worktree per the integration protocol. Re-read the three host signatures in `hermes_cli/plugins.py` against the Interfaces block; if they changed, stop and report.

Tests first in the new `anansi/tests/test_goals_cli.py`: tmp_path databases only (point `store.get_db_path` at the tmp path with monkeypatch, as `test_pre_llm_call.py`'s `hook_env` does), reset `anansi._ctx` in teardown, and cover the behavior list.

Create `anansi/goals_cli.py` (D-04). Module docstring: user-invoked goal entry for `/anansi goals` and `hermes anansi goals`, one shared `dispatch`, writes only through `store.apply_deltas`, no model-facing surface. Imports: stdlib `argparse`, `logging`, `shlex` and `from . import store` only (anti-creep import allowlist). Define `USAGE` listing the four subcommands for both surfaces, `ALLOWED_STATUSES`, and `dispatch(tokens, db_path=None)` returning `(exit_code, text)` with a subcommand table in the shape of the host `disk-cleanup` plugin's `_SUBCOMMANDS` (PATTERNS Plan 04). In this task implement `add` and `list`; `add` collects text tokens up to an optional `--domain X`, rejects empty text with exit 2 and `USAGE`, calls `store.ensure_db(db_path)`, reads the goal ids with `store.read_snapshot(db_path)`, writes `{"goals_add": [{"text": text, "status": "active", "domain": domain}]}` through `store.apply_deltas`, re-reads, and reports the new id (the id present after but not before whose text matches). `status` is passed as `active` explicitly because the store's default is the inert `candidate` (store.py goals_add comment). `list` prints one line per goal ordered by id: `#<id> [<status>]`, ` [flagged]` when `flagged_priority > 0`, the text, and `(domain: X)` when set; an empty store prints `no goals yet`. Define `handle_slash(raw_args, db_path=None)`: `shlex.split`; empty input or `help` returns `USAGE`; the first token must be `goals`; return the text from `dispatch(tokens[1:], db_path)`. Wrap `dispatch` and `handle_slash` bodies each in one `try/except Exception` that logs at debug and returns an error message (the host would otherwise print a raw plugin error). Keep the source free of every anti-creep forbidden substring, including the raw substrings `eval(` and `exec(` inside longer identifiers, and use no `open(`.

`anansi/__init__.py` (D-04, Pitfall 2). Add `_anansi_slash(raw_args)` that lazily imports `goals_cli` and returns `goals_cli.handle_slash(raw_args)`, catching every exception into a short error string. In `register()`, keep the four `register_hook` calls first and unguarded, then add a guarded block: `fn = getattr(ctx, "register_command", None)`; when present, call `fn("anansi", _anansi_slash, description=..., args_hint="goals add|flag|status|list")` inside `try/except Exception` with a debug log. Update the `register()` docstring (hooks plus guarded command registration; still no DB, config or I/O).

`anansi/tests/test_anticreep.py`: add `goals_cli.py` to the set in `test_scan_targets_are_the_plugin_modules` (the inventory test fails otherwise). Commit `feat(goals): add /anansi goals add and list through one shared dispatcher`.
  </action>
  <verify>
    <automated>./scripts/test.sh anansi/tests/test_goals_cli.py anansi/tests/test_anticreep.py anansi/tests/test_pre_llm_call.py -x</automated>
    <fails_when>non-zero exit, a final summary line containing "failed" or "error", or "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f anansi/goals_cli.py` succeeds and `grep -n "def dispatch" anansi/goals_cli.py` prints one line.
    - `grep -n '"status": "active"' anansi/goals_cli.py` prints at least one line.
    - `grep -n "goals_cli.py" anansi/tests/test_anticreep.py` prints the updated inventory line.
    - The behavior-list tests pass, including the stored status `active` and the quoted text round-trip.
    - `test_safe04_no_forbidden_api_substrings`, `test_safe04_import_allowlist` and `test_safe04_only_write_open_is_the_debug_dump` pass with the new module scanned.
  </acceptance_criteria>
  <done>A registered `/anansi goals add` writes an active goal that `goals list` shows, through the one shared dispatcher.</done>
</task>

<task id="08.1-04-T2" type="auto" tdd="true">
  <name>Task 2: flag, status, errors, eviction, the terminal CLI, and registration resilience</name>
  <read_first>
    anansi/goals_cli.py
    anansi/__init__.py
    anansi/store.py
    anansi/tests/test_goals_cli.py
    anansi/tests/test_anticreep.py
    anansi/tests/test_drive_store.py
    $HOME/.hermes/hermes-agent/hermes_cli/main.py
  </read_first>
  <files>anansi/goals_cli.py, anansi/__init__.py, anansi/tests/test_goals_cli.py, anansi/tests/test_anticreep.py</files>
  <behavior>
    - `goals flag 1` on an unflagged goal sets flagged_priority 1; a second `goals flag 1` replies `already flagged` and leaves `updated_at` unchanged.
    - `goals status 1 queued` changes status; repeating it replies unchanged and leaves `updated_at` unchanged; `goals status 1 done` returns exit 2 naming the allowed statuses.
    - `goals flag x` returns exit 2; `goals flag 99` on a missing id returns exit 1 with `no goal #99`.
    - `goals add ship --files a,b` returns exit 2 with text naming `--files` as unsupported (D-11), and nothing is written.
    - Unbalanced quotes in the slash text return the usage text instead of raising.
    - Adding goal number 51 while 50 unflagged goals exist reports the evicted goal's id and text in the reply.
    - With the database held under an exclusive lock, `goals add x` replies that the store is unavailable, the CLI path returns 1, and no row is added.
    - With `store.apply_deltas` monkeypatched to raise, `handle_slash` returns an error message and `handle_cli` returns 1.
    - Building a parser the way the host does (`add_subparsers`, `add_parser("anansi")`, `setup_fn(subparser)`, `set_defaults(func=handler_fn)`) and parsing `anansi goals add "ship it" --domain anansi` then calling `args.func(args)` returns 0, prints `added goal #`, and stores the goal.
    - With `hermes_cli.commands` importable (`pytest.importorskip`), `resolve_command("anansi")` is None.
    - A ctx whose `register_command` and `register_cli_command` both raise still records all four `register_hook` calls, and `register()` raises nothing; a ctx with neither method behaves the same.
    - No plugin module contains an AST call whose attribute name is `register_tool`.
  </behavior>
  <action>
Write the failing tests for the behavior list first, then implement.

`goals_cli.py`. Add `flag` and `status`: parse the id as a positive int (else exit 2 with `USAGE`); read the snapshot; a missing id returns exit 1 with `no goal #<id>`; `flag` on a goal whose `flagged_priority > 0` returns exit 0 with an "already flagged; unchanged" message and performs no write; otherwise write `{"goals_update": [{"id": id, "flagged_priority": 1}]}` and mention, when the goal's status is not `active`, that it surfaces once active. `status` validates against `ALLOWED_STATUSES` (exit 2 naming them), treats the current status as a no-op with no write, otherwise writes `{"goals_status": [{"id": id, "status": s}]}`. Skipping no-op writes keeps `updated_at`, which momentum falls back to, from being refreshed by a command that changed nothing (anti-erasure). In `add`, reject `--files` with an unsupported-option message (D-11) and reject any other `--` option with `USAGE`; compare goal ids before and after the write and name every evicted goal (Pitfall 8, `_enforce_goal_cap`). Map `store.ensure_db` returning False, `store.read_snapshot` returning None after a successful ensure, and `store.apply_deltas` returning False to exit 1 with "state store unavailable; nothing was written". `handle_slash` returns `USAGE` plus a one-line reason on the `ValueError` shlex raises for unbalanced quotes. Add `setup_cli(subparser)`: `subparsers = subparser.add_subparsers(dest="anansi_command")`, a `goals` parser with help text, and a positional `goal_args` with `nargs=argparse.REMAINDER`. Add `handle_cli(args, db_path=None)`: when `getattr(args, "anansi_command", None)` is not `goals`, print `USAGE` and return 2; otherwise print the text from `dispatch(list(args.goal_args or []), db_path)` and return its code; any exception prints an error and returns 1.

`__init__.py`. Add `_anansi_cli_setup(subparser)` and `_anansi_cli_handler(args)` that lazily import `goals_cli` and delegate, the handler returning 1 on any exception and the setup logging and swallowing any exception. In `register()` add a second guarded block for `register_cli_command(name="anansi", help=..., setup_fn=_anansi_cli_setup, handler_fn=_anansi_cli_handler, description=...)`, probed with `getattr` and wrapped in `try/except Exception` like the slash block.

`test_anticreep.py`: add a test that walks every module in `PLUGIN_MODULES` with `ast` (modeled on the `register_hook` walk in `test_pkg01_manifest_zero_deps_and_accurate_hooks`) and asserts no `ast.Call` has `func.attr == "register_tool"` (Constitution II, prohibition on model-facing goal writes). Commit `feat(goals): add flag, status, hermes anansi goals CLI and guarded registration`.
  </action>
  <verify>
    <automated>./scripts/test.sh anansi/tests/test_goals_cli.py anansi/tests/test_anticreep.py anansi/tests/test_pre_llm_call.py anansi/tests/test_failopen_matrix.py -x</automated>
    <fails_when>non-zero exit, a final summary line containing "failed" or "error", or "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "def setup_cli" anansi/goals_cli.py` and `grep -n "def handle_cli" anansi/goals_cli.py` each print one line.
    - `grep -n "REMAINDER" anansi/goals_cli.py` prints at least one line.
    - `grep -c "getattr(ctx, \"register_c" anansi/__init__.py` is at least 2 (slash and CLI probes).
    - Every behavior-list test exists in `anansi/tests/test_goals_cli.py` or `anansi/tests/test_anticreep.py` and passes; the built-in-name test is not skipped when run with the hermes venv.
  </acceptance_criteria>
  <done>All four subcommands work through both surfaces with defined exit codes, errors never raise, evictions are reported, and a failing registration cannot unload the hooks.</done>
</task>

<task id="08.1-04-T3" type="auto">
  <name>Task 3: Document goal entry, verify the real host loads the plugin, merge and push</name>
  <read_first>
    README.md
    CHANGELOG.md
    anansi/goals_cli.py
  </read_first>
  <files>README.md, CHANGELOG.md</files>
  <action>
In `README.md`, replace the paragraph stating that the plugin has no goal-entry command and that an external caller must populate goals with a short description of `/anansi goals add|flag|status|list` and `hermes -p <profile> anansi goals ...`: user-invoked only, goals added this way start `active`, `--files` is not supported, and the model has no goal-writing tool. Keep the sentences about the missing completed-goal status and about memory searches unless they became false. Add a dated `CHANGELOG.md` entry: Features (the two surfaces, one dispatcher, eviction reporting), Fixes (the drive layer had no real input path: `apply_deltas` goal keys had no caller), Learnings (`/goal` is a host built-in and the host silently skips plugin commands that collide with built-ins; any exception inside `register()` unloads every registration, so each new registration is guarded). Commit.

Real-host load check without a live profile: from the worktree run `hermes plugins doctor "$PWD/anansi" --ci`, which loads the plugin through the host's real registration path under a temporary HERMES_HOME with network blocked. Then integrate per the integration protocol. The live `hermes -p chief-of-staff anansi goals list` check runs in 08.1-06 Task 2, after install.
  </action>
  <verify>
    <automated>hermes plugins doctor "/Volumes/Asylum/repos/hermes-anansi-plugin/anansi" --ci</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && ./scripts/test.sh</automated>
    <fails_when>non-zero exit, or the final summary line reports any failed or error count</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && git fetch origin && git rev-list --count origin/main..main && git branch --list feat/08.1-04-anansi-goals</automated>
    <fails_when>the count is not 0, or the branch name is printed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "/anansi goals" README.md` prints at least one line, and the sentence claiming an external caller must populate goals is gone.
    - The newest `CHANGELOG.md` entry mentions `/anansi goals` and the `/goal` built-in collision.
    - The doctor command exits 0 in the worktree before the merge and against the main checkout's `anansi/` after it.
    - `git -C /Volumes/Asylum/repos/hermes-anansi-plugin log -1 --merges --format=%s main` contains `08.1-04`; `origin/main` equals `main`; the worktree and branch are gone.
  </acceptance_criteria>
  <done>Goal entry is documented, loads on the real host, and is on main and origin.</done>
</task>

</tasks>

<constitution_check>
- I Fail-open: the handlers are not hooks, so `_fail_open` does not wrap them; each surface catches every exception and returns a message or exit code. Registration is guarded so no command failure can unload the hooks.
- II No autonomy: user-invoked slash and CLI only; no tool registration (AST-scanned); goals are minted by Dr. Mani, never by the model.
- III Never-omit and anti-erasure: cap evictions are reported; no-op commands do not refresh `updated_at`.
- IV Single SQLite surface: all reads and writes go through `store`; `goals_cli.py` imports no sqlite3.
- V Zero deps, paths from config: stdlib only; the DB path comes from `store.get_db_path()` under the process's HERMES_HOME (the profile when invoked with `-p`).
- VI Inspectable drive: `list` shows status and flag state for every goal.
- VII Minimal change: no schema change (D-11), one new module, registration additions only.
</constitution_check>

<source_coverage>
D-04 (revised to `/anansi goals`), D-11, D-15, REQ-08.1-04, RESEARCH Plan 04 and Pitfalls 1, 2, 8, PATTERNS Plan 04. Deferred and excluded: reflection-proposed candidate goals with a confirm command, `register_system_prompt_section`. Canon referral: path traversal through `--domain` values is canon and stays with /gsd-secure-phase (the existing momentum containment guard for `domain` is unchanged); not minted here.
</source_coverage>

<verification>
Full `./scripts/test.sh` green on main after the merge, `hermes plugins doctor /Volumes/Asylum/repos/hermes-anansi-plugin/anansi --ci` exits 0 after the merge, and `git rev-list --count origin/main..main` prints 0.
</verification>

<success_criteria>
Dr. Mani can enter, flag, re-status and list goals from a session or a terminal; goals land active through the single store surface; nothing model-facing can write goals.
</success_criteria>

<output>
Create `.planning/phases/08.1-daily-use-v1/08.1-04-SUMMARY.md` when done.
</output>
