---
phase: 08.1-daily-use-v1
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - anansi/store.py
  - anansi/appraisal.py
  - anansi/config.py
  - anansi/reflection.py
  - anansi/tests/test_worker.py
  - anansi/tests/test_drive_velocity.py
  - anansi/tests/test_drive_neveromit.py
  - README.md
  - CHANGELOG.md
  - .planning/DECISIONS.md
  - .planning/GSD-MIGRATION.md
  - .planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/260912-cqc-PLAN.md
  - .planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/260912-cqc-SUMMARY.md
  - .planning/reference/phase-14-inputs/README.md
  - .planning/reference/phase-14-inputs/DRAFT-07-SECURITY.md
  - .planning/reference/phase-14-inputs/DRAFT-COMPLETION-PLAN.md
  - .planning/reference/phase-14-inputs/DRAFT-LIVE-VERIFICATION.md
  - .planning/reference/phase-14-inputs/DRAFT-live_trust_smoke.py
autonomous: true
requirements:
  - REQ-08.1-02
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-02: quick task 260912-cqc lands on `codex/clarify-appraisal-timeout` as its own commit (shared worker admission, `skipped:worker_busy` and `reflect_skipped:worker_busy` outcomes, `anansi/tests/test_worker.py`) without the worktree's stale `.planning/STATE.md` hunk."
    - "D-02: `store._repo_root()` and `store._last_commit_epoch()` follow a linked worktree's `.git` FILE through one shared `store._git_dir()` helper, landed as a separate commit with linked-worktree fixture tests."
    - "D-02: the full `./scripts/test.sh` suite passes in the worktree and again on `main` after the `--no-ff` merge, with no weakened assertion; the 260912-cqc failure is fixed by the `_repo_root()` repair, not waived."
    - "The never-omit full-hook test `test_persisted_flagged_priorities_survive_empty_signal_full_hook` pins `store._repo_root` and keeps every assertion byte-identical, so its result no longer depends on the age of the ambient checkout's reflog."
    - "D-02: the four deferred drafts live under `.planning/reference/phase-14-inputs/` marked DRAFT; three are byte-identical to their sources and `DRAFT-LIVE-VERIFICATION.md` differs from its source only by the removed fabricated ledger row; `.planning/GSD-MIGRATION.md` points at the new location and keeps the original hashes as provenance."
    - "D-02: PR #2 is closed unmerged with a comment pointing at the Phase 8 merge `d46e489`; `531f390` is not ported."
    - "D-02, D-15: local `001-close-known-gaps` is deleted non-force and `origin/001-close-known-gaps` is deleted only after PR #2 is closed and every commit unique to the branch is mapped to a main counterpart or to the do-not-port record for `531f390`."
    - "D-15: `main` is pushed to `origin` after the merge's green gate; the `codex/clarify-appraisal-timeout` worktree and branch are removed after the merge is verified."
    - "[EDGE REQ-08.1-02 adjacency] A linked worktree nested inside another checkout (the `.claude/worktrees/clarify-appraisal-timeout` shape) resolves to the nearest candidate: `_repo_root` returns the worktree root, not the enclosing checkout, and momentum reads the worktree's own reflog."
    - "[EDGE REQ-08.1-02 empty] An empty `.git` file, a `.git` file with no `gitdir:` line, or a `gitdir:` target that does not exist yields no git dir for that candidate; with no valid ancestor `goal_momentum` returns the benign unknown default and nothing raises."
    - "[EDGE REQ-08.1-02 ordering] For a linked worktree `_last_commit_epoch` reads the last line of `<gitdir>/logs/HEAD` (the per-worktree reflog), and a relative `gitdir:` path resolves against the directory that holds the `.git` file."
    - "[EDGE REQ-08.1-02 idempotency] `_git_dir`, `_repo_root` and `_last_commit_epoch` are read-only: repeated calls on the same tree return identical results and the plugin still has exactly one write-mode `open` (the debug dump); lineage steps check state first, so a re-run skips an already-closed PR, an already-deleted branch and already-moved drafts."
    - "[EDGE REQ-08.1-02 concurrency] A reflog whose last line is partially written or unparseable yields `None` from `_last_commit_epoch` (momentum unknown) without raising, so a git write racing an appraisal read degrades to unknown, never to an exception."
  artifacts:
    - path: anansi/store.py
      provides: "_git_dir helper; linked-worktree-aware _repo_root and _last_commit_epoch"
      contains: "def _git_dir"
    - path: anansi/tests/test_drive_velocity.py
      provides: "linked-worktree fixture tests (absolute, relative, nested, malformed, directory control, truncated reflog)"
    - path: anansi/tests/test_drive_neveromit.py
      provides: "hermetic never-omit full-hook test (pinned _repo_root)"
    - path: anansi/tests/test_worker.py
      provides: "quick 260912-cqc worker admission regressions (committed from the worktree)"
    - path: .planning/reference/phase-14-inputs/README.md
      provides: "DRAFT status, provenance hashes, and the removed-row note for the Phase 14 inputs"
  key_links:
    - from: anansi/store.py
      to: anansi/store.py
      via: "_repo_root walk-up predicate calls _git_dir(candidate); _last_commit_epoch reads <_git_dir(repo_root)>/logs/HEAD"
      pattern: "_git_dir\\("
    - from: anansi/tests/test_drive_neveromit.py
      to: anansi/store.py
      via: "monkeypatch.setattr(store, '_repo_root', ...) inside the persisted-flagged full-hook test"
      pattern: "_repo_root"
    - from: .planning/GSD-MIGRATION.md
      to: .planning/reference/phase-14-inputs/README.md
      via: "Deferred inputs section points at the new DRAFT location"
      pattern: "phase-14-inputs"
  assumptions:
    - id: A3-gitdir-format
      status: flagged-unresolved
      statement: "Research A3: a linked worktree's `.git` file is a single `gitdir: <path>` line, absolute or relative; confirmed only by the one real file read at planning time. Any other variant returns None (momentum unknown), which is fail-safe."
  prohibitions:
    - requirement_id: REQ-08.1-02
      category: values
      statement: "MUST NOT delete `001-close-known-gaps` (local or remote) or close PR #2 before every commit unique to that branch is mapped to a main counterpart or the do-not-port record for `531f390`; erasing unrecorded work is the anti-erasure failure this project builds against."
      status: unresolved
      verification: null
      flagged: true
    - requirement_id: REQ-08.1-02
      category: transparency
      statement: "MUST NOT carry the fabricated live-trust ledger row, or any other live result that no actual run produced, into a tracked file."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Close the pre-08.1 lineage so every later plan starts from one clean, honest `main` (D-02, D-15).

Purpose: the shared-worker repair (quick 260912-cqc) is uncommitted, its release gate is time-dependent because `_repo_root()` skips a linked worktree's `.git` file, four draft documents sit untracked in the main checkout (one carrying a fabricated live result), and the superseded PR #2 lineage is still open.
Output: two commits on `codex/clarify-appraisal-timeout` (quick task, then the `_git_dir` fix), relocated DRAFT inputs, a `--no-ff` merge pushed to `origin/main`, PR #2 closed, and both obsolete branches plus the quick-task worktree removed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/reference/CONSTITUTION.md
@.planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
@.planning/phases/08.1-daily-use-v1/08.1-ADR.md
@.planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
@.planning/phases/08.1-daily-use-v1/08.1-PATTERNS.md
@.planning/GSD-MIGRATION.md
</context>

## Artifacts this phase produces

- `anansi.store._git_dir(repo_root)`: returns the `.git` directory `Path`, or the resolved `gitdir:` target of a linked-worktree `.git` file, else `None`; never raises; read-mode `open` only.
- `anansi.store._repo_root(start=None)`: walk-up predicate changed to `_git_dir(candidate) is not None` (signature unchanged).
- `anansi.store._last_commit_epoch(repo_root)`: reads `<_git_dir(repo_root)>/HEAD` and `<_git_dir(repo_root)>/logs/HEAD` (signature unchanged).
- Test helper `_write_linked_worktree(...)` and linked-worktree tests in `anansi/tests/test_drive_velocity.py`.
- Quick-task symbols landed from the worktree as-is: `anansi.appraisal._submit_if_idle`, `_submission_lock`, `_active_future`, telemetry outcomes `skipped:worker_busy` and `reflect_skipped:worker_busy`, `anansi/tests/test_worker.py`.
- New directory `.planning/reference/phase-14-inputs/` with `README.md`, `DRAFT-07-SECURITY.md`, `DRAFT-COMPLETION-PLAN.md`, `DRAFT-LIVE-VERIFICATION.md`, `DRAFT-live_trust_smoke.py`.
- Git state: merge commit on `main`; PR #2 closed; branches `001-close-known-gaps` (local and remote) and `codex/clarify-appraisal-timeout` deleted; worktree `.claude/worktrees/clarify-appraisal-timeout` removed.

<integration_protocol>
This plan works in the EXISTING worktree `/Volumes/Asylum/repos/hermes-anansi-plugin/.claude/worktrees/clarify-appraisal-timeout` on branch `codex/clarify-appraisal-timeout` (D-02). Do not create another worktree. The main checkout `/Volumes/Asylum/repos/hermes-anansi-plugin` stays on `main` at all times (RESEARCH Pitfall 10: a later symlink install makes the main checkout's working tree live code).

- Run every `./scripts/test.sh` from the root of the tree under test. If `.devtools/pytest` is missing in that tree, copy the main checkout's gitignored `.devtools/` into it, or let `test.sh` stage it.
- Merges run from the main checkout with `git merge --no-ff`. Never rebase, squash, force-push, or pass `--no-verify`.
- The main checkout carries uncommitted planning documents (for example `.planning/ROADMAP.md` and the Phase 08.1 docs). Leave them untouched; they are not part of this plan's commits. `.planning/STATE.md` and `.planning/ROADMAP.md` updates belong to the execute-phase orchestrator.
- On any merge conflict: `git merge --abort`, stop, and report the conflicting paths.
- If a check fails after a change and the cause is clear, make one targeted fix; otherwise stop and report (global rule).
</integration_protocol>

<tasks>

<task id="08.1-01-T1" type="tracer" tdd="true">
  <name>Task 1: Commit quick 260912-cqc, then make momentum follow a linked worktree end to end</name>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
    .planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
    .planning/phases/08.1-daily-use-v1/08.1-PATTERNS.md
    .claude/worktrees/clarify-appraisal-timeout/.planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/260912-cqc-SUMMARY.md
    anansi/store.py
    anansi/tests/test_drive_velocity.py
    anansi/tests/test_drive_neveromit.py
    anansi/tests/test_failopen_matrix.py
    anansi/tests/test_anticreep.py
    README.md
    CHANGELOG.md
  </read_first>
  <files>anansi/appraisal.py, anansi/config.py, anansi/reflection.py, anansi/tests/test_worker.py, .planning/DECISIONS.md, .planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/260912-cqc-PLAN.md, .planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/260912-cqc-SUMMARY.md, anansi/store.py, anansi/tests/test_drive_velocity.py, anansi/tests/test_drive_neveromit.py, README.md, CHANGELOG.md</files>
  <behavior>
    - A linked-worktree fixture whose `.git` file holds an absolute `gitdir:` path: `_repo_root(start=<worktree>/sub)` returns the worktree root and `_last_commit_epoch(<worktree>)` returns the epoch written to `<gitdir>/logs/HEAD`.
    - The same fixture with a relative `gitdir:` path gives the same results.
    - A linked worktree nested inside a parent checkout whose own `.git` directory carries a different epoch resolves to the worktree and returns the worktree's epoch.
    - An empty `.git` file, a `.git` file without a `gitdir:` line, and a `gitdir:` target that does not exist each yield `goal_momentum(...) == {"momentum": "unknown", "stalled_days": None, "salience": 0.0}` when no valid ancestor exists, and nothing raises.
    - A `.git` directory control behaves exactly as before.
    - A truncated final reflog line makes `_last_commit_epoch` return `None`.
    - `test_persisted_flagged_priorities_survive_empty_signal_full_hook` passes regardless of how old the ambient checkout's reflog is.
  </behavior>
  <action>
Step A, preflight (D-02). In the worktree, run `git status --porcelain` and confirm it shows exactly: modified `.planning/DECISIONS.md`, `.planning/STATE.md`, `CHANGELOG.md`, `anansi/appraisal.py`, `anansi/config.py`, `anansi/reflection.py`; untracked `anansi/tests/test_worker.py` and `.planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/`. Any other path means the worktree changed since planning: stop and report.

Step B, commit 1 (quick task, D-02). Restore `.planning/STATE.md` to the branch HEAD version (`git restore .planning/STATE.md`); its hunk is stale ("Phase 9 remains ready to plan") and conflicts with main (RESEARCH Plan 01). Run `./scripts/test.sh` and note the pass/fail counts. The only acceptable failure at this commit is the known time-dependent `test_persisted_flagged_priorities_survive_empty_signal_full_hook`; any other failure stops the plan. Stage the three code files, `anansi/tests/test_worker.py`, `CHANGELOG.md`, `.planning/DECISIONS.md` and the quick directory, and commit as `fix(appraisal): admit one shared worker request at a time (quick 260912-cqc)`, recording the observed counts in the commit body. Do not edit any test in this commit.

Step C, failing tests first. In `anansi/tests/test_drive_velocity.py`, add a helper `_write_linked_worktree(tmp_path, epoch, *, relative=False, dot_git_text=None)` modeled on `_write_git`: it writes the per-worktree git dir at `<tmp>/main/.git/worktrees/wt` (a `HEAD` file plus `logs/HEAD` holding one reflog line in the exact format `_write_git` uses) and writes `<tmp>/wt/.git` as a FILE containing `gitdir: <absolute or relative path>` followed by a newline, or `dot_git_text` verbatim when given. Add one test per behavior bullet above, using only `tmp_path` (module docstring rule: the real repo `.git` is never read for assertions). For the nested case, give the parent a real `.git` directory via `_write_git` with a different epoch and place the worktree underneath it. Run the file and confirm the new tests fail before the fix.

Step D, the fix (D-02). In `anansi/store.py`, add `_git_dir(repo_root)` directly above `_repo_root`: return `Path(repo_root) / ".git"` when that is a directory; when it is a file, read its first line with `open(path, "r", encoding="utf-8")`, and if the line starts with `gitdir:` take the remainder stripped, resolve a relative target against `repo_root`, call `.resolve()`, and return it only if it is an existing directory; return `None` for every other case and wrap the whole body in one `try/except Exception: return None` (Constitution I and IV: stdlib read-mode file reads only, no shelling out, no git library). Change the `_repo_root` walk-up predicate from the `.git`-is-a-directory check to `_git_dir(candidate) is not None`. In `_last_commit_epoch`, replace the hardcoded `Path(repo_root) / ".git"` with `git_dir = _git_dir(repo_root)` and return `None` when it is `None`; the existing HEAD and reflog parsing stays unchanged because per-worktree `HEAD` and `logs/HEAD` live under the `gitdir:` target. Update both docstrings to describe the linked-worktree `.git` file.

Step E, hermetic never-omit test (Constitution III, RESEARCH Pitfall 4). In `test_persisted_flagged_priorities_survive_empty_signal_full_hook` in `anansi/tests/test_drive_neveromit.py`, add `monkeypatch.setattr(store, "_repo_root", lambda start=None: None)` before the hook runs, copying the pin at `test_failopen_matrix.py` line 584. Change no assertion text. With the pin, momentum falls back to the goal's own fresh `updated_at`, so the "fresh progress" assertion is deterministic; this removes an ambient-state dependency and does not weaken the check.

Step F, docs. In `README.md`, replace the clause stating that linked Git worktrees are not recognized by the repository detector with a sentence saying a linked worktree's `.git` file is followed to that worktree's own reflog. Add a dated `CHANGELOG.md` entry at the top: Fixes (root cause: the walk-up accepted only a `.git` directory and `_last_commit_epoch` hardcoded `.git`, so a linked worktree read the enclosing checkout's reflog or nothing), Learnings (a test that reads ambient git state is a time bomb; pin discovery in hook-level tests).

Step G, commit 2. Run the targeted files, then the full suite, and commit `fix(store): follow linked-worktree .git files for goal momentum`.
  </action>
  <verify>
    <automated>./scripts/test.sh anansi/tests/test_worker.py anansi/tests/test_drive_velocity.py anansi/tests/test_drive_neveromit.py anansi/tests/test_anticreep.py -x</automated>
    <fails_when>non-zero exit, a final summary line containing "failed" or "error", or "no tests ran"</fails_when>
    <automated>./scripts/test.sh</automated>
    <fails_when>non-zero exit, or the final summary line reports any failed or error count</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --oneline main..codex/clarify-appraisal-timeout` (run in the main checkout) lists the quick-task commit and, after it, the `fix(store)` commit as two distinct commits.
    - With `QC` set to the quick-task commit SHA (the first line of `git log --reverse --format=%H main..codex/clarify-appraisal-timeout`), `git show --stat "$QC"` includes `anansi/tests/test_worker.py` and does not include `.planning/STATE.md`.
    - `grep -n "def _git_dir" anansi/store.py` prints exactly one line, and its line number is lower than that of `def _repo_root`.
    - `grep -c "_git_dir(" anansi/store.py` is at least 3 (definition, `_repo_root`, `_last_commit_epoch`).
    - The new linked-worktree tests (absolute, relative, nested, empty or malformed `.git` file, missing target, directory control, truncated reflog) exist in `anansi/tests/test_drive_velocity.py` and pass.
    - With `FX` set to the `fix(store)` commit SHA, `git diff "$FX^..$FX" -- anansi/tests/test_drive_neveromit.py` shows only added lines (the `_repo_root` pin) and no removed assertion line.
    - The full suite in the worktree ends with 0 failed and 0 errors.
    - `grep -n "not recognized" README.md` no longer matches the worktree sentence.
  </acceptance_criteria>
  <done>Two commits exist on the quick-task branch, the worktree suite is fully green without a waived test, and momentum follows linked worktrees.</done>
</task>

<task id="08.1-01-T2" type="auto">
  <name>Task 2: File the Phase 14 drafts, merge to main with --no-ff, push</name>
  <read_first>
    .planning/GSD-MIGRATION.md
    .planning/reference/README.md
    .planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
    .claude/worktrees/clarify-appraisal-timeout/.planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/260912-cqc-SUMMARY.md
    specs/LIVE-VERIFICATION.md
  </read_first>
  <files>.planning/reference/phase-14-inputs/README.md, .planning/reference/phase-14-inputs/DRAFT-07-SECURITY.md, .planning/reference/phase-14-inputs/DRAFT-COMPLETION-PLAN.md, .planning/reference/phase-14-inputs/DRAFT-LIVE-VERIFICATION.md, .planning/reference/phase-14-inputs/DRAFT-live_trust_smoke.py, .planning/GSD-MIGRATION.md, .planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/260912-cqc-SUMMARY.md</files>
  <action>
<!-- planner-discipline-allow: trust_fallback_inconclusive -->
Step A, relocate the drafts (D-02). The four sources are untracked files in the MAIN checkout: `docs/07-SECURITY.md`, `specs/COMPLETION-PLAN.md`, `specs/LIVE-VERIFICATION.md`, `scripts/live_trust_smoke.py`. In the worktree create `.planning/reference/phase-14-inputs/` and copy them there as `DRAFT-07-SECURITY.md`, `DRAFT-COMPLETION-PLAN.md`, `DRAFT-LIVE-VERIFICATION.md` and `DRAFT-live_trust_smoke.py`. The DRAFT marking is the filename prefix plus the directory README; three copies stay byte-identical so their recorded SHA-256 provenance still holds. `DRAFT-LIVE-VERIFICATION.md` gets exactly one change: delete the "Log of Live Runs" table row whose outcome column reads `outcome=trust_fallback_inconclusive` (line 21 at planning time); no live run produced it (grill log 2026-09-23). Write `.planning/reference/phase-14-inputs/README.md` stating: status DRAFT (not evidence, not an executable lane, not an authorization); these are Phase 14 inputs per D-02; each file's source path and original SHA-256 (`56acc696306e8c070bf503ed88319e519530f6e4b4945239719acee553eb92fb` for 07-SECURITY, `0bc04ae777addac3e7ee569810c457385a3f5fad5378ed5103bb82c41d996ef1` for COMPLETION-PLAN, `4b892252f6ac1996523b7938ee12f318a39273c3f4295398f6983a84f48d3a32` for LIVE-VERIFICATION, `dac4eb926a4a76ad625e1cab0ccce3decd3420188a7565033b6bf44c10fa4fc3` for live_trust_smoke.py); that the LIVE-VERIFICATION copy differs from its source by the single removed fabricated row; and that the smoke script is not a supported proof lane. Describe the removed row by date and script in the README without repeating its outcome string. Update `.planning/GSD-MIGRATION.md` "## Deferred inputs" so it names the new location, keeps the four original hashes as provenance, and states which file differs and why. Append a follow-up line to the quick-task SUMMARY recording the `fix(store)` commit SHA and the final suite count from Task 1. Commit `docs(planning): file Phase 14 draft inputs and close quick 260912-cqc`.

Step B, merge (D-02, D-09). From the main checkout: confirm `git branch --show-current` prints `main`; run `git diff --name-only main...codex/clarify-appraisal-timeout` and confirm none of those paths has an uncommitted modification in the main checkout (`git status --porcelain`); then `git merge --no-ff codex/clarify-appraisal-timeout -m "Merge quick 260912-cqc and linked-worktree momentum fix (08.1-01)"`. Run the full `./scripts/test.sh` in the main checkout.

Step C, remove the untracked originals. Only after the merge and green suite: confirm with `shasum -a 256` that the three tracked byte-identical copies match the hashes above, and that `diff specs/LIVE-VERIFICATION.md .planning/reference/phase-14-inputs/DRAFT-LIVE-VERIFICATION.md` shows exactly one deleted line; then delete the four untracked originals from the main checkout and remove `specs/` if it is empty. The bytes are preserved in tracked history, so this completes the move D-02 authorized.

Step D, publish (D-15). `git push origin main` from the main checkout, then run `graphify update .` in the main checkout (project rule after code changes).
  </action>
  <verify>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && ./scripts/test.sh</automated>
    <fails_when>non-zero exit, or the final summary line reports any failed or error count</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && shasum -a 256 .planning/reference/phase-14-inputs/DRAFT-07-SECURITY.md .planning/reference/phase-14-inputs/DRAFT-COMPLETION-PLAN.md .planning/reference/phase-14-inputs/DRAFT-live_trust_smoke.py</automated>
    <fails_when>a printed hash differs from 56acc696…eb92fb, 0bc04ae7…996ef1 or dac4eb92…4fa4fc3 respectively, or shasum reports a missing file</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && ! git grep -q trust_fallback_inconclusive -- .planning/reference/phase-14-inputs</automated>
    <fails_when>non-zero exit, meaning the fabricated outcome string is present in a tracked Phase 14 input</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && git fetch origin && git rev-list --count origin/main..main</automated>
    <fails_when>output other than 0 (main not pushed) or a fetch error</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /Volumes/Asylum/repos/hermes-anansi-plugin log -1 --merges --format=%s main` contains `08.1-01`.
    - `git -C /Volumes/Asylum/repos/hermes-anansi-plugin ls-files .planning/reference/phase-14-inputs` lists exactly five files: `README.md` and the four `DRAFT-` files.
    - `grep -n "phase-14-inputs" .planning/GSD-MIGRATION.md` prints at least one line, and the four original hashes still appear in that file.
    - `grep -c "DRAFT" .planning/reference/phase-14-inputs/README.md` is at least 1.
    - `docs/07-SECURITY.md`, `specs/COMPLETION-PLAN.md`, `specs/LIVE-VERIFICATION.md` and `scripts/live_trust_smoke.py` no longer exist in the main checkout.
    - The full suite on main ends with 0 failed and 0 errors, and `origin/main` equals local `main`.
  </acceptance_criteria>
  <done>The drafts are tracked as DRAFT Phase 14 inputs without the fabricated row, main carries the quick task and fix through a --no-ff merge, and origin/main matches.</done>
</task>

<task id="08.1-01-T3" type="auto">
  <name>Task 3: Close PR #2 and remove the superseded branches and the quick-task worktree</name>
  <reversibility rating="costly">Deleting origin/001-close-known-gaps prevents reopening PR #2 until the branch is re-pushed; the commits stay recoverable from GitHub's refs/pull/2/head, which is verified before the delete. Authorized by D-02 and D-15, so no checkpoint.</reversibility>
  <read_first>
    .planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
    .planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
    .planning/GSD-MIGRATION.md
    anansi/store.py
  </read_first>
  <files>.planning/phases/08.1-daily-use-v1/08.1-01-SUMMARY.md</files>
  <action>
Step A, no-unique-work evidence (D-02, prohibition on erasure). In the main checkout run `git log --oneline main..001-close-known-gaps`. At planning time it listed seven commits: `af2a0bc`, `5976df3`, `8da86e8`, `4bfbee6`, `cb986a2`, `61b28a5`, `531f390`. If the list differs, stop and report. Map each to evidence on main and verify each claim with a concrete command (grep, ls, or git log) before writing it down: `cb986a2` to main's schema-v5 additive upgrade (`_GOALS_V5_ADDED_COLUMNS` in `anansi/store.py`); `8da86e8` and `4bfbee6` to the Phase 8 plans 08-01..08-04 merged in `d46e489`; `5976df3` to Phase 8's CHANGELOG and README updates; `61b28a5` to the GSD migration recorded in `.planning/GSD-MIGRATION.md`; `af2a0bc` to the backlog specs preserved under `.planning/reference/` (list the directories); `531f390` to the do-not-port decision (D-02). Record the table in the plan SUMMARY. Confirm `git rev-parse 001-close-known-gaps origin/001-close-known-gaps` prints `af2a0bc3a45ceef3d37c15bba567e8f865879edc` twice.

Step B, PR #2 (D-02). If `gh pr view 2 --json state --jq .state` is already `CLOSED`, skip. Otherwise run `gh pr close 2 --comment "<text>"` with a comment saying: superseded by Phase 8, which re-implemented the G1-G8 gaps through GSD and merged in `d46e489`; the `531f390` model default is intentionally not ported (08.1 D-02); the untracked draft specs are filed as DRAFT Phase 14 inputs under `.planning/reference/phase-14-inputs/`. Do not pass `--delete-branch`.

Step C, branches (D-02, D-15). Run `git ls-remote origin refs/pull/2/head` and confirm it prints `af2a0bc3a45ceef3d37c15bba567e8f865879edc` (recoverability evidence; record it). Then `git branch -d 001-close-known-gaps`; if git refuses, stop and report, never use the force variant. Then `git push origin --delete 001-close-known-gaps` (D-15 authorizes this after PR #2 is closed; D-14's keep-the-remote default is superseded).

Step D, worktree cleanup (global cleanup rule). Re-audit live state first: `git worktree list`, `git -C .claude/worktrees/clarify-appraisal-timeout status --porcelain` must be empty, and `git merge-base --is-ancestor codex/clarify-appraisal-timeout main` must succeed. If removal refuses only because of gitignored staging directories (`.devtools/`, `__pycache__/`), delete those ignored directories and retry; never pass `--force` for unignored content. Run `git worktree remove .claude/worktrees/clarify-appraisal-timeout`, then `git branch -d codex/clarify-appraisal-timeout`, then `git worktree prune`.
  </action>
  <verify>
    <automated>gh pr view 2 --repo bionicbutterfly13/hermes-anansi-plugin --json state --jq .state</automated>
    <fails_when>output other than CLOSED, or a gh error</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && git branch --list 001-close-known-gaps codex/clarify-appraisal-timeout && git ls-remote --heads origin 001-close-known-gaps</automated>
    <fails_when>any branch name is printed, or a non-zero exit from git</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && git worktree list</automated>
    <fails_when>the output contains clarify-appraisal-timeout</fails_when>
  </verify>
  <acceptance_criteria>
    - The plan SUMMARY contains a seven-row mapping table with one verified main counterpart or the do-not-port record per commit, and the recorded `refs/pull/2/head` SHA `af2a0bc3a45ceef3d37c15bba567e8f865879edc`.
    - `gh pr view 2 --json state,comments` shows state `CLOSED` and a comment containing `d46e489`.
    - `git branch --list 001-close-known-gaps codex/clarify-appraisal-timeout` prints nothing, and `git ls-remote --heads origin 001-close-known-gaps` prints nothing.
    - `git worktree list` shows only the main checkout (plus any worktrees that existed for unrelated work, each named in the SUMMARY).
  </acceptance_criteria>
  <done>PR #2 is closed with a pointer to Phase 8, the superseded branches and the quick-task worktree are gone, and the no-unique-work evidence is recorded.</done>
</task>

</tasks>

<constitution_check>
- I Fail-open: `_git_dir` wraps its whole body and returns `None`; every caller already treats `None` as unknown momentum.
- II No autonomy: unchanged; no output text changes.
- III Never-omit: the release-gate test `test_persisted_flagged_priorities_survive_empty_signal_full_hook` gains a `_repo_root` pin with byte-identical assertions. Complexity Tracking: this touches a never-omit gate test; justification is that the test previously passed or failed on the age of the ambient checkout's reflog (RESEARCH Pitfall 4). The pin makes it deterministic and does not narrow what it asserts.
- IV Ground truth at read time: stdlib read-mode `open` of the `.git` file and per-worktree reflog; no subprocess, no git library.
- V Zero deps, paths discovered: the git dir comes from walk-up discovery, never a literal.
- VI Drive inspectability: unchanged.
- VII Minimal change: the quick task and the fix are separate commits; the drafts move without content edits except the one fabricated row.
</constitution_check>

<source_coverage>
D-02 (all parts), D-14 (superseded by D-15 for the remote), D-15 (push main, delete remote branch), REQ-08.1-02, RESEARCH Plan 01 and Pitfalls 3, 4, 10, PATTERNS Plan 01. Canon referral: none.
</source_coverage>

<verification>
Run `./scripts/test.sh` in the main checkout after the merge; a non-zero exit or any failed/error count blocks the plan. `git rev-list --count origin/main..main` must print 0, `gh pr view 2 --json state --jq .state` must print CLOSED, and `git worktree list` must not list the quick-task worktree.
</verification>

<success_criteria>
`main` contains quick 260912-cqc and the linked-worktree fix through one `--no-ff` merge, the full suite is green on main with no waived test, the Phase 14 drafts are tracked as DRAFT without the fabricated row, PR #2 is closed, and both obsolete branches and the quick-task worktree are gone.
</success_criteria>

<output>
Create `.planning/phases/08.1-daily-use-v1/08.1-01-SUMMARY.md` when done, including the seven-commit mapping table and the recorded refs/pull/2/head SHA.
</output>
