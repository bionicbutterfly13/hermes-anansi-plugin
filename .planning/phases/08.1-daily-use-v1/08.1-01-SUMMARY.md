---
phase: 08.1-daily-use-v1
plan: "01"
subsystem: store, worker scheduling, lineage
tags: [git-worktree, momentum, fail-open, never-omit, lineage-cleanup]

requires:
  - phase: 08-close-known-gaps
    provides: "Phase 8 merge d46e489 (G1-G8 re-implemented through GSD), schema v5, drive pressure"
provides:
  - "store._git_dir(): linked-worktree-aware git dir; _repo_root and _last_commit_epoch route through it"
  - "quick 260912-cqc shared-worker admission (skipped:worker_busy / reflect_skipped:worker_busy) on main"
  - "hermetic never-omit full-hook test (store._repo_root pinned)"
  - ".planning/reference/phase-14-inputs/ with four DRAFT inputs and provenance README"
  - "clean lineage: PR #2 closed, 001-close-known-gaps (local+remote) and codex/clarify-appraisal-timeout deleted, quick-task worktree removed"
affects: [08.1-02, 08.1-03, 08.1-05, phase-14]

actuals:
  tokens: 20988
  tasks: 3
  commits: 4
plan_head_before: adf6f5cb9a73ef5f8d8dc8025a71bc26a6dc2eeb

tech-stack:
  added: []
  patterns:
    - "Repository discovery follows a `.git` FILE's `gitdir:` line with stdlib read-mode open(); anything malformed returns None"
    - "Hook-level tests pin store._repo_root; git ground-truth parsing is tested only on tmp_path fixtures"

key-files:
  created:
    - anansi/tests/test_worker.py
    - .planning/reference/phase-14-inputs/README.md
    - .planning/reference/phase-14-inputs/DRAFT-07-SECURITY.md
    - .planning/reference/phase-14-inputs/DRAFT-COMPLETION-PLAN.md
    - .planning/reference/phase-14-inputs/DRAFT-LIVE-VERIFICATION.md
    - .planning/reference/phase-14-inputs/DRAFT-live_trust_smoke.py
    - .planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/260912-cqc-PLAN.md
    - .planning/quick/260912-cqc-prevent-shared-appraisal-and-reflection-/260912-cqc-SUMMARY.md
  modified:
    - anansi/store.py
    - anansi/appraisal.py
    - anansi/config.py
    - anansi/reflection.py
    - anansi/tests/test_drive_velocity.py
    - anansi/tests/test_drive_neveromit.py
    - README.md
    - CHANGELOG.md
    - .planning/DECISIONS.md
    - .planning/GSD-MIGRATION.md

key-decisions:
  - "Quick 260912-cqc committed as-is minus the stale worktree STATE.md hunk (restored to branch HEAD before staging)"
  - "_git_dir resolves a relative gitdir: against the directory holding the .git file and returns the target only if it is an existing directory"
  - "DRAFT-LIVE-VERIFICATION.md drops only the 2026-07-22 live_trust_smoke.py row; the other 2026-07-22 row is kept but labeled unverified in the DRAFT README"
  - "PR #2 comment also records that drive_flagged_want_cap is not adopted (INGEST-CONFLICTS constitution precedence)"

patterns-established:
  - "Ambient-state-free gate tests: pin discovery in any test that runs the full hook"

requirements-completed: [REQ-08.1-02]

coverage:
  - id: D1
    description: "Quick 260912-cqc shared worker admission landed as its own commit without the stale STATE.md hunk"
    requirement: REQ-08.1-02
    verification:
      - kind: unit
        ref: "anansi/tests/test_worker.py (14 cases)"
        status: pass
      - kind: other
        ref: "git show --stat 4d0b9b1 (includes test_worker.py, excludes .planning/STATE.md)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Goal momentum follows a linked worktree's .git file (absolute, relative, nested, malformed, directory control, truncated reflog)"
    requirement: REQ-08.1-02
    verification:
      - kind: unit
        ref: "anansi/tests/test_drive_velocity.py#test_linked_worktree_absolute_gitdir, test_linked_worktree_relative_gitdir, test_nested_linked_worktree_resolves_to_nearest_candidate, test_malformed_dot_git_file_yields_unknown, test_dot_git_directory_control_unchanged, test_truncated_final_reflog_line_returns_none"
        status: pass
    human_judgment: false
  - id: D3
    description: "Never-omit full-hook test is hermetic with byte-identical assertions"
    requirement: REQ-08.1-02
    verification:
      - kind: unit
        ref: "anansi/tests/test_drive_neveromit.py#test_persisted_flagged_priorities_survive_empty_signal_full_hook"
        status: pass
      - kind: other
        ref: "old version fails / pinned version passes when run from a cwd with a 9-day-stale reflog"
        status: pass
    human_judgment: false
  - id: D4
    description: "Full suite green in the worktree and on main after the --no-ff merge; main pushed"
    requirement: REQ-08.1-02
    verification:
      - kind: integration
        ref: "./scripts/test.sh (worktree 219 passed; main 219 passed); git rev-list --count origin/main..main == 0"
        status: pass
    human_judgment: false
  - id: D5
    description: "Four drafts tracked as DRAFT Phase 14 inputs; three byte-identical, one minus the fabricated row; originals removed"
    requirement: REQ-08.1-02
    verification:
      - kind: other
        ref: "shasum -a 256 (three hashes match); diff shows exactly one deleted line; git grep trust_fallback_inconclusive empty"
        status: pass
    human_judgment: false
  - id: D6
    description: "PR #2 closed with d46e489 pointer; obsolete branches and quick-task worktree removed after no-unique-work mapping"
    requirement: REQ-08.1-02
    verification:
      - kind: other
        ref: "gh pr view 2 --json state,comments (CLOSED, comment contains d46e489); git branch --list / ls-remote --heads empty; git worktree list shows main only"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-09-24
status: complete
---

# Phase 08.1 Plan 01: Lineage Cleanup and Linked-Worktree Momentum Summary

**Shared-worker admission (quick 260912-cqc) and a stdlib `_git_dir()` that follows linked-worktree `.git` files landed on main through `--no-ff` merge `e3daa90` (219 passed), with the Phase 14 drafts filed as DRAFT and the PR #2 lineage closed and deleted.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-09-24T21:41:32Z
- **Completed:** 2026-09-24T21:53:43Z
- **Tasks:** 3
- **Files modified:** 18

## Accomplishments

- Quick 260912-cqc committed on its own (`4d0b9b1`); the stale worktree STATE.md hunk was restored, not committed.
- `store._git_dir()` added; `_repo_root` and `_last_commit_epoch` route through it, so a nested linked worktree resolves to itself and reads its own reflog. Six tmp_path fixture tests cover it and failed before the fix.
- The never-omit full-hook test now pins `store._repo_root` (two added lines, no assertion changed). Evidence: the pre-fix version fails and the pinned version passes when run from a cwd whose reflog is 9 days old.
- Four drafts filed under `.planning/reference/phase-14-inputs/` with a provenance README; the untracked originals were deleted from the main checkout after hash and diff checks.
- `e3daa90` merged with `--no-ff`, suite green on main (219 passed, 0 failed), pushed; `origin/main` == `main`.
- PR #2 closed unmerged; `001-close-known-gaps` deleted locally (non-force) and on origin; `codex/clarify-appraisal-timeout` and its worktree removed.

## Task Commits

1. **Task 1: Commit quick 260912-cqc, then make momentum follow a linked worktree** - `4d0b9b1` (fix, quick task), `757457c` (fix, store)
2. **Task 2: File the Phase 14 drafts, merge to main with --no-ff, push** - `55e8573` (docs), merge `e3daa90`
3. **Task 3: Close PR #2 and remove the superseded branches and worktree** - no commit (GitHub and ref operations only)

**Plan metadata:** see final commit step (planning docs commit is governed by `commit_docs: false`).

Suite counts: worktree at `4d0b9b1` 213 passed / 0 failed; worktree at `757457c` 219 passed / 0 failed; main at `e3daa90` 219 passed / 0 failed.

## No-unique-work mapping for `001-close-known-gaps` (Task 3 Step A)

`git log --oneline main..001-close-known-gaps` listed exactly the seven planned commits. `git rev-parse 001-close-known-gaps origin/001-close-known-gaps` printed `af2a0bc3a45ceef3d37c15bba567e8f865879edc` twice.

| Commit | Subject | Main counterpart or record | Verified by |
|---|---|---|---|
| `af2a0bc` | backlog specs | `.planning/reference/` holds `001-close-known-gaps`, `002-autobiographical-user-model`, `003-reconsolidation-and-heartbeat`, `004-interruption-lanes`, `005-tuning-and-audit-surfaces`, `006-deferred-v1-and-parity`, `007-drive-security-verification`, `BACKLOG.md` | `ls .planning/reference/` |
| `5976df3` | changelog + config reference | Phase 8 CHANGELOG entries (2026-09-11 "Phase 8 review repairs", "Phase 8 evidence boundary and regression closure"), `drive_pressure` documented in `anansi/README.md:113,146`, root README aligned in `1e65010`. Its `drive_flagged_want_cap` docs are superseded by the constitution-precedence resolution in `.planning/INGEST-CONFLICTS.md:13-15` | `grep -n "^## " CHANGELOG.md`; `grep -n drive_pressure anansi/README.md`; `grep -n withheld .planning/INGEST-CONFLICTS.md` |
| `8da86e8` | drive pressure, flagged wants, matching, stalled=0, config-degradation telemetry | Phase 8 plans 08-01..08-04 (PLAN + SUMMARY present) merged in `d46e489`; main has `drive_pressure` coercion and `get_degradations()` / `_config_degradations()` in `anansi/config.py:258,306`. The flagged-want cap is not adopted (INGEST-CONFLICTS precedence) | `git merge-base --is-ancestor d46e489 main`; `ls .planning/phases/08-close-known-gaps/`; `grep -n "def get_degradations" anansi/config.py` |
| `4bfbee6` | rename master->primary kill-switch test | `test_primary_kill_switch_disables_reflection` at `anansi/tests/test_reflection.py:518`, via Phase 8 (`d46e489`) | `grep -n primary_kill_switch anansi/tests/test_reflection.py` |
| `cb986a2` | schema v5 additive upgrade | `SCHEMA_VERSION = 5` (`anansi/store.py:35`), `_GOALS_V5_ADDED_COLUMNS` (`:119`), `_try_upgrade_v4_to_v5` (`:247`) | `grep -n` on `anansi/store.py` |
| `61b28a5` | adopt Spec Kit | Superseded by the GSD migration recorded in `.planning/GSD-MIGRATION.md` (line 40 records the byte-for-byte intake comparison against `af2a0bc`); `.specify/` no longer exists on main | `grep -n 001-close-known-gaps .planning/GSD-MIGRATION.md`; `ls .specify` |
| `531f390` | default appraisal model to gpt-4o-mini | Do-not-port record: D-02 in `08.1-CONTEXT.md:21` and scope fence line 106; main keeps `DEFAULT_MODEL = None` (`anansi/config.py:87`) | `git show 531f390 -- anansi/config.py`; `grep -n "^DEFAULT_MODEL" anansi/config.py` |

**Recoverability:** `git ls-remote origin refs/pull/2/head` printed `af2a0bc3a45ceef3d37c15bba567e8f865879edc` before and after the remote branch delete.

**PR #2:** state `CLOSED`, unmerged, comment points at `d46e489`, records that `531f390` is not ported and that the flagged-want cap is not adopted, and names `.planning/reference/phase-14-inputs/`. `--delete-branch` was not used.

**Worktrees after cleanup:** `git worktree list` shows only the main checkout (`/Volumes/Asylum/repos/hermes-anansi-plugin`). No unrelated worktrees existed.

## Files Created/Modified

- `anansi/store.py` - `_git_dir()` helper; linked-worktree-aware `_repo_root` and `_last_commit_epoch`
- `anansi/tests/test_drive_velocity.py` - `_write_linked_worktree` helper and six linked-worktree tests
- `anansi/tests/test_drive_neveromit.py` - `_repo_root` pin in the persisted-flagged full-hook test
- `anansi/appraisal.py`, `anansi/config.py`, `anansi/reflection.py`, `anansi/tests/test_worker.py` - quick 260912-cqc (landed as-is)
- `README.md`, `CHANGELOG.md` - worktree limitation sentence replaced; dated 2026-09-24 entry with Fixes and Learnings
- `.planning/DECISIONS.md`, `.planning/quick/260912-cqc-*/` - quick-task record, SUMMARY follow-up line
- `.planning/reference/phase-14-inputs/` - README plus four DRAFT files
- `.planning/GSD-MIGRATION.md` - "Deferred inputs" points at the new location, keeps the four original hashes

## Decisions Made

- Kept the plan's single-row removal from `DRAFT-LIVE-VERIFICATION.md`. The other 2026-07-22 row (`live_drive_smoke.py`, timeout) has no independent record in the repository either; it stays in the byte-derived copy but the DRAFT README labels it an unverified claim, not evidence.
- Wrote the `GSD-MIGRATION.md` note as a forward statement ("deletes ... only after the merge and a hash check") because the commit precedes the deletion; the deletion then happened as stated.
- Commit messages carry no AI attribution trailers, per the orchestrator-relayed standing rule and the repository's existing history.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Staged pytest by copying `.devtools/` instead of letting test.sh install it**
- **Found during:** Task 1 Step B
- **Issue:** The worktree had no `.devtools/pytest`; `test.sh` would run `uv pip install` (a package install).
- **Fix:** Copied the main checkout's gitignored `.devtools/` into the worktree, as the plan's integration protocol allows.
- **Files modified:** none tracked
- **Verification:** `git status --porcelain` unchanged; suite ran.

**2. [Rule 2 - Transparency] PR #2 comment and DRAFT README carry two extra facts**
- **Found during:** Tasks 2 and 3
- **Issue:** `5976df3`/`8da86e8` also carried a `drive_flagged_want_cap` that main deliberately did not adopt; the remaining 2026-07-22 ledger row is unverified.
- **Fix:** The PR comment names the INGEST-CONFLICTS precedence resolution; the DRAFT README labels the remaining row unverified.
- **Verification:** `gh pr view 2 --json comments`; README text.

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 transparency). **Impact:** no scope change; no code or assertion affected.

## Issues Encountered

- At commit 1 the suite was 213 passed / 0 failed, not the recorded 212/1: the time-dependent never-omit test passed only because main's reflog was fresh (RESEARCH finding 2). Commit 2 removed the dependency; the stale-reflog reproduction confirms it.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `main` at `e3daa90` is the clean baseline for 08.1-02; `origin/main` matches.
- 08.1-02 still has its own preconditions (D-10 fork re-sync, chief-of-staff repair confirmation).
- The orchestrator owns STATE.md, ROADMAP.md and the record of quick 260912-cqc in main's STATE.md.

---
*Phase: 08.1-daily-use-v1*
*Completed: 2026-09-24*

## Self-Check: PASSED

- Files: `anansi/store.py` (`def _git_dir` at line 363, before `_repo_root` at 389), `anansi/tests/test_worker.py`, five files under `.planning/reference/phase-14-inputs/` all tracked on main.
- Commits: `4d0b9b1`, `757457c`, `55e8573`, `e3daa90` present in `git log` on main; `git rev-list --count adf6f5c..HEAD` = 4.
- Plan verification: main suite 219 passed; `origin/main..main` = 0; PR #2 `CLOSED`; worktree list shows main only.
