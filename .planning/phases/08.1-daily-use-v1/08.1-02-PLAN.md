---
phase: 08.1-daily-use-v1
plan: "02"
type: execute
wave: 2
depends_on:
  - "08.1-01"
files_modified:
  - .planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md
autonomous: false
requirements:
  - REQ-08.1-05
  - REQ-08.1-07
coupling_justified:
  - "08.1-03: both touch shared mutable state outside the repo. (1) The hermes-agent host checkout and venv at $HOME/.hermes/hermes-agent: 08.1-03's test gate runs on it and this plan's Task 2 re-syncs it (D-10). Task 2 re-syncs only when no other 08.1 plan worktree exists, and 08.1-03 checks for an in-progress host merge before each full-suite run, so a test gate never runs on a half-merged host; either order is correct because the suite must pass on the re-synced host anyway. (2) The chief-of-staff anansi state.db: 08.1-03 upgrades it to schema v6 additively and backs it up first when this plan's install already exists; if 08.1-03 merges first, this plan's first session creates the DB directly at v6. Both orders preserve every row."
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Dr. Mani confirmed the chief-of-staff repair workstream is finished before this plan wrote anything to that profile (CONTEXT evidence_2026_09_24 precondition)."
    - "D-10: the hermes-agent fork was re-synced with NousResearch upstream immediately before install (or recorded as already in sync), and `./scripts/test.sh` plus the host plugin validator both pass on the re-synced host before any profile write."
    - "D-05: `$PROFILE_HOME/plugins/anansi` is a symlink to `/Volumes/Asylum/repos/hermes-anansi-plugin/anansi` (the main checkout, which stays on main), `anansi` is in chief-of-staff `plugins.enabled`, and `hermes -p chief-of-staff plugins doctor anansi --ci` exits 0."
    - "A real chief-of-staff turn wrote at least one anansi telemetry row to `$PROFILE_HOME/anansi/state.db`, and that row is recorded verbatim, whatever its outcome."
    - "D-07, D-09: `scripts/live_drive_smoke.py` ran against the chief-of-staff profile on the main model and its exit code, outcome, wall_ms, model and error are recorded verbatim in `08.1-LIVE-SMOKE.md` as the latency baseline, whatever the outcome."
    - "Every long-lived chief-of-staff process that started before the enable was restarted or is named in the record as awaiting Dr. Mani's restart; no gateway that was not running was started."
    - "D-05: the rollback command (`plugins.entries.anansi.enabled false` plus restart) and the full-uninstall steps are written in the record."
    - "[EDGE REQ-08.1-05 adjacency] If `$PROFILE_HOME/plugins/anansi` already exists and is anything other than a symlink to the main checkout's `anansi/`, the install stops without modifying it; an existing symlink to that exact target is accepted unchanged."
    - "[EDGE REQ-08.1-05 empty] An absent `$PROFILE_HOME/plugins/` directory is created only by `mkdir -p` immediately before the symlink, and an absent `plugins.entries.anansi` block means the plugin runs on its defaults (enabled true); this plan writes no `plugins.entries.anansi` key."
    - "[EDGE REQ-08.1-05 ordering] `anansi` enters `plugins.enabled` before the verification turn runs, and the verified telemetry row's `ts` is later than the recorded enable time."
  artifacts:
    - path: .planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md
      provides: "install record, real-turn baseline row, smoke exit and latency baseline, rollback steps"
      contains: "08.1-02"
  key_links:
    - from: "$PROFILE_HOME/plugins/anansi"
      to: anansi/__init__.py
      via: "directory symlink followed by host plugin discovery (plugins_discovery child.is_dir) so every new chief-of-staff process imports the main checkout's plugin"
      pattern: "readlink"
    - from: anansi/__init__.py
      to: "$PROFILE_HOME/anansi/state.db"
      via: "pre_llm_call record_telemetry under the profile's HERMES_HOME"
      pattern: "telemetry"
  assumptions:
    - id: codex-auth-unverified
      status: flagged-unresolved
      statement: "openai-codex authentication on chief-of-staff was not probed at planning time (secrets not read). A provider failure yields an honest exit 2 INCONCLUSIVE, which this plan records as the baseline rather than retrying around it."
    - id: long-lived-processes
      status: flagged-unresolved
      statement: "hermes-fork-upstream-merge skill notes Dr. Mani runs chief-of-staff from the CLI and does not run gateways; research saw no gateway process. Long-lived processes are enumerated at run time, not assumed."
  prohibitions:
    - requirement_id: REQ-08.1-05
      category: safety
      statement: "MUST NOT write to the chief-of-staff profile (plugins/, config.yaml, .env) before Dr. Mani confirms the repair workstream is finished, and MUST NOT change any profile key other than adding anansi to plugins.enabled."
      status: unresolved
      verification: null
      flagged: true
    - requirement_id: REQ-08.1-07
      category: transparency
      statement: "MUST NOT record a live smoke result, exit code, latency, or model that an actual run in this plan did not produce; FAIL and INCONCLUSIVE results are recorded as such and never retried into a PASS by changing the environment silently."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Install Anansi in the `chief-of-staff` Hermes profile and record the first authorized live result on the main model as the latency baseline (D-05, D-07, D-09, D-10).

Purpose: Dr. Mani can only use Anansi daily once it loads in the profile he works in, and later latency decisions (D-13) need a main-model baseline.
Output: host re-synced (D-10), symlink install enabled, one real turn's telemetry row, one `live_drive_smoke.py` run, all recorded in `08.1-LIVE-SMOKE.md`.

This plan changes no tracked source, so it creates no branch or worktree; its only repository artifact is the planning record (commit_docs is false; planning docs are committed with the phase docs). Plans 08.1-03..06 do not depend on this plan; only 08.1-06 Task 2 (live activation) waits for it through a precondition.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/reference/CONSTITUTION.md
@.planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
@.planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
@.planning/phases/08-close-known-gaps/08-LIVE-SMOKE.md
@scripts/live_drive_smoke.py
</context>

## Artifacts this phase produces

- `.planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md` (new record) with sections `## Install (08.1-02)`, `## Real-turn baseline (08.1-02)`, `## Smoke runs`, `## Exit semantics`, `## Rollback`, and one machine-checkable line `- smoke 08.1-02 exit: <0|1|2>`.
- Operator state outside the repo (not `files_modified`): directory symlink `$PROFILE_HOME/plugins/anansi`; `anansi` in chief-of-staff `plugins.enabled`; `$PROFILE_HOME/anansi/state.db` created by the first session; host tag `pre-upstream-sync-<YYYYMMDD>` and a local merge commit in `$HOME/.hermes/hermes-agent` (not pushed).
- `$PROFILE_HOME` below always means `dirname "$(hermes -p chief-of-staff config path)"`, resolved at run time, never typed as a literal.

<tasks>

<task id="08.1-02-T1" type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Dr. Mani confirms the chief-of-staff repair is finished</name>
  <read_first>
    .planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
    .planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
  </read_first>
  <action>Confirm that the separate chief-of-staff repair workstream is finished, so installing Anansi cannot collide with it (CONTEXT evidence_2026_09_24, Plan 02 precondition).</action>
  <instructions>
    Before presenting this checkpoint, Claude does the read-only groundwork: resolve `$PROFILE_HOME`; list `$PROFILE_HOME/plugins/` (names and symlink targets only); print the top-level and `plugins.*` / `auxiliary.*` key names of `$PROFILE_HOME/config.yaml` without printing any value of a secret-bearing field; list running chief-of-staff processes with `ps -axo pid,lstart,command` filtered to `-p chief-of-staff` (read-only). Present that snapshot.
    The one thing only Dr. Mani can do: confirm the repair is finished. Also ask him to name any open interactive chief-of-staff session he wants to keep running during install (it will need a restart afterwards).
    If he says the repair is still in progress, this plan halts here as a designed stop; 08.1-03..06 continue without it.
  </instructions>
  <verification>After the reply, re-run the same read-only snapshot and compare it with the one presented. Any change to `plugins/` or the listed config keys is reported to Dr. Mani before Task 2 starts.</verification>
  <resume-signal>Type "repair done", or describe what is still in progress</resume-signal>
  <acceptance_criteria>
    - Dr. Mani's reply explicitly states the repair is finished; any other reply halts the plan.
    - The pre- and post-reply snapshots are both recorded in the plan SUMMARY (key names only, no secret values).
  </acceptance_criteria>
</task>

<task id="08.1-02-T2" type="tracer">
  <name>Task 2: Re-synced host to one real chief-of-staff turn with anansi telemetry</name>
  <precondition>`git -C /Volumes/Asylum/repos/hermes-anansi-plugin worktree list` shows no 08.1 plan worktree, and `pgrep -fl 'hermes update'` prints nothing, so no test gate or updater is running against the host venv while it is re-synced.</precondition>
  <read_first>
    .planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
    .planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
    .planning/phases/08-close-known-gaps/08-LIVE-SMOKE.md
    ~/.claude/skills/hermes-fork-upstream-merge/SKILL.md
    scripts/test.sh
    anansi/__init__.py
  </read_first>
  <files>.planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md</files>
  <action>
Step A, host re-sync (D-10), following the `hermes-fork-upstream-merge` skill in `$HOME/.hermes/hermes-agent`. `git fetch upstream`. If `git rev-list --count HEAD..upstream/main` prints 0, record "already in sync at <sha>" and go to Step B. Otherwise: tag the current HEAD `pre-upstream-sync-<YYYYMMDD>`; dry-run with `git merge-tree --write-tree --name-only upstream/main HEAD` and stop with a report if it lists conflicting paths; merge with `git pull --no-commit --no-ff upstream main` and commit with a message naming the fork-only files carried; install new Python dependencies with the skill's editable-install command; run the skill's import smoke. For the config migration step (`hermes update --yes`), if the command classifier denies it, ask Dr. Mani to run it himself (skill gotcha) and wait. Do not push the fork: D-15 covers only this repository; record the new merge SHA for Dr. Mani.

Step B, gates on the re-synced host (D-10). From the main checkout run the full `./scripts/test.sh`. Run the host plugin validator on the main checkout's `anansi/` directory under a scratch `HERMES_HOME`, as the skill's post-sync step prescribes (the verify block holds the exact command). Any failure stops the plan before the profile is touched; report the failing test or check.

Step C, install (D-05). Resolve `$PROFILE_HOME`. Re-read `$PROFILE_HOME/plugins/` (Task 1 snapshot rule). If `$PROFILE_HOME/plugins/anansi` exists and is not a symlink whose target is `/Volumes/Asylum/repos/hermes-anansi-plugin/anansi`, stop without modifying it. Otherwise `mkdir -p "$PROFILE_HOME/plugins"` and create the directory symlink to the main checkout's `anansi/` (never a plan worktree: RESEARCH Pitfall 10). Run `hermes -p chief-of-staff plugins enable anansi`; if it asks for interactive capability consent, stop and ask Dr. Mani to run the same command himself. Record the enable time in UTC. Run `hermes -p chief-of-staff plugins doctor anansi --ci` and `hermes -p chief-of-staff plugins list`. Write no other profile key (prohibition P-safety).

Step D, one real turn (D-07 authorizes live runs). Run one one-shot turn with plugin debug logging: `HERMES_PLUGINS_DEBUG=1 hermes -p chief-of-staff -z "Anansi install check: in one sentence, what is one risk of adding a pre-reply appraisal step?"` (the message asks for no tools). Read the newest telemetry rows through a read-only URI: `sqlite3 "file:$PROFILE_HOME/anansi/state.db?mode=ro" "SELECT ts,outcome,wall_ms,model,error FROM telemetry ORDER BY id DESC LIMIT 5;"`.

Step E, record. Create `08.1-LIVE-SMOKE.md` by copying the section order and the exit-semantics text of `08-LIVE-SMOKE.md` verbatim, with: Authorization citing D-07 and D-05; `## Install (08.1-02)` (host sync SHA or "already in sync", validator result, symlink target, enable time, doctor result); `## Real-turn baseline (08.1-02)` with the telemetry rows exactly as returned.
  </action>
  <verify>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && ./scripts/test.sh</automated>
    <fails_when>non-zero exit, or the final summary line reports any failed or error count</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && HERMES_HOME="$(mktemp -d)" "$HOME/.hermes/hermes-agent/venv/bin/python" -c "import sys; from pathlib import Path; from hermes_cli.plugin_validate import validate_plugin_dir; r = validate_plugin_dir(Path('anansi')); print(r.to_dict()); sys.exit(r.exit_code)"</automated>
    <fails_when>non-zero exit, or an ImportError/Traceback in the output</fails_when>
    <automated>readlink "$(dirname "$(hermes -p chief-of-staff config path)")/plugins/anansi"</automated>
    <fails_when>output other than /Volumes/Asylum/repos/hermes-anansi-plugin/anansi, or non-zero exit</fails_when>
    <automated>hermes -p chief-of-staff plugins doctor anansi --ci</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>sqlite3 "file:$(dirname "$(hermes -p chief-of-staff config path)")/anansi/state.db?mode=ro" "SELECT COUNT(*) FROM telemetry;"</automated>
    <fails_when>output 0, "unable to open database", or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - The record's `## Install (08.1-02)` section names the host HEAD SHA after Step A and the validator exit code 0.
    - `readlink` of the profile's `plugins/anansi` prints `/Volumes/Asylum/repos/hermes-anansi-plugin/anansi`.
    - `hermes -p chief-of-staff plugins list` output shows `anansi` enabled.
    - The telemetry row recorded under `## Real-turn baseline (08.1-02)` has a `ts` later than the recorded enable time.
    - `git -C "$HOME/.hermes/hermes-agent" status --porcelain` is empty and no push of the fork occurred (the SUMMARY states the unpushed SHA).
  </acceptance_criteria>
  <done>A re-synced host loads Anansi in chief-of-staff and one real turn produced a recorded telemetry row.</done>
</task>

<task id="08.1-02-T3" type="auto">
  <name>Task 3: Record the main-model latency baseline with the live smoke, and settle long-lived processes</name>
  <read_first>
    scripts/live_drive_smoke.py
    .planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md
    .planning/phases/08-close-known-gaps/08-LIVE-SMOKE.md
    .planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
    ~/.claude/skills/launchd-agent-exit-78-external-volume-logs/SKILL.md
  </read_first>
  <files>.planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md</files>
  <action>
Step A, long-lived processes (REQ-08.1-05). List running chief-of-staff processes with `ps -axo pid,lstart,command` filtered to `-p chief-of-staff` (exclude the grep itself). For each process started before the recorded enable time: a gateway is restarted with `hermes -p chief-of-staff gateway restart`, and if that does not return within 60 seconds, with the stop-then-start sequence from the `launchd-agent-exit-78-external-volume-logs` skill, confirmed by `hermes -p chief-of-staff gateway status`; an interactive CLI session or the desktop app belongs to Dr. Mani, so list it and ask him to restart it rather than killing it. Never start a gateway that was not running (hermes-fork-upstream-merge skill gotcha). Record each process and its outcome.

Step B, live smoke on the main model (D-07, D-09). From the main checkout: `HERMES_HOME="$PROFILE_HOME" "$HOME/.hermes/hermes-agent/venv/bin/python" scripts/live_drive_smoke.py; echo "exit=$?"`. The host package imports from the venv without the script's own path bootstrap (RESEARCH Plan 02). Record the exit code and the printed `outcome`, `wall_ms`, `model` and `error` verbatim in a `## Smoke runs` table (`Date (UTC) | Plan | Command | Exit | Outcome | wall_ms | Model | Error`) and add the line `- smoke 08.1-02 exit: <code>`. Record the result whatever it is (D-07: "records the first run and its latency baseline whatever the outcome"). The script probes api.anthropic.com before the provider call (RESEARCH Pitfall 11, fixed in 08.1-05); if it prints the pending-network INCONCLUSIVE line, record that reason. Do not re-run with a changed environment to chase a PASS.

Step C, rollback and status. Add `## Rollback`: `hermes -p chief-of-staff config set plugins.entries.anansi.enabled false`, then restart long-lived processes; appraisal and reflection stop at once and turn capture stops once 08.1-06 lands; full uninstall is `hermes -p chief-of-staff plugins disable anansi` plus removing the symlink. Set the record's Status line to PASS only for exit 0, FAIL for exit 1, INCONCLUSIVE for exit 2.
  </action>
  <verify>
    <automated>grep -E "^- smoke 08\.1-02 exit: [012]$" /Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md</automated>
    <fails_when>no matching line (grep exit 1)</fails_when>
    <automated>grep -c "plugins.entries.anansi.enabled false" /Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md</automated>
    <fails_when>output 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `08.1-LIVE-SMOKE.md` contains one `## Smoke runs` row for 08.1-02 whose Exit, Outcome, wall_ms, Model and Error cells equal the script's printed values.
    - The Status line matches the exit code (0 PASS, 1 FAIL, 2 INCONCLUSIVE).
    - Every chief-of-staff process found in Step A is listed with "restarted", "awaiting Dr. Mani", or "started after enable".
    - The `## Rollback` section contains the enabled-false command and the uninstall steps.
  </acceptance_criteria>
  <done>The main-model latency baseline and install record exist, and every long-lived chief-of-staff process is accounted for.</done>
</task>

</tasks>

<constitution_check>
- I Fail-open: no code change; the live runs exercise the existing fail-open paths and record their outcomes.
- II No autonomy: the install adds hooks only; no tool, no memory-provider write.
- III Never-omit: unchanged code; the smoke's PASS criterion is the flagged drive want surfacing.
- IV Single SQLite surface: the profile's `state.db` is created by `store.ensure_db`; this plan reads it only through read-only URIs.
- V Paths from config: `$PROFILE_HOME` is derived from `hermes -p chief-of-staff config path`; plugin code still resolves paths from `HERMES_HOME`.
- VI, VII: unchanged; the live result is recorded honestly whatever it is.
</constitution_check>

<source_coverage>
D-05, D-07 (first recorded run), D-09 (plan 02 position), D-10, the CONTEXT Plan 02 precondition, REQ-08.1-05, REQ-08.1-07 (partial: first smoke; the window and review live in 08.1-06), RESEARCH Plan 02 and Pitfalls 10, 11.
</source_coverage>

<verification>
`readlink` of the profile's `plugins/anansi` points at the main checkout's `anansi/`, `hermes -p chief-of-staff plugins doctor anansi --ci` exits 0, the profile telemetry table has at least one row, and `08.1-LIVE-SMOKE.md` carries a `- smoke 08.1-02 exit: N` line with the verbatim values.
</verification>

<success_criteria>
Anansi loads in chief-of-staff from the main checkout, a real turn produced telemetry, and the main-model smoke result and latency baseline are recorded exactly as observed.
</success_criteria>

<output>
Create `.planning/phases/08.1-daily-use-v1/08.1-02-SUMMARY.md` when done, including both profile snapshots, the host SHA, and the process list.
</output>
