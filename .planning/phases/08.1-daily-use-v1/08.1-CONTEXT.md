# Phase 08.1: Daily-use v1 - Context

**Source:** ADR Ingest Express Path (`.planning/phases/08.1-daily-use-v1/08.1-ADR.md`), derived verbatim from
`.planning/grill-logs/2026-09-23-anansi-valuable-completion.md` (Q1-Q9 + corrections).

<domain>
Make Anansi a v1 that Dr. Mani uses every day in the `chief-of-staff` Hermes profile, prove it live, and collect
seven days of evidence. Then decide whether Phases 9-14 proceed. The phase adds no new cognitive layers.
</domain>

<decisions>
## Locked decisions (Dr. Mani, 2026-09-23; plan confirmed 2026-09-24)

- **D-01:** Valuable completion means a v1 that Dr. Mani uses daily; the roadmap is then decided from real use ("b").
- **D-02:** Lineage cleanup ("a"): commit quick task 260912-cqc on `codex/clarify-appraisal-timeout`
  (worktree `.claude/worktrees/clarify-appraisal-timeout`); fix `store._repo_root()` to follow a linked worktree's
  `.git` FILE, with a test, as its own commit; merge with `--no-ff`; move the 4 untracked drafts
  (`docs/07-SECURITY.md`, `scripts/live_trust_smoke.py`, `specs/COMPLETION-PLAN.md`, `specs/LIVE-VERIFICATION.md`) to
  `.planning/reference/phase-14-inputs/` marked DRAFT, dropping the fabricated `trust_fallback_inconclusive` ledger row;
  close PR #2 unmerged with a comment pointing at Phase 8; delete branch `001-close-known-gaps` (non-force, after
  confirming no unique work beyond the recorded `531f390`). Do NOT port `531f390`.
- **D-03:** Every appraisal block carries a turn marker in its header plus an "applies to this message only" line;
  each block's token size is recorded in telemetry. The host fork is not patched ("a").
- **D-04:** Goal entry: user-invoked `/goal` slash command via `ctx.register_command`
  (`add <text> [--domain X] [--files a,b]`, `flag <id>`, `status <id> active|queued|backburner`, `list`) plus
  `hermes anansi goals ...` via `ctx.register_cli_command`, one shared handler. No agent-invoked tool may write goals
  (Constitution II) ("a").
- **D-05:** Install in profile `chief-of-staff` via symlink `$HERMES_HOME/plugins/anansi` -> repo `anansi/`; rollback
  is `enabled: false` ("a").
- **D-06:** Register auxiliary tasks `anansi_appraisal` and `anansi_reflection`; pass `task=` to
  `ctx.llm.complete_structured`. chief-of-staff config: `auxiliary.anansi_appraisal` = provider `openai-codex`,
  model `gpt-6-sol` (Dr. Mani: "gpt 6 sol is the model to use"). Reflection stays `auto`. Fall back to the
  main-model path when `register_auxiliary_task` is absent ("a", corrected).
- **D-07:** v1 exit gate ("a"; authorizes the live run): `scripts/live_drive_smoke.py` exit 0 recorded; 7-day telemetry
  thresholds; day-7 review of 20 sampled `ANANSI_DEBUG_DUMP` blocks labeled useful / neutral / misleading.
- **D-08:** `enabled: false` also stops turn capture; regression test asserts no `turn_log` row ("a").
- **D-09:** Plan order: 01 cleanup; 02 install + first live smoke on the main model (latency baseline); 03 turn
  marker + block-size telemetry; 04 `/goal`; 05 aux routing; 06 kill switch ("yes").

## Binding constraints

- Constitution Principles I-VII (`.planning/reference/CONSTITUTION.md`) bind every plan: fail-open, no autonomy,
  never-omit, single SQLite surface + ground truth at read time, zero new dependencies + paths from config,
  inspectable drive, minimal surgical change.
- `.planning/INGEST-CONFLICTS.md` precedence still applies (no cap may withhold a flagged priority).
- Commits and `--no-ff` merges for this phase's plans are explicitly authorized (D-02, D-09). Pushing to origin is authorized at checkpoints (D-15).
- **D-10 (2026-09-24):** Re-sync the hermes-agent fork with NousResearch upstream once more immediately before plan 08.1-02 (install), then re-run `./scripts/test.sh` and the host plugin validator. Do not sync during planning. (Dr. Mani: "ok"; upstream was 420 commits ahead at 9a6108fdf2.)
- **D-04 revised (2026-09-24):** `/goal` is a built-in Hermes command (`hermes_cli/commands.py:117`) and the host skips conflicting plugin commands (`hermes_cli/plugins.py:672`). Dr. Mani chose "a": slash command `/anansi goals add|flag|status|list`, mirroring CLI `hermes anansi goals ...`, one shared parser/handler. Supersedes the `/goal` name in D-04.
- **D-11 (stated default, not objected to):** drop `--files` from v1 (no storage column).
- **D-12 (stated default):** the 7-day measurement clock starts after the plan 06 merge and a Hermes restart.
- **D-13 (stated default):** reasoning effort for gpt-6-sol is decided after the plan 05 latency measurement.
- **D-14 (stated default):** delete local branch `001-close-known-gaps` only; leave `origin/001-close-known-gaps` (a remote delete is a push, not authorized).
- **D-15 (2026-09-24):** Dr. Mani: "push when convenient". Interpreted as: pushing this phase's merged work on `main` to `origin` (private repo `bionicbutterfly13/hermes-anansi-plugin`) is authorized at natural checkpoints (after each plan merge whose gate is green), and deleting `origin/001-close-known-gaps` after PR #2 is closed is authorized too. This supersedes the remote-branch part of D-14. It does not authorize any public or upstream push.
- **D-16 (2026-09-24):** Measurement window. Dr. Mani: "i'll use chief of staff a lot to make it work". Rule: the window ends at 7 days or 40 appraised turns, whichever comes later (option A as the stated safety floor; with heavy use it reduces to 7 days). Telemetry rates computed over fewer than 40 appraised turns are labeled low-confidence. Confirmed by Dr. Mani: "7 days or 40 messages", read as the rule he was shown: at least 7 days AND at least 40 appraised turns.
</decisions>

<canonical_refs>
- `.planning/phases/08.1-daily-use-v1/08.1-ADR.md`
- `.planning/grill-logs/2026-09-23-anansi-valuable-completion.md`
- `.planning/reference/CONSTITUTION.md`
- `.planning/phases/08-close-known-gaps/08-LIVE-SMOKE.md` (exit semantics 0/1/2; authorization now granted by D-07)
- Host (hermes-agent 0.21.4, `~/.hermes/hermes-agent` at `d3378a6a49`):
  `hermes_cli/plugins.py` (`register_command`, `register_cli_command`, `register_auxiliary_task`),
  `agent/plugin_llm.py` (`complete_structured(task=)`, `_check_task`),
  `agent/turn_context.py` (`_stamp_api_content_sidecar`, `compose_user_api_content`),
  `hermes_cli/codex_models.py:20` (`gpt-6-sol`).
</canonical_refs>

<specifics>
## Success Criteria

- `./scripts/test.sh` fully green after each plan; no weakened assertions.
- `scripts/live_drive_smoke.py` exit 0 on chief-of-staff, recorded; plan 02 records the first run and its latency
  baseline whatever the outcome.
- Seven days: p50 <= 6s; timeouts <= 10% of appraised turns; parse_fail + llm_error <= 5%; median block size reported.
- Day-7 decision rule: >= 50% useful and 0 misleading means proceed to Phase 9; < 25% useful means freeze; in between,
  the next increment is appraisal-prompt quality.

## Risk Summary

- gpt-6-sol account acceptance unverified (forward-compat template, `codex_models.py:43`).
- Up to 8.0s added per chief-of-staff turn; timeouts silently drop the block.
- Multimodal turns drop the block.
- Flagged priorities can exceed the ~500-token soft cap and replay every turn.
- PR #2 closure and branch deletion are outward-facing (authorized in D-02).
</specifics>

<code_context>
Integration baseline: main `355a7c9`. Phase 8 merged (199 tests). Uncommitted quick task 260912-cqc lives in
`.claude/worktrees/clarify-appraisal-timeout` (212 passed / 1 failed; the failure is caused by `_repo_root()`,
store.py:363-388). Goal writes exist only via `store.apply_deltas` (store.py:809-865), with no callers. `post_llm_call`
lacks an `enabled` check (reflection.record_turn, reflection.py:194-214). Render soft cap `_MAX_BLOCK_TOKENS = 500`
(render.py:25). Telemetry summary at store.py:986.
</code_context>

<deferred>
- Stable priorities/goals via `register_system_prompt_section` (after day-7 review).
- Reflection-proposed inert candidate goals with `/goal confirm` (after day-7 review).
- `/anansi purge-turns` (if the day-7 review finds unwanted excerpts).
- Reflection on host `on_session_finalize`.
- DONE 2026-09-24: hermes-agent fork merge `d3378a6a49` pushed to origin main (Dr. Mani: "yes push").
</deferred>

<scope_fence>
Out of scope: Phases 9-14 feature work; patching hermes-agent replay; any agent-invoked goal-writing tool; proactive
outreach; porting `531f390`.
</scope_fence>

<evidence_2026_09_24>
Telemetry cap vs. usage: `CAPS["telemetry"] = 2000` (store.py:43). chief-of-staff `state.db` user-message counts
(counts only, no content read) for the last 10 active days: 2, 4, 11, 2, 3, 6, 2, 2, 4, 17 (2026-09-13..09-24), which is
about 53 user turns. At >= 2 telemetry rows per turn, a 7-day window writes roughly 100-250 rows, so the 2000-row cap
is not a risk. The binding constraint is sample size: 7 days yields roughly 25-40 appraised turns, which is enough
for 20 review blocks but makes the rate thresholds noisy.
Dr. Mani (2026-09-24): "usually chief of staff more active- being fixed right now". The low 09-13..09-24 volume is
temporary; the chief-of-staff profile is under repair in another workstream.
Plan 02 precondition (stated default): install into chief-of-staff only after Dr. Mani confirms that repair is finished,
and after re-reading the profile's `config.yaml` and `plugins/` so the install does not conflict with the repair.
</evidence_2026_09_24>
