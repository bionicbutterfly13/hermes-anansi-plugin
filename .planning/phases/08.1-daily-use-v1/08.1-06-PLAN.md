---
phase: 08.1-daily-use-v1
plan: "06"
type: execute
wave: 5
depends_on:
  - "08.1-05"
files_modified:
  - anansi/__init__.py
  - anansi/tests/test_failopen_matrix.py
  - README.md
  - CHANGELOG.md
  - .planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md
  - .planning/phases/08.1-daily-use-v1/08.1-EXIT-GATE.md
autonomous: false
requirements:
  - REQ-08.1-08
  - REQ-08.1-07
  - REQ-08.1-06
  - REQ-08.1-05
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-08: with `enabled: false`, `post_llm_call` writes zero `turn_log` rows; with `enabled: true` the same call writes exactly one."
    - "D-08 invariant (assumption-delta test): with `enabled: false`, a full turn lifecycle (`on_session_start`, `pre_llm_call`, `post_llm_call`, `on_session_end`) makes zero LLM calls and writes zero `turn_log` rows; its only telemetry rows are `skipped:disabled` and `reflect_skipped:disabled`."
    - "README no longer states that the post-turn hook captures excerpts while `enabled` is false."
    - "D-15: the kill-switch branch is merged to main with `--no-ff`, main is pushed to origin after the green gate, and the plan worktree and branch are removed."
    - "D-06: on chief-of-staff, `auxiliary.anansi_appraisal.provider` is `openai-codex` and `auxiliary.anansi_appraisal.model` is `gpt-6-sol`, `auxiliary.anansi_reflection` is unset, and `plugins.entries.anansi.llm.model` is unset."
    - "D-06: every ok or trust_fallback telemetry row from the live activation turns reports a model beginning with `gpt-6-sol`, read from the telemetry `model` column; any other model is recorded as a D-06 verification failure and the clock does not start."
    - "D-13: the live gpt-6-sol latency sample (median wall_ms over the activation rows) and the resulting reasoning-effort decision are recorded before the clock starts."
    - "D-05, D-08 live: a rollback drill with `plugins.entries.anansi.enabled false` produced a `skipped:disabled` row and no new `turn_log` row on chief-of-staff, and the plugin was re-enabled."
    - "`hermes -p chief-of-staff anansi goals list` exits 0 on the installed profile (08.1-04 live check)."
    - "D-12: the measurement clock start (UTC, the format store writes) is recorded in `08.1-EXIT-GATE.md` only after the 08.1-06 merge, the activation config, and a restart of every long-lived chief-of-staff process, together with the debug-dump block count at that moment."
    - "D-07: at least one `live_drive_smoke.py` exit 0 on chief-of-staff is recorded, and the final-configuration run (`--task anansi_appraisal`) exit code is recorded whatever it is."
    - "D-16: the exit gate is evaluated only when the window since clock start covers at least 7 days AND at least 40 appraised turns (outcomes ok, trust_fallback, timeout, parse_fail, llm_error); rates over fewer than 40 appraised turns are labeled low-confidence."
    - "D-07: over the window, `telemetry_summary(since=clock_start)` gives p50_wall_ms <= 6000, timeouts <= 10% of appraised turns, parse_fail + llm_error <= 5% of appraised turns, and a reported median_block_tokens; the oldest telemetry row is not newer than clock start (no cap eviction inside the window)."
    - "D-07: Dr. Mani labels 20 debug-dump blocks sampled from after clock start as useful, neutral or misleading, and the decision rule is applied and recorded: >= 10 useful and 0 misleading proceeds to Phase 9; fewer than 5 useful freezes; otherwise the next increment is appraisal-prompt quality. Phases 9-14 stay gated until it is recorded."
  artifacts:
    - path: anansi/__init__.py
      provides: "post_llm_call returns before record_turn when enabled is false"
      contains: "get_cfg"
    - path: anansi/tests/test_failopen_matrix.py
      provides: "D-08 regression, enabled control, and the disabled full-lifecycle invariant, with matrix table rows"
    - path: .planning/phases/08.1-daily-use-v1/08.1-EXIT-GATE.md
      provides: "clock start, window rule, evaluation procedure, results, 20-block labels, decision"
      contains: "clock_start_utc"
    - path: .planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md
      provides: "activation run: config, smoke --task row, model check, latency sample, D-13 outcome, rollback drill"
      contains: "08.1-06"
  key_links:
    - from: anansi/__init__.py
      to: anansi/config.py
      via: "post_llm_call reads config.get_cfg()['enabled'] before reflection.record_turn"
      pattern: "enabled"
    - from: .planning/phases/08.1-daily-use-v1/08.1-EXIT-GATE.md
      to: anansi/store.py
      via: "evaluation calls store.telemetry_summary(db_path=<live>, since=<clock_start_utc>) read-only"
      pattern: "telemetry_summary"
  assumptions:
    - id: EDGE-REQ-08.1-07-unclassified
      status: flagged-unresolved
      statement: "The fallback edge probe classified REQ-08.1-07 as unclassified. Reviewed manually: window shorter than 7 days or 40 turns, cap eviction inside the window, and dump blocks from before clock start are handled by the truths above; no further edge behavior is inferred."
    - id: EDGE-REQ-08.1-08-unclassified
      status: flagged-unresolved
      statement: "The fallback edge probe classified REQ-08.1-08 as unclassified. Reviewed manually: config is cached per session and reset at on_session_start, so a live flip applies from the next session; tests pin get_cfg. No further edge behavior is inferred."
    - id: dump-env-propagation
      status: flagged-unresolved
      statement: "Research: an UPPER_SNAKE key set with `hermes config set` lands in the profile `.env`; that the long-lived chief-of-staff process exports it to `os.environ` is verified live in Task 2 by a block appearing in the dump file, not assumed."
  prohibitions:
    - requirement_id: REQ-08.1-08
      category: privacy
      statement: "MUST NOT capture or persist any user or assistant excerpt while `enabled` is false."
      status: unresolved
      verification: null
      flagged: true
    - requirement_id: REQ-08.1-07
      category: privacy
      statement: "MUST NOT copy chief-of-staff conversation text or rendered block contents into tracked repository files; the exit-gate record holds counts, rates, block ordinals and labels only."
      status: unresolved
      verification: null
      flagged: true
    - requirement_id: REQ-08.1-07
      category: transparency
      statement: "MUST NOT evaluate or record the D-07 decision on a window shorter than 7 days or 40 appraised turns without the low-confidence label, and MUST NOT fill in labels Dr. Mani did not give."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Make `enabled: false` a complete off switch, then activate the full v1 on chief-of-staff and hand the seven-day exit gate to Dr. Mani (D-08, D-06, D-12, D-13, D-07, D-16). The exit gate is where D-01 is settled: valuable completion means a v1 Dr. Mani uses daily, and the roadmap after it is decided from that real use.

Purpose: the kill switch must stop data collection, not only appraisal (D-08). Once every code plan is merged and the plugin is installed, the gpt-6-sol route, the debug dump, the rollback drill and the clock start happen in one live activation, and the phase closes on real use.
Output: the capture guard with a regression and an invariant test; a live activation record; `08.1-EXIT-GATE.md` with the clock start and evaluation procedure; the blocking exit-gate checkpoint.

Task 2 waits for 08.1-02 through a precondition, not a `depends_on` edge, so the kill-switch code (Task 1) lands even while the install waits on Dr. Mani.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/reference/CONSTITUTION.md
@.planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
@.planning/phases/08.1-daily-use-v1/08.1-ADR.md
@.planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
@.planning/phases/08.1-daily-use-v1/08.1-PATTERNS.md
@anansi/__init__.py
@anansi/tests/test_failopen_matrix.py
</context>

<assumption_delta_decision>
Detector: `assumption-delta scan 08.1` fired `pluralization` on "also" in "`enabled: false` also stops turn capture".
- Primary noun: the plugin-level `enabled` key (`plugins.entries.anansi.enabled`) as the single master switch for every Anansi effect: appraisal, reflection and turn capture.
- Decision: `no-change`. `enabled` already is the master switch for appraisal and reflection; this plan closes the one path that bypassed it (`post_llm_call` capture). No second switch is added alongside it; `drive_enabled` and `reflection_enabled` stay narrower sub-switches beneath it.
- Invariant test (accepted): with `enabled: false`, a full turn lifecycle makes zero LLM calls and writes zero `turn_log` rows (Task 1 behavior). It goes red if a future hook path forgets the master switch.
</assumption_delta_decision>

## Artifacts this phase produces

- `anansi.post_llm_call`: returns `None` before `reflection.record_turn` when `config.get_cfg().get("enabled", True)` is false (lazy `config` import inside the function; no telemetry row, because `pre_llm_call` already records `skipped:disabled` for the same turn).
- Fail-open matrix rows "kill switch: post_llm_call capture" and "kill switch: full lifecycle invariant" with their tests in `anansi/tests/test_failopen_matrix.py`.
- `.planning/phases/08.1-daily-use-v1/08.1-EXIT-GATE.md` (new): `clock_start_utc`, `dump_blocks_at_clock_start`, window rule, evaluation procedure, results, labels, decision.
- `08.1-LIVE-SMOKE.md` additions: `## Activation (08.1-06)` and the line `- smoke 08.1-06 exit: <0|1|2>`.
- Operator state outside the repo: chief-of-staff `auxiliary.anansi_appraisal.provider = openai-codex`, `auxiliary.anansi_appraisal.model = gpt-6-sol`; `ANANSI_DEBUG_DUMP` in the profile `.env` pointing at `$PROFILE_HOME/anansi/debug-dump.log`.

<integration_protocol>
- Branch `fix/08.1-06-kill-switch-capture`, sibling worktree `/Volumes/Asylum/repos/hermes-anansi-plugin-08.1-06-kill-switch-capture`, created from the main checkout with `git worktree add -b fix/08.1-06-kill-switch-capture ../hermes-anansi-plugin-08.1-06-kill-switch-capture main` after `git fetch origin` shows `main` is not behind `origin/main`. The main checkout stays on `main`.
- If `.devtools/pytest` is missing in the worktree, copy the main checkout's gitignored `.devtools/` into it, or let `./scripts/test.sh` stage it.
- Before every full-suite run, confirm `$HOME/.hermes/hermes-agent/.git/MERGE_HEAD` does not exist; wait if a host re-sync is in progress.
- Never rebase, squash, force-push or pass `--no-verify`.
- Integration: full suite green in the worktree; from the main checkout `git merge --no-ff fix/08.1-06-kill-switch-capture -m "Merge 08.1-06 complete kill switch"`; full suite green on main; `git push origin main` (D-15); `graphify update .`; re-audit worktree list, status and ancestry; `git worktree remove ../hermes-anansi-plugin-08.1-06-kill-switch-capture`; `git branch -d fix/08.1-06-kill-switch-capture`. On a merge conflict: abort and report.
- `$PROFILE_HOME` always means `dirname "$(hermes -p chief-of-staff config path)"`, resolved at run time.
</integration_protocol>

<tasks>

<task id="08.1-06-T1" type="tracer" tdd="true">
  <name>Task 1: `enabled: false` stops turn capture, proven from config to the turn_log table, merged and pushed</name>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/phases/08.1-daily-use-v1/08.1-PATTERNS.md
    anansi/__init__.py
    anansi/config.py
    anansi/reflection.py
    anansi/tests/test_failopen_matrix.py
    anansi/tests/test_pre_llm_call.py
    README.md
    CHANGELOG.md
  </read_first>
  <files>anansi/__init__.py, anansi/tests/test_failopen_matrix.py, README.md, CHANGELOG.md</files>
  <behavior>
    - With `pinned_env.state["cfg"] = _cfg(enabled=False)`, `anansi.post_llm_call(session_id="s1", turn_id="t1", user_message="msg", assistant_response="resp")` returns None and a read-only count of `turn_log` is 0.
    - The same call with `enabled=True` returns None and the count is 1.
    - With `enabled=False`, calling `on_session_start`, `pre_llm_call`, `post_llm_call` and `on_session_end` for one session leaves the counting caller at 0 calls, `turn_log` at 0 rows, and telemetry outcomes limited to `skipped:disabled` and `reflect_skipped:disabled`.
  </behavior>
  <action>
Set up the branch and worktree per the integration protocol.

Tests first in `anansi/tests/test_failopen_matrix.py`, modeled on the existing `post_llm_call` locked-DB test (read-only `turn_log` count) and using the `pinned_env` fixture (config is cached per session and reset only in `on_session_start`, so tests must use the pinned `get_cfg`, never the real cache). Add the three behavior-list tests and two rows to the module docstring table: "kill switch: post_llm_call capture (D-08)" and "kill switch: full lifecycle invariant (D-08)".

`anansi/__init__.py` (D-08). In `post_llm_call`, after the debug log, lazily import `config` alongside `reflection` and return `None` when `config.get_cfg().get("enabled", True)` is false, before `reflection.record_turn`. Write no telemetry row there: `pre_llm_call` already records `skipped:disabled` for the same turn, and a second row would double-count disabled turns in `telemetry_summary`. Update the docstring to say capture honors the kill switch. The `_fail_open` wrapper stays.

`README.md`: replace the paragraph saying `enabled: false` suppresses appraisal and reflection while the post-turn hook still captures excerpts with: `enabled: false` stops appraisal, reflection and post-turn capture; the setting is read per session, so it applies from the next session start; `ANANSI_DEBUG_DUMP` still writes rendered blocks when set and enabled. `CHANGELOG.md`: dated entry with Fixes (root cause: `post_llm_call` called `record_turn` without consulting `enabled`, so the documented kill switch kept collecting excerpts) and Learnings (a kill switch is a claim about every write path; test the whole lifecycle, not one hook).

Run the targeted file and the full suite in the worktree, commit `fix(capture): stop turn capture when anansi is disabled`, and integrate per the integration protocol.
  </action>
  <verify>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && ./scripts/test.sh anansi/tests/test_failopen_matrix.py anansi/tests/test_reflection.py anansi/tests/test_pre_llm_call.py -x</automated>
    <fails_when>non-zero exit, a final summary line containing "failed" or "error", or "no tests ran"</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && ./scripts/test.sh</automated>
    <fails_when>non-zero exit, or the final summary line reports any failed or error count</fails_when>
    <automated>cd /Volumes/Asylum/repos/hermes-anansi-plugin && git fetch origin && git rev-list --count origin/main..main && git branch --list fix/08.1-06-kill-switch-capture</automated>
    <fails_when>the count is not 0, or the branch name is printed</fails_when>
  </verify>
  <acceptance_criteria>
    - In `anansi/__init__.py`, within `post_llm_call`, the line containing `get_cfg()` precedes the line containing `reflection.record_turn(`.
    - The fail-open matrix docstring contains rows beginning `| kill switch: post_llm_call capture (D-08)` and `| kill switch: full lifecycle invariant (D-08)`.
    - The three behavior-list tests pass, and the enabled control test asserts exactly 1 row.
    - `grep -n "stops appraisal, reflection and post-turn capture" README.md` prints one line (the rewritten kill-switch paragraph).
    - `git -C /Volumes/Asylum/repos/hermes-anansi-plugin log -1 --merges --format=%s main` contains `08.1-06`; the worktree and branch are gone.
  </acceptance_criteria>
  <done>The kill switch stops capture, the whole disabled lifecycle is pinned by a test, and the change is on main and origin.</done>
</task>

<task id="08.1-06-T2" type="auto">
  <name>Task 2: Live activation on chief-of-staff: gpt-6-sol route, rollback drill, latency, and clock start</name>
  <precondition>08.1-02 is complete: `.planning/phases/08.1-daily-use-v1/08.1-02-SUMMARY.md` exists and `readlink "$(dirname "$(hermes -p chief-of-staff config path)")/plugins/anansi"` prints `/Volumes/Asylum/repos/hermes-anansi-plugin/anansi`.</precondition>
  <read_first>
    .planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
    .planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md
    .planning/phases/08.1-daily-use-v1/08.1-02-SUMMARY.md
    .planning/phases/08.1-daily-use-v1/08.1-RESEARCH.md
    README.md
    scripts/live_drive_smoke.py
    anansi/store.py
    ~/.claude/skills/launchd-agent-exit-78-external-volume-logs/SKILL.md
  </read_first>
  <files>.planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md, .planning/phases/08.1-daily-use-v1/08.1-EXIT-GATE.md</files>
  <action>
Unmet precondition: check both precondition facts before Step A. If `08.1-02-SUMMARY.md` is missing or the `readlink` output differs, Task 2 becomes a blocking wait after the Task 1 merge. Run none of Steps A-H and write nothing to the chief-of-staff profile. Leave Task 1 merged and pushed, do not start Task 3, and do not write `08.1-06-SUMMARY.md`. Report the plan as paused, quoting the failing check's output and naming the resume condition (08.1-02 complete, both checks passing). Resume at Step A when both checks pass. Never create the symlink or the 08.1-02 summary from this plan to satisfy the check; 08.1-02 owns the install and its Dr. Mani confirmation.

All steps run from the main checkout on `main` (which now holds every 08.1 code plan). D-07 authorizes these live runs; each is recorded under `## Activation (08.1-06)` in `08.1-LIVE-SMOKE.md` exactly as observed.

Step A, precedence check (D-06, RESEARCH Plan 05). Read `$PROFILE_HOME/config.yaml` with the hermes venv python and `yaml.safe_load`, printing only whether `plugins.entries.anansi.llm.model` and any `plugins.entries.anansi.llm.allow_*` key are present, never a value. If any is present, stop and ask Dr. Mani before changing it.

Step B, profile config (D-06). `hermes -p chief-of-staff config set auxiliary.anansi_appraisal.provider openai-codex` and `hermes -p chief-of-staff config set auxiliary.anansi_appraisal.model gpt-6-sol`. Leave `auxiliary.anansi_reflection` unset (auto, main model). Set no reasoning effort yet (D-13). Set the review dump (D-07): `hermes -p chief-of-staff config set ANANSI_DEBUG_DUMP "$PROFILE_HOME/anansi/debug-dump.log"` and confirm with `grep -c '^ANANSI_DEBUG_DUMP=' "$PROFILE_HOME/.env"` (a count only; never print the file).

Step C, rollback drill (D-05, D-08 live). Record the `turn_log` row count through a read-only URI. `hermes -p chief-of-staff config set plugins.entries.anansi.enabled false`; run one one-shot turn `hermes -p chief-of-staff -z "Anansi rollback drill: reply with the word ok."`; confirm the newest telemetry row is `skipped:disabled` and the `turn_log` count is unchanged; then `hermes -p chief-of-staff config set plugins.entries.anansi.enabled true`. Record both counts.

Step D, restart (D-12). List chief-of-staff processes with `ps -axo pid,lstart,command` filtered to `-p chief-of-staff`. Restart any running gateway (`hermes -p chief-of-staff gateway restart`, or the stop-then-start sequence from the launchd skill if it does not return in 60 seconds); never start a gateway that was not running. Ask Dr. Mani to restart any interactive session or desktop app that started before the 08.1-06 merge; do not kill them. Continue only when no chief-of-staff process older than the Step B config change remains.

Step E, aux-route smoke (D-07). `HERMES_HOME="$PROFILE_HOME" "$HOME/.hermes/hermes-agent/venv/bin/python" scripts/live_drive_smoke.py --task anansi_appraisal --probe-host chatgpt.com; echo "exit=$?"`. Add a `## Smoke runs` row for 08.1-06 with the printed exit, outcome, wall_ms, model and error, and the line `- smoke 08.1-06 exit: <code>`.

Step F, activation turns, model check and latency (D-06, D-13). Run five one-shot turns `hermes -p chief-of-staff -z "Anansi activation check <n> of 5: in one sentence, name one thing that makes a reminder useful."` (no tools requested). Read the resulting telemetry rows read-only (`ts, outcome, wall_ms, model, block_tokens, error`) and record them. Confirm the dump file gained blocks whose first line starts with `[anansi appraisal] turn`. Model check: every ok or trust_fallback row's `model` must begin with `gpt-6-sol`; if any does not, or every row failed, stop and present the rows to Dr. Mani as a decision (he chooses whether to keep gpt-6-sol and investigate account acceptance or to name another model); do not start the clock. D-13: compute the median `wall_ms` over the ok and trust_fallback rows from Steps E and F; if it is at most 6000 ms and no row timed out, record "D-13: host default reasoning effort kept; no reasoning_effort key set; median <X> ms over <n> rows"; otherwise stop and present the numbers to Dr. Mani with two options, (a) `hermes -p chief-of-staff config set auxiliary.anansi_appraisal.reasoning_effort low` then repeat Steps D-F, or (b) keep the default and start the clock, and record his choice.

Step G, live goal CLI (08.1-04). `hermes -p chief-of-staff anansi goals list` must exit 0; record the exit code and the goal count only.

Step H, clock start (D-12, D-16). Create `08.1-EXIT-GATE.md` with: `clock_start_utc` set to the current UTC time in the ISO format `store._utc_now_iso()` writes (taken after Steps A-G pass); `dump_blocks_at_clock_start` = the number of lines in the dump file that start with `[anansi appraisal]`; the window rule (at least 7 days AND at least 40 appraised turns; rates below 40 labeled low-confidence); appraised turns defined as the sum of `by_outcome` for ok, trust_fallback, timeout, parse_fail and llm_error, with `skipped:worker_busy` reported separately; the D-07 thresholds; the evaluation command (hermes venv python with the main checkout on `sys.path`, `HERMES_HOME="$PROFILE_HOME"`, calling `anansi.store.telemetry_summary(since=<clock_start_utc>)`, read-only); the oldest-row eviction check; the sampling rule (20 blocks drawn uniformly without replacement from blocks after `dump_blocks_at_clock_start`, with the random seed recorded); the decision rule; empty Results, Labels and Decision sections marked PENDING; and a privacy note that only counts, rates, block ordinals and labels are recorded.
  </action>
  <verify>
    <automated>grep -E "^- smoke 08\.1-06 exit: [012]$" /Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md</automated>
    <fails_when>no matching line (grep exit 1)</fails_when>
    <automated>hermes -p chief-of-staff anansi goals list</automated>
    <fails_when>non-zero exit, or output containing "invalid choice"</fails_when>
    <automated>grep -E "^clock_start_utc: 20[0-9]{2}-[0-9]{2}-[0-9]{2}T" /Volumes/Asylum/repos/hermes-anansi-plugin/.planning/phases/08.1-daily-use-v1/08.1-EXIT-GATE.md</automated>
    <fails_when>no matching line (grep exit 1)</fails_when>
  </verify>
  <acceptance_criteria>
    - `08.1-LIVE-SMOKE.md` `## Activation (08.1-06)` records: the precedence-check result (presence flags only), both `auxiliary.anansi_appraisal` values, the rollback-drill counts showing no new `turn_log` row, the process list with restart outcomes, the smoke row, five activation telemetry rows with their `model` values, and the D-13 line or Dr. Mani's recorded choice.
    - Every ok or trust_fallback activation row's `model` begins with `gpt-6-sol`, or the record states the D-06 failure and `08.1-EXIT-GATE.md` has no clock start.
    - `08.1-EXIT-GATE.md` contains `clock_start_utc:`, `dump_blocks_at_clock_start:`, the 7-day AND 40-turn rule, the evaluation command, and PENDING Results, Labels and Decision sections.
    - No conversation text or block text appears in either record.
  </acceptance_criteria>
  <done>Chief-of-staff runs the full v1 on gpt-6-sol with a proven rollback, the latency decision is recorded, and the measurement clock is running.</done>
</task>

<task id="08.1-06-T3" type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: v1 exit gate after the measurement window (D-07, D-16)</name>
  <read_first>
    .planning/phases/08.1-daily-use-v1/08.1-EXIT-GATE.md
    .planning/phases/08.1-daily-use-v1/08.1-LIVE-SMOKE.md
    .planning/phases/08.1-daily-use-v1/08.1-CONTEXT.md
  </read_first>
  <action>Evaluate the v1 exit gate once the window rule is met, then record the decision that gates Phases 9-14.</action>
  <what-built>Anansi v1 on chief-of-staff: turn-marked blocks with block-size telemetry (08.1-03), `/anansi goals` and `hermes anansi goals` (08.1-04), appraisal on gpt-6-sol through `anansi_appraisal` (08.1-05, activated in Task 2), and a complete kill switch (Task 1). The clock start and evaluation procedure are in `08.1-EXIT-GATE.md`.</what-built>
  <how-to-verify>
    On resume, Claude does the automatable part first, read-only:
    1. Compute days since `clock_start_utc` and call `anansi.store.telemetry_summary(since=<clock_start_utc>)` against the chief-of-staff DB. If fewer than 7 days or fewer than 40 appraised turns have elapsed, report both numbers and stop; the window continues (D-16).
    2. Report p50_wall_ms (target <= 6000), timeout share of appraised turns (target <= 10%), parse_fail + llm_error share (target <= 5%), median_block_tokens, and the `skipped:worker_busy` count; confirm the oldest telemetry row is not newer than the clock start, else label the window incomplete.
    3. Confirm `08.1-LIVE-SMOKE.md` records at least one exit 0 on chief-of-staff and the final-configuration run's exit code.
    4. Draw 20 blocks from the debug dump after `dump_blocks_at_clock_start` with the recorded seed and show them to Dr. Mani as a numbered list in chat (never written to the repository).
    Dr. Mani then labels each block useful, neutral or misleading.
    5. Apply the decision rule: at least 10 useful and 0 misleading means proceed to Phase 9; fewer than 5 useful means freeze; anything else means the next increment is appraisal-prompt quality. Write results, labels (block ordinal plus label only) and the decision into `08.1-EXIT-GATE.md`.
  </how-to-verify>
  <resume-signal>Type the 20 labels in order (for example: U N U M ...), or "not yet" to keep the window open</resume-signal>
  <acceptance_criteria>
    - `08.1-EXIT-GATE.md` Results section holds the window length, appraised-turn count, the four D-07 figures, and a low-confidence label if fewer than 40 appraised turns were available.
    - The Labels section holds exactly 20 ordinal-and-label entries given by Dr. Mani and no block text.
    - The Decision section names one of: proceed to Phase 9, freeze, or appraisal-prompt quality, and cites the counts that produced it.
  </acceptance_criteria>
</task>

</tasks>

<constitution_check>
- I Fail-open: the capture guard is a read of the cached config inside the existing `_fail_open` hook; a config failure still resolves through `get_cfg`'s never-raise contract.
- II No autonomy: unchanged; the kill switch only removes behavior.
- III Never-omit: rendering untouched; the live smoke's PASS criterion remains the flagged drive want.
- IV Single SQLite surface: live evaluation reads through `store.telemetry_summary` over a read-only URI.
- V Paths and values from config: the profile path is derived from `hermes config path`; the model choice is profile config, not code.
- VI Inspectable drive: the day-7 review labels real blocks.
- VII Minimal change and verify before ship: one guard; live activation proves rollback, model and latency before the clock starts.
</constitution_check>

<source_coverage>
D-06 (live config and model check), D-07, D-08, D-12, D-13, D-15, D-16, REQ-08.1-05 (live rollback and CLI check), REQ-08.1-06 (live), REQ-08.1-07, REQ-08.1-08, RESEARCH Plan 06 and Pitfalls 7, 9, the assumption-delta checkpoint. Deferred and excluded: `/anansi purge-turns`, reflection on `on_session_finalize`, system-prompt placement of priorities.
</source_coverage>

<verification>
Task 1: full `./scripts/test.sh` green on main and `git rev-list --count origin/main..main` prints 0. Task 2: `08.1-LIVE-SMOKE.md` carries `- smoke 08.1-06 exit: N`, `hermes -p chief-of-staff anansi goals list` exits 0, and `08.1-EXIT-GATE.md` carries `clock_start_utc`. Task 3: human evaluation recorded per D-07 and D-16; this is phase-level verification and is not satisfied by any automated check.
</verification>

<success_criteria>
`enabled: false` stops every Anansi effect including capture; chief-of-staff runs the full v1 with appraisal on gpt-6-sol; after at least 7 days and 40 appraised turns, the telemetry thresholds and Dr. Mani's 20 labels produce a recorded decision for Phases 9-14.
</success_criteria>

<output>
Create `.planning/phases/08.1-daily-use-v1/08.1-06-SUMMARY.md` when Tasks 1 and 2 are done (the window then runs); the exit-gate outcome is recorded in `08.1-EXIT-GATE.md` at resume.
</output>
