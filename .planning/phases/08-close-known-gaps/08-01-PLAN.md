---
phase: 08-close-known-gaps
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - anansi/store.py
  - anansi/config.py
  - anansi/render.py
  - anansi/__init__.py
  - anansi/tests/test_drive_config.py
  - anansi/tests/test_drive_store.py
  - anansi/tests/test_reflection_store.py
autonomous: true
requirements:
  - REQ-001-close-known-gaps-fr-001
  - REQ-001-close-known-gaps-fr-002
  - REQ-001-close-known-gaps-fr-003
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "US-1/SC-002 and probe FR-001 boundary: support_style=firm, push_when_stalled=1, and stall_threshold_days=3 round-trip unchanged; legacy rows read NULL, 0, NULL."
    - "US-1 scenario 2: after a fresh snapshot, persisted support_style and push_when_stalled authorize the per-goal push/under-support effect only when persisted stall_threshold_days is reached; a below-threshold control does not render that effect."
    - "Probe FR-001 precision: the three pressure fields are persisted as their SQLite text/integer values without rounding or lossy conversion."
    - "Probe FR-001 idempotency: repeated ensure_db calls on a migrated v5 database do not add duplicate columns or change goal rows."
    - "Probe FR-001 concurrency: a locked, structurally sound v4 database returns unavailable without quarantine, sidecar movement, partial migration, or a raised exception."
    - "US-2/SC-003: quiet, standard, and firm produce distinguishable drive effects from identical input; standard preserves the pre-phase output contract and invalid input coerces to standard."
    - "Actual corrupt state follows the existing fail-open recovery contract; lock unavailability never masquerades as corruption."
  artifacts:
    - path: anansi/store.py
      provides: "schema v5, additive v4 migration, pressure-field goal writes"
    - path: anansi/config.py
      provides: "existing drive_pressure coercion consumed by the hook"
    - path: anansi/render.py
      provides: "pressure-aware render_block behavior"
    - path: anansi/tests/test_drive_store.py
      provides: "round-trip, migration, idempotency, lock, and corruption regressions"
    - path: anansi/tests/test_reflection_store.py
      provides: "the affected shared-store current-schema assertion updated for schema v5"
  key_links:
    - from: anansi/store.py
      to: anansi/render.py
      via: "read_snapshot goals carry persisted support_style, push_when_stalled, stall_threshold_days, and read-time momentum into per-goal push/under-support rendering"
    - from: anansi/config.py
      to: anansi/__init__.py
      via: "get_cfg()['drive_pressure'] is passed only on the drive-on render path"
    - from: anansi/__init__.py
      to: anansi/render.py
      via: "pre_llm_call calls render_block(..., pressure=effective_pressure)"
  assumptions:
    - id: EDGE-FR002-unclassified
      status: flagged-unresolved
      statement: "The fallback probe classified FR-002 as unclassified; the source contract supplies migration preservation, legacy defaults, lock, and corruption cases, but no additional unnamed behavior is inferred."
  prohibitions:
    - statement: "A transient lock on a structurally sound v4 database must not quarantine or erase user goal state."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Prove the Phase 8 architecture with one real path from persisted per-goal pressure and global configuration through the hook to rendered drive output, then make the v4-to-v5 migration additive and lock-safe.

Purpose: Close US-1 and US-2 without importing the historical branch wholesale or weakening fail-open state handling.
Output: Schema v5 pressure persistence, pressure-aware rendering, and migration regressions.
</objective>

<execution_context>
@/Users/manisaintvictor/.codex/gsd-core/workflows/execute-plan.md
@/Users/manisaintvictor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/INGEST-CONFLICTS.md
@.planning/phases/08-close-known-gaps/08-CONTEXT.md
@.planning/phases/08-close-known-gaps/08-RESEARCH.md
@.planning/phases/08-close-known-gaps/08-PATTERNS.md
@.planning/reference/CONSTITUTION.md
@.planning/reference/001-close-known-gaps/contracts/store-goals-schema.md
@.planning/reference/001-close-known-gaps/contracts/config-keys.md
</context>

## Artifacts this phase produces

- `anansi.store.SCHEMA_VERSION = 5`
- `anansi.store._GOALS_V5_ADDED_COLUMNS`
- `anansi.store._try_upgrade_v4_to_v5(path)` with `True` = upgraded/current, `None` = unavailable, `False` = invalid
- `Goal.support_style`, `Goal.push_when_stalled`, and `Goal.stall_threshold_days` SQLite columns
- `anansi.render.render_block(..., pressure=None)`

<tasks>

<task id="08-01-T1" type="tracer" tdd="true">
  <name>Task 1: Migrate safely and carry one persisted pressure setting through the hook</name>
  <reversibility rating="costly">Existing state databases adopt additive schema v5; rollback is possible but requires a tested reverse data migration rather than a code-only revert.</reversibility>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/INGEST-CONFLICTS.md
    .planning/reference/001-close-known-gaps/contracts/store-goals-schema.md
    .planning/reference/001-close-known-gaps/contracts/config-keys.md
    anansi/store.py
    anansi/config.py
    anansi/render.py
    anansi/__init__.py
    anansi/tests/test_drive_store.py
    anansi/tests/test_reflection_store.py
  </read_first>
  <files>anansi/store.py, anansi/render.py, anansi/__init__.py, anansi/tests/test_drive_store.py, anansi/tests/test_reflection_store.py</files>
  <behavior>
    - A fresh v5 database stores and reloads firm, true, and 3 for one active goal.
    - A genuine populated v4 database upgrades in place and retains ids, texts, statuses, flags, domains, and timestamps with null/false pressure defaults.
    - A held write lock leaves the database and quarantine set unchanged; corruption still follows existing fail-open recovery.
    - After a fresh snapshot, a persisted firm/true goal stalled at or beyond its persisted threshold renders the push-zone and under-support effects; the same goal below its threshold does not.
    - Every assertion describing the current schema reports v5, while fixtures deliberately constructing historical v4 state remain v4.
    - A migrated persisted goal reaches pre_llm_call and render_block through the pressure-aware interface; global standard keeps the existing drive output contract.
  </behavior>
  <action>Start with failing v4 migration, lock, fresh round-trip, persisted-goal-to-render threshold, and one full-hook standard-pressure regression in `test_drive_store.py`. For the render regression, persist two active flagged goals through `apply_deltas`, both with `support_style=firm` and `push_when_stalled=1`, but with thresholds 3 and 5. Use direct SQLite only as test instrumentation to give both rows the same four-days-old `updated_at`, then reload through a fresh `read_snapshot` and exercise `pre_llm_call` with deterministic matching fake-model signals against the temporary database. The threshold-3 goal must render the existing push-zone and under-support effects; the threshold-5 control must retain the same neutral stalled read without either effect. Implement the schema bump and safe migration in this same task so no commit can advertise v5 before the migration path exists. Add exactly `support_style TEXT`, `push_when_stalled INTEGER NOT NULL DEFAULT 0`, and `stall_threshold_days INTEGER` to fresh goals DDL and both goal write paths. Add `_GOALS_V5_ADDED_COLUMNS` and tri-state `_try_upgrade_v4_to_v5(path)`: validate quick_check, required tables, and version; add missing columns plus the meta version update in one transaction; return `None` for transient open/lock unavailability and `False` only for structural invalidity or unsupported versions. Make `ensure_db` return unavailable without quarantine for `None`, while preserving the established recovery path for `False`. In `enrich_goal_signals`, replace model-provided pressure metadata with all three values from the matched persisted goal, including legacy `NULL`, `0`, `NULL`. Apply `stall_threshold_days` to both drive-note push-zone and drive-want under-support decisions: a positive integer threshold must be reached before the user-authorized `push_when_stalled` or firm/push support style takes effect; absent or invalid thresholds retain the current any-stalled behavior for legacy rows. Extend `render_block` with `pressure=None` and pass the existing configured value from `pre_llm_call` only on the drive-on path, keeping global standard byte-compatible and separate from per-goal authorization. Update every affected current-schema surface: the current-schema test name, module wording, literal version assertion, meta-row assertion, and recreated-current-schema assertion in `test_drive_store.py`, plus the literal current-version assertion/comment in `test_reflection_store.py`. Preserve `4` in fixtures and assertions whose purpose is to construct or verify historical v4 input for the additive migration, and preserve historical v3 fixtures. Reuse selected concepts visible through `git show af2a0bc:...`; do not merge or cherry-pick that branch.</action>
  <verify>
    <automated>./scripts/test.sh anansi/tests/test_drive_store.py anansi/tests/test_reflection_store.py -x</automated>
    <fails_when>non-zero exit, no collected tests, any v4 row changes, a lock produces quarantine/schema mutation, persisted pressure or threshold does not change the per-goal push/under-support result after reload, a current-schema assertion still expects v4, a historical v4 fixture is rewritten as v5, or the global standard full-hook path changes established output</fails_when>
  </verify>
  <acceptance_criteria>
    - `store.SCHEMA_VERSION == 5` and a fresh goals table exposes all three new columns.
    - A fresh snapshot reproduces `firm`, `1`, and `3` exactly after a separate read connection.
    - A populated v4 fixture becomes v5 with every existing row intact and legacy defaults `None`, `0`, `None`.
    - A lock returns unavailable within the existing bound with no quarantine artifact; corrupt state still follows current recovery.
    - A reloaded firm/true/threshold-3 goal renders push-zone and under-support at or above day 3 but not below day 3; the values come from the persisted goal rather than model output or the global pressure setting.
    - Current-schema assertions in `test_drive_store.py` and `test_reflection_store.py` expect v5; historical v4 migration fixtures still create and assert v4 before upgrade.
    - The migrated persisted state reaches a byte-compatible global-standard hook result.
  </acceptance_criteria>
  <done>The v5 migration and one safe persisted-goal to hook to renderer slice land atomically with focused green evidence.</done>
</task>

<task id="08-01-T2" type="auto" tdd="true">
  <name>Task 2: Expand quiet, standard, and firm pressure behavior</name>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/phases/08-close-known-gaps/08-RESEARCH.md
    .planning/reference/001-close-known-gaps/contracts/config-keys.md
    anansi/config.py
    anansi/render.py
    anansi/__init__.py
    anansi/tests/test_drive_config.py
  </read_first>
  <files>anansi/config.py, anansi/render.py, anansi/__init__.py, anansi/tests/test_drive_config.py</files>
  <behavior>
    - Identical state produces observably different quiet, standard, and firm drive effects.
    - Standard remains byte-compatible with the pre-phase drive output contract.
    - Invalid pressure input coerces to standard without raising.
    - Drive-off emits no drive fields while preserving the ordinary appraisal block byte-for-byte against a no-goals control.
  </behavior>
  <action>Write table-driven full-hook tests for quiet, standard, firm, invalid pressure, and drive-off before expanding the tracer's standard behavior. Let quiet reduce and firm increase only the existing non-flagged note limit and inspectable salience bonus within the top-3 and energy-budget ceilings. Keep global drive pressure separate from persisted per-goal support authorization and do not overwrite the persisted support style, push flag, or stall threshold established in Task 1. Preserve literal want text, neutral stalled wording, kill switches, actual-failure empty injection, and the ordinary appraisal block when drive is off. Add no configuration key or dependency.</action>
  <verify>
    <automated>./scripts/test.sh anansi/tests/test_drive_config.py -x</automated>
    <fails_when>non-zero exit, no collected tests, standard output changes, invalid input raises, or drive-off suppresses ordinary appraisal output</fails_when>
  </verify>
  <acceptance_criteria>
    - Quiet, standard, and firm are distinguishable for identical state within existing output ceilings.
    - Standard exactly retains established drive text and ordering.
    - Invalid input uses standard behavior and records no exception.
    - Drive-off contains no drive fields and equals the same valid appraisal with no goals.
  </acceptance_criteria>
  <done>US-1, US-2, SC-002, and SC-003 have atomic migration plus complete pressure-policy evidence.</done>
</task>

</tasks>

<constitution_check>
Principles I, III, IV, V, VI, and VII are explicit in the migration, hook wiring, zero-dependency constraint, and focused tests. Principle II remains unchanged because pressure only changes observational salience and note count.
</constitution_check>

<verification>
Run `./scripts/test.sh anansi/tests/test_drive_store.py anansi/tests/test_reflection_store.py anansi/tests/test_drive_config.py -x`; a non-zero exit, zero collected tests, per-goal threshold regression, current-schema fixture mismatch, or global-standard output regression blocks the plan.
</verification>

<success_criteria>
US-1, US-2, SC-002, and SC-003 pass against fresh and migrated temporary databases without provider access.
</success_criteria>

<output>
Create `.planning/phases/08-close-known-gaps/08-01-SUMMARY.md` when done.
</output>
