---
phase: 08-close-known-gaps
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - anansi/config.py
  - anansi/__init__.py
  - anansi/store.py
  - anansi/tests/test_drive_config.py
  - anansi/tests/test_failopen_matrix.py
  - anansi/tests/test_telemetry_store.py
autonomous: true
requirements:
  - REQ-001-close-known-gaps-fr-004
  - REQ-001-close-known-gaps-fr-005
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "US-3/SC-004: each malformed provided config key produces exactly one config_degraded row on the session reload; valid and normalization-equivalent values produce none."
    - "Rejected config telemetry contains the key, a type/length shape, and the applied default, never the rejected literal."
    - "Probe FR-003 idempotency: repeated reads of the same cached effective config return the same values and do not duplicate degradation rows within one load cycle."
    - "config_degraded is inspectable but excluded from failure_count and last_error selection."
    - "A locked or unavailable telemetry store cannot raise, block, or change the subsequent hook output."
  artifacts:
    - path: anansi/config.py
      provides: "secret-safe degradation descriptors for the latest config load"
    - path: anansi/__init__.py
      provides: "once-per-session reload telemetry emission"
    - path: anansi/store.py
      provides: "config_degraded non-failure telemetry vocabulary"
    - path: anansi/tests/test_failopen_matrix.py
      provides: "full-hook unavailable-telemetry regression"
  key_links:
    - from: anansi/config.py
      to: anansi/__init__.py
      via: "get_cfg reload stores shape-only degradation descriptors consumed by on_session_start"
    - from: anansi/__init__.py
      to: anansi/store.py
      via: "record_telemetry('config_degraded', error=shape_only_message) in a nested fail-open boundary"
  assumptions:
    - id: EDGE-FR003-concurrency
      status: flagged-unresolved
      statement: "Concurrent cache reload ordering is not specified by the source; Phase 8 preserves the current process-local cache rather than inventing a synchronization contract."
    - id: EDGE-FR004-unclassified
      status: flagged-unresolved
      statement: "The fallback probe classified FR-004 as unclassified; the plan implements the source's one-row-per-degraded-key contract and does not infer another event stream."
    - id: EDGE-FR005-unclassified
      status: flagged-unresolved
      statement: "The fallback probe classified FR-005 as unclassified; the plan covers redaction and unavailable storage without inventing provider or credential handling."
  prohibitions:
    - statement: "Configuration telemetry must not persist or print the literal rejected value, including credential-shaped input."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Make every config coercion inspectable through one secret-safe, non-failure telemetry event while keeping configuration loading and hook execution fail-open.

Purpose: Close US-3 without coupling `config.py` to storage or turning diagnostic loss into turn failure.
Output: Degradation descriptors, hook emission, telemetry classification, and failure-path tests.
</objective>

<execution_context>
@/Users/manisaintvictor/.codex/gsd-core/workflows/execute-plan.md
@/Users/manisaintvictor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-close-known-gaps/08-CONTEXT.md
@.planning/phases/08-close-known-gaps/08-RESEARCH.md
@.planning/phases/08-close-known-gaps/08-PATTERNS.md
@.planning/reference/CONSTITUTION.md
@.planning/reference/001-close-known-gaps/contracts/telemetry-outcomes.md
@.planning/phases/08-close-known-gaps/08-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `anansi.config._value_shape(raw)`
- `anansi.config._config_degradations(entry, effective)`
- `anansi.config.get_degradations()`
- telemetry outcome `config_degraded`

<tasks>

<task id="08-02-T1" type="auto" tdd="true">
  <name>Task 1: Detect coercions without retaining rejected values</name>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/reference/001-close-known-gaps/contracts/telemetry-outcomes.md
    anansi/config.py
    anansi/tests/test_drive_config.py
  </read_first>
  <files>anansi/config.py, anansi/tests/test_drive_config.py</files>
  <behavior>
    - Each provided known key that is rejected or clamped appears once in the latest degradation list.
    - The descriptor uses type and length only; the raw value is not retained.
    - Valid values and benign case/whitespace/numeric normalization produce no event.
    - get_cfg remains cached, defensive, and non-raising.
  </behavior>
  <action>Write table-driven failing tests covering malformed values for every existing scalar/list/choice key in the `get_cfg` result, including `drive_pressure`, plus valid and normalization-equivalent controls. Add `_value_shape(raw)`, `_config_degradations(entry, effective)`, and `get_degradations()` based on the candidate branch's shape-only concept. Keep the rejected object out of module state and output; cache only `(key, shape, applied_default)` records. A clamped numeric value counts as degraded, a parse-equivalent numeric string does not, an invalid list counts once for its key, and `reset_cache()` clears both caches. Do not import `store` from `config.py` and do not add a new config key.</action>
  <verify>
    <automated>./scripts/test.sh anansi/tests/test_drive_config.py -x</automated>
    <fails_when>non-zero exit, no collected tests, duplicate key events, or any rejected fixture literal appears in a descriptor</fails_when>
  </verify>
  <acceptance_criteria>
    - `get_degradations()` returns one shape-only record per degraded provided key after a forced load.
    - Valid input returns an empty degradation list.
    - Repeated cached `get_cfg()` calls return equal dictionaries and do not create additional descriptors.
    - Tests include a credential-shaped string and prove its literal does not appear in any descriptor.
  </acceptance_criteria>
  <done>Config coercion is deterministically discoverable without storing the rejected input.</done>
</task>

<task id="08-02-T2" type="auto" tdd="true">
  <name>Task 2: Emit degradation telemetry once and keep telemetry failure outside hook behavior</name>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/reference/001-close-known-gaps/contracts/telemetry-outcomes.md
    anansi/config.py
    anansi/__init__.py
    anansi/store.py
    anansi/tests/test_failopen_matrix.py
    anansi/tests/test_telemetry_store.py
  </read_first>
  <files>anansi/__init__.py, anansi/store.py, anansi/tests/test_failopen_matrix.py, anansi/tests/test_telemetry_store.py</files>
  <behavior>
    - on_session_start records one config_degraded row per descriptor from that reload.
    - Each error field is `<key>: rejected <shape>, applied <default>` using only safe scalar formatting.
    - config_degraded does not increment telemetry failure_count or become last_error.
    - When record_telemetry returns False or raises under a test double, session start and the next eligible pre_llm_call retain their established outputs.
  </behavior>
  <action>After `config.reset_cache()` in `on_session_start`, load config once, iterate `get_degradations()`, and call the existing `store.record_telemetry` with outcome `config_degraded`, the session id, and a shape-only error string. Enclose diagnostic emission in its own nested fail-open boundary so no telemetry exception reaches reflection or later hooks. Extend `telemetry_summary` so `config_degraded` is a non-failure observation alongside existing non-failure outcomes. Do not log raw configuration objects and do not emit on ordinary cached per-turn reads. Add a full-hook test that makes telemetry storage unavailable and compares the subsequent ordinary appraisal result with the available-storage control.</action>
  <verify>
    <automated>./scripts/test.sh anansi/tests/test_drive_config.py anansi/tests/test_failopen_matrix.py anansi/tests/test_telemetry_store.py -x</automated>
    <fails_when>non-zero exit, no collected tests, a malformed key does not map to exactly one row, or telemetry unavailability changes/raises from the hook</fails_when>
  </verify>
  <acceptance_criteria>
    - One session reload with N degraded keys stores exactly N `config_degraded` rows; a valid reload stores zero.
    - Stored error fields identify key, shape, and default but contain no raw rejected fixture.
    - `telemetry_summary()['failure_count']` excludes `config_degraded`, and `last_error` ignores it.
    - The unavailable-store full-hook case returns the same ordinary appraisal context as its control and never raises.
  </acceptance_criteria>
  <done>US-3 scenarios 1-3 and SC-004 have focused configuration, store, and hook evidence.</done>
</task>

</tasks>

<constitution_check>
Principles I, IV, V, VI, and VII are enforced through nested fail-open emission, the single telemetry store, zero dependencies, inspectable degradation, and focused tests. Principle II is unchanged because telemetry records configuration shape, not an instruction or action.
</constitution_check>

<verification>
Run `./scripts/test.sh anansi/tests/test_drive_config.py anansi/tests/test_failopen_matrix.py anansi/tests/test_telemetry_store.py -x`; any non-zero exit or zero collected tests blocks the plan.
</verification>

<success_criteria>
US-3 and SC-004 pass with one secret-safe observation per degraded key and no hook behavior change when diagnostics cannot be stored.
</success_criteria>

<output>
Create `.planning/phases/08-close-known-gaps/08-02-SUMMARY.md` when done.
</output>
