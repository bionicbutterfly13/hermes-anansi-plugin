---
phase: 08-close-known-gaps
plan: "05"
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - anansi/config.py
  - anansi/tests/test_drive_config.py
  - anansi/tests/test_failopen_matrix.py
  - CHANGELOG.md
autonomous: true
gap_closure: true
requirements:
  - REQ-001-close-known-gaps-fr-004
  - REQ-001-close-known-gaps-fr-005
  - REQ-001-close-known-gaps-fr-012
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Native positive and negative infinity for history_chars, max_tokens, reflect_every_n_turns, reflect_max_tokens, and drive_energy_budget never raise and each resolve to that key's documented default."
    - "Native NaN remains a non-raising invalid-input control, while finite valid values and the established lower and upper bounds retain their current effective values."
    - "Each affected provided key produces exactly one shape-only config_degraded row on a real on_session_start reload, with the key and applied default but no raw non-finite input."
    - "A cached get_cfg read neither reloads the host entry nor duplicates degradation records; reset_cache and the next session reload rebuild the same five records once for that load."
    - "The complete suite remains green with the prior consent, priority, firm-pressure, timing, fail-open, never-omit, and live-evidence-boundary repairs intact."
  artifacts:
    - path: anansi/config.py
      provides: "non-raising existing integer coercion boundary for native non-finite floats"
    - path: anansi/tests/test_drive_config.py
      provides: "table-driven integer-key defaults, bounds, NaN, infinity, and cache/reload regressions"
    - path: anansi/tests/test_failopen_matrix.py
      provides: "real on_session_start SQLite config_degraded telemetry and raw-input exclusion regressions"
    - path: CHANGELOG.md
      provides: "dated root-cause and learning note for the non-finite integer config gap"
  key_links:
    - from: anansi/config.py
      to: anansi/__init__.py
      via: "get_cfg builds the five integer fields and leaves shape-only degradation records for on_session_start"
    - from: anansi/__init__.py
      to: anansi/store.py
      via: "on_session_start emits one config_degraded telemetry row per record through the existing nested fail-open boundary"
    - from: anansi/tests/test_failopen_matrix.py
      to: anansi/config.py
      via: "temporary SQLite state and the real session hook prove reload-to-telemetry behavior rather than a descriptor-only unit path"
---

<objective>
Close the verified non-finite integer configuration gap across config loading, cache reload, and session-start telemetry at the completed Phase 8 integration head `50eed75`.

Purpose: Restore the REQ-001-close-known-gaps-fr-004 and REQ-001-close-known-gaps-fr-005 guarantee that malformed provided configuration is non-raising, inspectable, and secret-safe, then re-run the REQ-001-close-known-gaps-fr-012 constitutional gate.
Output: One surgical `_coerce_int` correction, table-driven regressions for all five integer keys, real SQLite telemetry evidence, and a dated technical learning record.
</objective>

<execution_context>
@/Users/manisaintvictor/.codex/gsd-core/workflows/execute-plan.md
@/Users/manisaintvictor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-close-known-gaps/08-CONTEXT.md
@.planning/phases/08-close-known-gaps/08-VERIFICATION.md
@.planning/phases/08-close-known-gaps/08-VALIDATION.md
@.planning/reference/CONSTITUTION.md
@.planning/phases/08-close-known-gaps/08-02-SUMMARY.md
@.planning/phases/08-close-known-gaps/08-04-SUMMARY.md
</context>

## Artifacts this phase produces

- `anansi.config._coerce_int` catches the complete conversion-exception family needed for native non-finite floats.
- Table-driven config tests cover the five existing integer keys without adding a helper or configuration surface.
- Full session-start tests persist shape-only degradation rows in a temporary SQLite database.
- A dated `CHANGELOG.md` Fixes/Learnings note records the exact root cause and reusable boundary lesson.

<tasks>

<task id="08-05-T1" type="tracer" tdd="true">
  <name>Task 1: Carry native non-finite integer config safely from reload to SQLite telemetry</name>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/phases/08-close-known-gaps/08-VERIFICATION.md
    .planning/phases/08-close-known-gaps/08-02-SUMMARY.md
    .planning/phases/08-close-known-gaps/08-04-SUMMARY.md
    anansi/config.py
    anansi/__init__.py
    anansi/tests/test_drive_config.py
    anansi/tests/test_failopen_matrix.py
    CHANGELOG.md
  </read_first>
  <files>anansi/config.py, anansi/tests/test_drive_config.py, anansi/tests/test_failopen_matrix.py, CHANGELOG.md</files>
  <behavior>
    - For each of `history_chars`, `max_tokens`, `reflect_every_n_turns`, `reflect_max_tokens`, and `drive_energy_budget`, native `float("inf")` and `float("-inf")` return the exact documented default and produce one degradation record instead of raising.
    - Native `float("nan")` remains the invalid non-raising control for every integer key; valid numeric strings remain normalization-equivalent and produce no degradation.
    - Existing finite clamp policy is unchanged: history_chars has floor 0/default 4000; max_tokens has floor 1/default 700; reflect_every_n_turns has bounds 1..50/default 5; reflect_max_tokens has floor 1/default 700; drive_energy_budget has floor 0/default 3.
    - A cache hit returns the same effective dictionary and degradation list without a second host-entry load; reset_cache clears both, and the next real session-start load reconstructs them once.
    - Real `on_session_start` calls against temporary SQLite state persist exactly five `config_degraded` rows for an entry containing all five affected keys, for both signs of infinity; every row contains the key, `<float>` shape, and exact applied default, with no raw `inf` or `-inf` representation.
  </behavior>
  <action>Start RED in `test_drive_config.py` with a table keyed by the five existing configuration names, their exact defaults, valid normalization controls, and current finite bounds. Exercise `get_cfg(force_reload=True)` with native positive infinity, negative infinity, and NaN for every row; assert the effective value, one `(key, "<float>", applied_default)` degradation record, and no exception. Extend the existing cached-read coverage with a counted `_load_host_entry` double so a cache hit does not reload or duplicate records, `reset_cache()` clears both caches, and the next load reconstructs one record per provided key. Keep the current valid numeric-string controls and finite clamp assertions unchanged.

In `test_failopen_matrix.py`, add table-driven positive/negative infinity cases that supply all five affected keys in one host entry, call the real `anansi.on_session_start` against the fixture's temporary SQLite database, and query actual `config_degraded` rows. Require exactly one row per key per reload with the existing shape-only message format and exact defaults `4000`, `700`, `5`, `700`, and `3`; assert that neither raw non-finite representation appears. Call `on_session_start` twice with distinct session ids and partition the rows by session id to prove each session resets the cache, reloads the same entry once, and emits exactly five fresh rows, while an ordinary cached `get_cfg()` call emits nothing itself. Preserve the existing unavailable-telemetry full-hook comparison unchanged.

Make the production fix only in `_coerce_int`: include `OverflowError` in the existing `int(value)` conversion exception boundary and return `default`, matching its current TypeError/ValueError path. Leave finite floor/ceiling logic, `_coerce_float`, `_number_degraded`, descriptor shape, hook emission, SQLite vocabulary, and every config key untouched. Do not modify `anansi/__init__.py`, `anansi/store.py`, drive rendering, persisted consent/priority handling, firm ordering, timing, or the live-smoke record. After focused and full tests pass, add a dated technical `CHANGELOG.md` Fixes/Learnings entry: the root cause is that `int()` raises `OverflowError` for native infinity, so all five integer keys aborted `get_cfg` before secret-safe descriptors and telemetry could exist; record that defensive integer coercion must cover that conversion exception without changing finite bounds. Add no dependency, provider call, host access, public prose, or metadata-dependent runtime assertion.</action>
  <verify>
    <automated>./scripts/test.sh</automated>
    <fails_when>the command exits non-zero; no tests are collected; any affected infinity or NaN case raises; an affected key resolves to the wrong default or changes a finite bound; a reload omits or duplicates a key's row; any stored message contains the raw non-finite input; a cache hit reloads the host entry; reset/reload retains stale records; or any existing constitutional regression is weakened or skipped</fails_when>
  </verify>
  <acceptance_criteria>
    - All ten native positive/negative infinity key cases and the five NaN controls return the documented defaults without raising.
    - Existing valid numeric-string values remain accepted without degradation, and finite lower/upper clamping remains byte-for-byte behaviorally unchanged.
    - Each real session reload containing all five invalid integer values stores exactly five rows under its session id, each with the existing shape-only message and exact applied default.
    - Cached reads preserve one load's records without side effects; reset and the next session reload rebuild records from the new host entry only.
    - No raw rejected input is retained in config module state or persisted telemetry.
    - `./scripts/test.sh` exits 0 with the prior consent, priority, firm-pressure, timing, fail-open, never-omit, and offline live-boundary tests still enabled.
    - `CHANGELOG.md` records the config-specific root cause and lesson without claiming live-provider evidence.
  </acceptance_criteria>
  <done>REQ-001-close-known-gaps-fr-004, REQ-001-close-known-gaps-fr-005, and REQ-001-close-known-gaps-fr-012 pass for the complete native non-finite integer key family from config reload through persisted session telemetry.</done>
</task>

</tasks>

<constitution_check>
Principle I is restored by making every affected integer config non-raising and keeping telemetry loss isolated. Principle V is enforced by defensive coercion with no dependency or path change. Principle VII is enforced by one production-line exception-boundary correction, failure-first tests, the canonical suite, and a dated learning note. Principles II, III, IV, and VI remain unchanged and are protected by the full regression gate.
</constitution_check>

<source_coverage_audit>
## Multi-Source Coverage Audit

- GOAL: Plans 08-01 through 08-04 already reconcile the eight known gaps against main; 08-05 closes the verifier's sole new config boundary failure without reopening the completed consent, priority, pressure, timing, terminology, or evidence-lane work.
- REQ: 08-05 covers the blocked REQ-001-close-known-gaps-fr-004 and REQ-001-close-known-gaps-fr-005 behaviors and repeats REQ-001-close-known-gaps-fr-012 through the canonical suite. The verifier marks FR-001..003 and FR-006..012 satisfied, so they are excluded from gap replanning except the explicit FR-012 regression gate.
- RESEARCH: 08-05 follows the existing pure cached config coercion, shape-only degradation, nested session telemetry, temporary SQLite, zero-dependency, and canonical-test patterns. It adds no provider, host, service, storage, rendering, or configuration surface.
- CONTEXT: Constitution Principles I-VII remain binding; the firm-pressure decision and every prior selected branch repair remain intact. Provider access and the real exit-0 live proof stay deferred to Phase 14 and `08-LIVE-SMOKE.md` remains UNRUN.
- VERIFICATION: Failed truths 7 and 8, the partial `config.py`, `test_drive_config.py`, and `test_failopen_matrix.py` artifacts, and the partial config-to-session-telemetry link are all exercised by 08-05-T1. The separately deferred provider truth is excluded.
</source_coverage_audit>

<verification>
Run `./scripts/test.sh anansi/tests/test_drive_config.py anansi/tests/test_failopen_matrix.py -x`, then run `./scripts/test.sh`. Either non-zero exit, zero collected tests, a missing or raw-bearing telemetry row, or a changed constitutional behavior blocks completion.
</verification>

<success_criteria>
The five integer configuration keys accept native positive infinity, negative infinity, and NaN as invalid inputs without raising, preserve their exact documented defaults and finite bounds, emit one secret-safe row per provided key on each session reload, and leave the complete 194-test-era constitutional behavior green without relying on a historical test count.
</success_criteria>

<output>
Create `.planning/phases/08-close-known-gaps/08-05-SUMMARY.md` when done.
</output>
