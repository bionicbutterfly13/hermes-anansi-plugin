---
phase: 08-close-known-gaps
plan: "03"
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - anansi/store.py
  - anansi/render.py
  - anansi/__init__.py
  - anansi/tests/test_drive_neveromit.py
  - anansi/tests/test_drive_store.py
  - anansi/tests/test_drive_velocity.py
  - anansi/tests/test_failopen_matrix.py
autonomous: true
requirements:
  - REQ-001-close-known-gaps-fr-006
  - REQ-001-close-known-gaps-fr-007
  - REQ-001-close-known-gaps-fr-008
  - REQ-001-close-known-gaps-fr-009
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "US-4/SC-005 and probe FR-006 boundary: a successful appraisal with an empty signal mapping still renders every persisted flagged, active priority; actual appraisal failure remains an empty injection."
    - "Probe FR-006 precision and FR-007 boundary/precision: more than 50 flagged active priorities survive persistence, domain filtering, and the full hook path; the cap applies only to unflagged rows and no omission marker replaces user state."
    - "Candidate and retired goals remain inert even when flagged; an active flagged priority may bypass the model-derived domain whitelist, while unflagged goals remain domain-contained."
    - "Drive-off emits no drive fields and preserves the ordinary appraisal output byte-for-byte against the same appraisal with no goals."
    - "US-5 and probe FR-012 adjacency/empty: goal association requires a non-empty whole-token subset match; empty or null text associates nothing and substrings do not match."
    - "US-6 and probe FR-012 ordering: persisted read-time stalled_days, including zero, overrides model timing; flagged wants sort first, then stalled ahead of fresh, with stable order for ties."
    - "Probe FR-008 empty: zero-day freshness is represented as fresh evidence rather than unknown or stalled."
  artifacts:
    - path: anansi/store.py
      provides: "flag-preserving goal retention and persisted read-time momentum"
    - path: anansi/render.py
      provides: "constitution-first flagged rendering, token association, and stable ordering"
    - path: anansi/tests/test_drive_neveromit.py
      provides: "persisted full-hook anti-erasure regressions"
    - path: anansi/tests/test_drive_velocity.py
      provides: "matching, freshness, override, and ordering regressions"
  key_links:
    - from: anansi/store.py
      to: anansi/__init__.py
      via: "read_snapshot preserves all active flagged priorities before hook domain selection"
    - from: anansi/__init__.py
      to: anansi/render.py
      via: "flagged active goals bypass domain filtering and reach render_block even when signals is an empty mapping"
    - from: anansi.render.enrich_goal_signals
      to: anansi.render.render_block
      via: "whole-token association and persisted momentum determine displayed ordering"
  assumptions:
    - id: EDGE-FR008-encoding
      status: flagged-unresolved
      statement: "The source specifies whole-token containment but no Unicode normalization contract; Phase 8 uses deterministic ASCII alphanumeric tokens and does not infer broader normalization."
    - id: EDGE-FR009-unclassified
      status: flagged-unresolved
      statement: "The fallback probe classified FR-009 as unclassified; the plan proves persisted timing authority and stable ordering without inventing another freshness state."
  prohibitions:
    - statement: "Storage caps, domain filtering, or model omission must not erase confirmed persisted flagged priorities, and rendering must not promote candidate or retired goals."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Enforce the constitution's never-omit guarantee across persistence, filtering, successful empty-signal appraisal, and rendering, then make goal matching and timing use precise persisted evidence.

Purpose: Close US-4 through US-6 without weakening fail-open behavior or claiming direct-renderer tests prove persisted state survives.
Output: End-to-end anti-erasure behavior plus deterministic matching, freshness, and ordering regressions.
</objective>

<execution_context>
@/Users/manisaintvictor/.codex/gsd-core/workflows/execute-plan.md
@/Users/manisaintvictor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/INGEST-CONFLICTS.md
@.planning/phases/08-close-known-gaps/08-CONTEXT.md
@.planning/phases/08-close-known-gaps/08-RESEARCH.md
@.planning/phases/08-close-known-gaps/08-PATTERNS.md
@.planning/reference/CONSTITUTION.md
@.planning/reference/001-close-known-gaps/spec.md
@.planning/phases/08-close-known-gaps/08-02-SUMMARY.md
</context>

## Artifacts this phase produces

- `anansi.store._enforce_goal_cap(conn)` with flagged-row exemption
- `anansi.render._GOAL_WORD_RE`
- `anansi.render._goal_tokens(text)`
- `anansi.render._goal_text_matches(goal_text, signal_text)`
- persisted full-hook anti-erasure and ground-truth-at-read-time regression cases

<tasks>

<task id="08-03-T1" type="auto" tdd="true">
  <name>Task 1: Preserve flagged priorities from storage through an empty-signal hook result</name>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/INGEST-CONFLICTS.md
    .planning/phases/08-close-known-gaps/08-RESEARCH.md
    anansi/store.py
    anansi/__init__.py
    anansi/render.py
    anansi/tests/test_drive_store.py
    anansi/tests/test_drive_neveromit.py
  </read_first>
  <files>anansi/store.py, anansi/__init__.py, anansi/render.py, anansi/tests/test_drive_store.py, anansi/tests/test_drive_neveromit.py</files>
  <behavior>
    - Fifty-five active flagged priorities plus sixty unflagged goals round-trip through apply_deltas and read_snapshot with every flagged priority intact.
    - An active flagged priority outside inferred domains reaches the full pre_llm_call result; an equally flagged candidate or retired goal does not.
    - A successful fake-model result with `signals={}` renders all active flagged wants using the protected literal prefix and no cap/withheld marker.
    - A fake appraisal failure represented by `signals=None` injects nothing.
    - With drive disabled, the ordinary appraisal block equals the no-goals control byte-for-byte and contains no drive fields.
  </behavior>
  <action>First replace the lower-authority empty-signal assertion in `test_drive_neveromit.py` with constitution-authoritative full-hook cases that create state through `apply_deltas`, reload it through `read_snapshot`, and use deterministic fake appraisal results. Add `_enforce_goal_cap(conn)` in `store.py` so the configured goals cap limits only unflagged rows; confirmed flagged rows are exempt and remain persisted. In `__init__.py`, preserve active flagged non-candidate priorities through domain selection while keeping unflagged goals subject to the existing domain filter and candidate/retired rows inert. In `render.py`, compute active flagged wants before suppressing a successful empty signal mapping and render them without an aggregate cap or omission marker. Preserve the actual-failure `signals is None` empty-injection path, all kill switches, and the existing fail-open exception boundaries. Do not claim anti-erasure from direct renderer input alone.</action>
  <verify>
    <automated>./scripts/test.sh anansi/tests/test_drive_store.py anansi/tests/test_drive_neveromit.py -x</automated>
    <fails_when>non-zero exit, no collected tests, any persisted flagged active id disappears, a candidate/retired goal renders, or an actual appraisal failure injects content</fails_when>
  </verify>
  <acceptance_criteria>
    - A separately read snapshot contains all 55 flagged active priorities after unflagged cap pressure.
    - The full hook result for successful `signals={}` contains one protected want line for every flagged active priority, including an out-of-domain goal.
    - Candidate and retired controls never appear, and unflagged out-of-domain controls remain filtered.
    - The `signals=None` failure case is `None`; drive-off returns the same ordinary appraisal text as the no-goals control.
  </acceptance_criteria>
  <done>US-4 scenarios, the model-omission and crowding edges, FR-006/FR-007, and SC-005 are proven across persistence and the real hook seam.</done>
</task>

<task id="08-03-T2" type="auto" tdd="true">
  <name>Task 2: Use whole-token matching and persisted timing for drive evidence</name>
  <read_first>
    .planning/reference/CONSTITUTION.md
    .planning/phases/08-close-known-gaps/08-RESEARCH.md
    anansi/render.py
    anansi/tests/test_drive_velocity.py
    anansi/tests/test_failopen_matrix.py
  </read_first>
  <files>anansi/render.py, anansi/tests/test_drive_velocity.py, anansi/tests/test_failopen_matrix.py</files>
  <behavior>
    - Goal `ship api` matches signal text containing both whole tokens in any order and does not match `shipping capillary`.
    - Empty or null goal/signal text yields no association and no exception.
    - Persisted stalled_days replaces a contradictory model value; persisted zero produces fresh wording and sorts below stalled evidence.
    - If persistence has no timing evidence, a model-supplied stalled_days value is removed rather than treated as ground truth.
    - Flagged wants sort ahead of stalled and fresh notes; equal-rank inputs preserve source order.
  </behavior>
  <action>Write focused regressions before replacing loose substring checks with `_GOAL_WORD_RE`, `_goal_tokens(text)`, and `_goal_text_matches(goal_text, signal_text)`. Tokenize case-insensitively with deterministic ASCII alphanumeric tokens; require the non-empty goal-token set to be a subset of signal tokens, and use the same predicate for association and duplicate suppression. In `enrich_goal_signals`, make the persisted goal row authoritative at read time: copy an integer `stalled_days`, including zero, and remove model timing when persistence has no value. Keep neutral observational wording. Sort rendered items stably by flagged first, then positive stalled evidence, then zero-day freshness, retaining source order inside equal ranks. Keep exceptions fail-open and do not introduce directives, Unicode normalization, or a dependency.</action>
  <verify>
    <automated>./scripts/test.sh anansi/tests/test_drive_velocity.py anansi/tests/test_failopen_matrix.py -x</automated>
    <fails_when>non-zero exit, no collected tests, substring-only text associates, zero is treated as missing, model timing overrides persistence, or equal-rank ordering changes</fails_when>
  </verify>
  <acceptance_criteria>
    - Whole-token positive, substring negative, case-folding, empty, and null fixtures pass.
    - Persisted values `0`, positive integer, and `None` each produce the specified model-override behavior.
    - The renderer places flagged wants first, stalled notes second, fresh notes third, with deterministic stable ties.
    - Existing fail-open matrix cases remain green.
  </acceptance_criteria>
  <done>US-5, US-6, FR-008, FR-009, and the all-fresh edge have precise automated evidence.</done>
</task>

</tasks>

<constitution_check>
Principle III overrides the historical empty-signal suppression and aggregate-cap assertions. Principles I, II, IV, V, VI, and VII remain binding through failure-only empty injection, inert candidate state, persisted read-time authority, the existing SQLite surface, observational wording, and focused tests.
</constitution_check>

<verification>
Run `./scripts/test.sh anansi/tests/test_drive_store.py anansi/tests/test_drive_neveromit.py anansi/tests/test_drive_velocity.py anansi/tests/test_failopen_matrix.py -x`; any non-zero exit or zero collected tests blocks the plan.
</verification>

<success_criteria>
US-4 through US-6 and SC-005 pass end to end, including more than 50 persisted flagged priorities, successful empty model signals, candidate inertness, precise association, and read-time timing authority.
</success_criteria>

<output>
Create `.planning/phases/08-close-known-gaps/08-03-SUMMARY.md` when done.
</output>
